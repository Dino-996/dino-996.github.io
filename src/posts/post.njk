---
pagination:
  data: posts
  size: 1
  alias: post
  addAllPagesToCollections: true
layout: post.njk
permalink: "blog/{{ post.slug | slugify }}/index.html"
tags:
  - posts
eleventyComputed:
  title: "{{ post.title | safe }}"
  description: "{{ post.description | safe }}"
  postDate: "{{ post.date }}"
  updatedAt: "{{ post.updatedAt }}"
  image: "{{ post.image }}"
  imageAlt: "{{ post.imageAlt }}"
  strapiTags: "{{ post.strapiTags | safe }}"
  author: "{{ post.author | safe }}"
  authorAvatar: "{{ post.authorAvatar }}"
---

{% if post.content %}{{ post.content | markdownBlock | safe }}{% endif %}
