<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title>DinoSec</title>
  <subtitle>Developer portfolio e blog tecnico</subtitle>
  <link href="https://dino-996.github.io/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="https://dino-996.github.io" rel="alternate" type="text/html"/>
  <id>https://dino-996.github.io/</id>
  <updated>2026-09-21T00:00:00.000Z</updated>
  <author>
    <name>Davide Sabia</name>
    <email>davidesabia22@gmail.com</email>
    <uri>https://dino-996.github.io</uri>
  </author>

  
  <entry>
    <title>Input e Output in assembly x86_64</title>
    <link href="https://dino-996.github.io/blog/input-e-output-in-assembly-x86-64/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/input-e-output-in-assembly-x86-64/</id>
    <published>2026-09-21T00:00:00.000Z</published>
    <updated>2026-09-21T00:00:00.000Z</updated>
    
    <summary>Guida pratica all&#39;uso delle principali syscall Linux (write, read, open, exit) in Assembly x86_64. L&#39;articolo illustra le convenzioni di chiamata nei registri, l&#39;allocazione dinamica di buffer sullo stack e le tecniche di indirizzamento RIP-relative con .asciz e lea.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;la-chiamata-write-stampare-a-schermo&quot;&gt;La chiamata write, stampare a schermo&lt;/h2&gt;
&lt;p&gt;Quando un programma deve mostrare del testo, non invia i caratteri uno alla volta: passa al kernel l&#39;indirizzo di memoria del blocco da stampare, la sua lunghezza e il canale di output, in un&#39;unica invocazione della syscall &lt;code&gt;write&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;L&#39;equivalente concettuale in linguaggio C è &lt;code&gt;write(file_descriptor, indirizzo_memoria, lunghezza)&lt;/code&gt;. In assembly x86_64 su Linux, i parametri vanno caricati nei registri prima di eseguire l&#39;istruzione &lt;code&gt;syscall&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Registro&lt;/th&gt; &lt;th&gt;Ruolo nella write&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rax&lt;/td&gt; &lt;td&gt;ID della syscall, deve valere 1&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdi&lt;/td&gt; &lt;td&gt;File descriptor di destinazione: 0 stdin, 1 stdout, 2 stderr&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rsi&lt;/td&gt; &lt;td&gt;Indirizzo di memoria da cui iniziare a leggere i caratteri&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdx&lt;/td&gt; &lt;td&gt;Numero esatto di byte da stampare&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;leggere-gli-argomenti-da-riga-di-comando&quot;&gt;Leggere gli argomenti da riga di comando&lt;/h3&gt;
&lt;p&gt;Quando si avvia un programma passando un parametro, ad esempio &lt;code&gt;./programma H&lt;/code&gt;, Linux carica queste informazioni sullo stack. Lo stack non contiene direttamente il carattere &lt;code&gt;&#39;H&#39;&lt;/code&gt;, ma un puntatore che indica dove si trova quel testo in memoria. Nel contesto di questa serie di esercizi, il puntatore al primo argomento si trova alla posizione relativa &lt;code&gt;[rsp+16]&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Per preparare l&#39;output basta copiare quel puntatore nel registro dedicato ai dati e impostare gli altri parametri per stampare un solo carattere su stdout.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov rax,1
mov rdi,1
mov rsi,[rsp+16]
mov rdx,1
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Con &lt;code&gt;rdi&lt;/code&gt; a 1, &lt;code&gt;rdx&lt;/code&gt; a 1 e &lt;code&gt;rax&lt;/code&gt; a 1, la syscall stampa a terminale esattamente il carattere passato da riga di comando.&lt;/p&gt;
&lt;h3 id=&quot;concatenare-write-ed-exit-chiudere-il-programma-correttamente&quot;&gt;Concatenare write ed exit, chiudere il programma correttamente&lt;/h3&gt;
&lt;p&gt;Un programma che esegue solo la &lt;code&gt;write&lt;/code&gt; e non termina esplicitamente continua a far leggere alla CPU le locazioni di memoria successive, eseguendo istruzioni non valide e andando in crash. Per gestire correttamente il ciclo di vita del processo occorre invocare due syscall in sequenza nello stesso flusso: prima &lt;code&gt;write&lt;/code&gt; per l&#39;output, poi &lt;code&gt;exit&lt;/code&gt; per la chiusura.&lt;/p&gt;
&lt;p&gt;La seconda fase riutilizza i registri con valori diversi:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;rdi&lt;/code&gt; = il codice di uscita da restituire al sistema operativo (da 0 a 255)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;rax&lt;/code&gt; = 60, l&#39;ID numerico della syscall &lt;code&gt;sys_exit&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov rax,1
mov rdi,1
mov rsi,[rsp+16]
mov rdx,1
syscall
mov rdi,42
mov rax,60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La struttura completa richiede in totale otto istruzioni, cinque per la fase di scrittura e tre per la terminazione, e garantisce che l&#39;output venga mostrato prima che il processo si chiuda con stato 42.&lt;/p&gt;
&lt;h3 id=&quot;scrivere-stringhe-piu-lunghe-di-un-singolo-byte&quot;&gt;Scrivere stringhe più lunghe di un singolo byte&lt;/h3&gt;
&lt;p&gt;Per scrivere un testo più lungo, ad esempio una stringa di 64 caratteri già presente in memoria, non serve cambiare la logica del programma: basta estendere il valore caricato in &lt;code&gt;rdx&lt;/code&gt;, il registro che dice al kernel quanti byte consecutivi leggere a partire dall&#39;indirizzo puntato da &lt;code&gt;rsi&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov rax,1
mov rdi,1
mov rsi,[rsp+16]
mov rdx,64
syscall
mov rdi,42
mov rax,60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;L&#39;unica variazione rispetto al blocco precedente è il valore di &lt;code&gt;rdx&lt;/code&gt;, che passa da 1 a 64: tutto il resto del flusso, incluse la chiusura con &lt;code&gt;exit&lt;/code&gt; e i valori di &lt;code&gt;rdi&lt;/code&gt; e &lt;code&gt;rax&lt;/code&gt; per la write, resta identico.&lt;/p&gt;
&lt;h2 id=&quot;la-chiamata-read-leggere-input-da-stdin&quot;&gt;La chiamata read, leggere input da stdin&lt;/h2&gt;
&lt;p&gt;La syscall &lt;code&gt;read&lt;/code&gt; compie il percorso opposto rispetto a &lt;code&gt;write&lt;/code&gt;: quando il programma la invoca, è il kernel a trasferire i byte ricevuti dallo standard input all&#39;interno di un&#39;area di memoria del programma. La sintassi concettuale è &lt;code&gt;read(file_descriptor, indirizzo_memoria, lunghezza)&lt;/code&gt;&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Registro&lt;/th&gt; &lt;th&gt;Ruolo nella read&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rax&lt;/td&gt; &lt;td&gt;ID della syscall, deve valere 0&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdi&lt;/td&gt; &lt;td&gt;File descriptor sorgente, 0 per stdin&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rsi&lt;/td&gt; &lt;td&gt;Indirizzo del buffer in cui salvare i caratteri ricevuti&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdx&lt;/td&gt; &lt;td&gt;Numero massimo di byte da raccogliere dall&#39;input&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;lo-stack-come-buffer-temporaneo&quot;&gt;Lo stack come buffer temporaneo&lt;/h3&gt;
&lt;p&gt;I dati letti devono finire in un&#39;area di RAM valida e scrivibile. Il registro &lt;code&gt;rsp&lt;/code&gt; punta alla sommità dello stack, una zona di memoria già allocata per il programma e liberamente utilizzabile come buffer. A differenza degli esempi precedenti, in cui si caricava il valore contenuto nell&#39;indirizzo dello stack con &lt;code&gt;mov rsi, [rsp+16]&lt;/code&gt;, qui serve l&#39;indirizzo stesso:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rsi,rsp
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;code&gt;mov rsi, rsp&lt;/code&gt; copia nel registro &lt;code&gt;rsi&lt;/code&gt; il valore che &lt;code&gt;rsp&lt;/code&gt; contiene in quel momento, cioè l&#39;indirizzo della sommità dello stack: &lt;code&gt;rsi&lt;/code&gt; diventa un puntatore a quella zona di memoria, pronta per essere riempita dalla &lt;code&gt;read&lt;/code&gt;.&lt;br&gt;
&lt;code&gt;mov rsi, [rsp+16]&lt;/code&gt;, invece, non copia un indirizzo ma il &lt;strong&gt;contenuto&lt;/strong&gt; presente nella memoria a quella posizione: negli articoli precedenti, in quella locazione lo stack conteneva già un puntatore (all&#39;argomento da riga di comando), quindi l&#39;istruzione dereferenziava quella cella per recuperarlo. Con &lt;code&gt;read&lt;/code&gt; non esiste ancora nessun dato scritto in memoria da recuperare, quindi serve l&#39;indirizzo dello stack stesso come destinazione, non il contenuto di una sua cella.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;il-flusso-completo-a-tre-fasi&quot;&gt;Il flusso completo a tre fasi&lt;/h3&gt;
&lt;p&gt;Concatenando lettura, scrittura e uscita si ottiene un programma che legge fino a 255 byte da stdin, li ristampa a schermo e chiude il processo con stato 42.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
scrittura (write)
mov rax,0
mov rdi,0
mov rsi,rsp
mov rdx,255
syscall
# lettura (read)
mov rax,1
mov rdi,1
mov rsi,rsp
mov rdx,255
# uscita (exit)
syscall
mov rdi,42
mov rax,60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;lettura-a-lunghezza-variabile&quot;&gt;Lettura a lunghezza variabile&lt;/h2&gt;
&lt;p&gt;Nei programmi reali la quantità di dati in arrivo da stdin non è quasi mai nota a priori. Specificare un numero rigido di byte nella &lt;code&gt;write&lt;/code&gt; porta a due problemi: stampare meno dati del dovuto se l&#39;input è lungo, oppure stampare byte residui presenti sullo stack se l&#39;input è più corto del previsto.&lt;/p&gt;
&lt;p&gt;La soluzione sfrutta il valore che il kernel scrive in &lt;code&gt;rax&lt;/code&gt; al termine di ogni syscall. Per &lt;code&gt;read&lt;/code&gt;, quel valore corrisponde al numero effettivo di byte letti: se si chiedono 128 byte ma l&#39;input ne contiene solo 50, &lt;code&gt;rax&lt;/code&gt; conterrà 50 al termine dell&#39;operazione.&lt;/p&gt;
&lt;p&gt;Trasferendo quel valore in &lt;code&gt;rdx&lt;/code&gt; prima della &lt;code&gt;write&lt;/code&gt;, si stampa esattamente la quantità di dati ricevuta.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov rax,0
mov rdi,0
mov rsi,rsp
mov rdx,128
syscall
mov rdx,rax
mov rax,1
mov rdi,1
mov rsi,rsp
syscall
mov rdi,42
mov rax,60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;L&#39;ordine delle istruzioni è critico: &lt;code&gt;mov rdx, rax&lt;/code&gt; deve avvenire prima di &lt;code&gt;mov rax, 1&lt;/code&gt;, altrimenti il conteggio dei byte letti verrebbe sovrascritto dall&#39;ID della syscall &lt;code&gt;write&lt;/code&gt; prima di poter essere copiato.&lt;/p&gt;
&lt;h2 id=&quot;aprire-file-su-disco-con-open&quot;&gt;Aprire file su disco con open&lt;/h2&gt;
&lt;p&gt;Fino a questo punto il programma ha lavorato solo con i tre canali standard già aperti all&#39;avvio: stdin (0), stdout (1) e stderr (2). Per leggere un file memorizzato su disco occorre prima chiedere a Linux di aprirlo tramite la syscall &lt;code&gt;open&lt;/code&gt;, la cui struttura concettuale è &lt;code&gt;open(percorso_file, modalita)&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Registro&lt;/th&gt; &lt;th&gt;Ruolo nella open&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rax&lt;/td&gt; &lt;td&gt;ID della syscall, deve valere 2&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdi&lt;/td&gt; &lt;td&gt;Puntatore al percorso del file in RAM&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rsi&lt;/td&gt; &lt;td&gt;Modalità di accesso, 0 indica la sola lettura&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Eseguita la syscall, Linux restituisce in &lt;code&gt;rax&lt;/code&gt; un nuovo file descriptor, un intero a partire da 3, collegato al file appena aperto.&lt;/p&gt;
&lt;h3 id=&quot;il-flusso-completo-a-quattro-fasi&quot;&gt;Il flusso completo a quattro fasi&lt;/h3&gt;
&lt;p&gt;Il file descriptor restituito da &lt;code&gt;open&lt;/code&gt; va spostato subito nel primo argomento della &lt;code&gt;read&lt;/code&gt; con &lt;code&gt;mov rdi, rax&lt;/code&gt;, altrimenti la lettura successiva continuerebbe a puntare a stdin.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
# apertura file (open)
mov rax,2
mov rdi,[rsp+16]
mov rsi,0
syscall
mov rdi,rax
mov rax,0
mov rsi,rsp
mov rdx,256
syscall
mov rdx,rax
mov rax,1
mov rdi,1
mov rsi,rsp
syscall
mov rdi,42
mov rax,60
syscall

&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;costruire-un-percorso-file-manualmente-nello-stack&quot;&gt;Costruire un percorso file manualmente nello stack&lt;/h2&gt;
&lt;p&gt;Quando l&#39;indirizzo del file non arriva dagli argomenti da riga di comando, occorre posizionare manualmente i byte del percorso in un&#39;area scrivibile come lo stack. Per la stringa &lt;code&gt;/flag&lt;/code&gt; si applicano tre regole fondamentali.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Direttiva &lt;code&gt;BYTE PTR&lt;/code&gt;&lt;/strong&gt;: quando si sposta un valore immediato, un carattere tra apici come &lt;code&gt;&#39;f&#39;&lt;/code&gt; o un numero, in una locazione di memoria come &lt;code&gt;[rsp+N]&lt;/code&gt;, l&#39;assembler non conosce da solo la dimensione della destinazione. &lt;code&gt;BYTE PTR&lt;/code&gt; specifica che l&#39;operazione deve occupare esattamente un byte.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Codifica ASCII&lt;/strong&gt;: i singoli apici permettono di scrivere caratteri leggibili, come &lt;code&gt;&#39;f&#39;&lt;/code&gt;, che la CPU interpreta direttamente come valori ASCII esadecimali, ad esempio &lt;code&gt;0x66&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Terminatore nullo&lt;/strong&gt;: l&#39;ultimo byte della stringa deve essere impostato a 0. Linux legge il percorso a partire dall&#39;indirizzo fornito e si arresta solo quando incontra un byte nullo; in sua assenza la &lt;code&gt;open&lt;/code&gt; proseguirebbe leggendo la memoria sovrastante, tentando di aprire un file con un nome invalido.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov BYTE PTR [rsp],&#39;/&#39;
mov BYTE PTR [rsp+1],&#39;f&#39;
mov BYTE PTR [rsp+2],&#39;l&#39;
mov BYTE PTR [rsp+3],&#39;a&#39;
mov BYTE PTR [rsp+4],&#39;g&#39;
mov BYTE PTR [rsp+5],0
mov rax,2
mov rdi,rsp
mov rsi,0
syscall
mov rdi,rax
mov rax,0
mov rsi,rsp
mov rdx,256
syscall
mov rdx,rax
mov rax,1
mov rdi,1
mov rsi,rsp
syscall
mov rdi,42
mov rax,60
syscall

&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;stringhe-rip-relative-con-asciz-e-lea&quot;&gt;Stringhe RIP-relative con asciz e lea&lt;/h2&gt;
&lt;p&gt;Scrivere stringhe byte per byte nello stack funziona, ma è lungo e aumenta il rischio di errori. L&#39;assembler permette di posizionare dati statici direttamente nel binario, usando la direttiva &lt;code&gt;.asciz &amp;quot;/flag&amp;quot;&lt;/code&gt;, che inserisce la sequenza di caratteri ASCII aggiungendo automaticamente il byte nullo finale. Per sicurezza, la stringa va posizionata dopo la syscall di &lt;code&gt;exit&lt;/code&gt;, così il processo termina prima di raggiungere quei byte, evitando che la CPU tenti di eseguirli come istruzioni.&lt;/p&gt;
&lt;h3 id=&quot;indirizzamento-rip-relative&quot;&gt;Indirizzamento RIP-relative&lt;/h3&gt;
&lt;p&gt;Nei sistemi moderni la posizione del programma in memoria RAM varia a ogni esecuzione, quindi l&#39;assembler non conosce l&#39;indirizzo assoluto della stringa, ma conosce la distanza tra l&#39;istruzione corrente e la stringa stessa, calcolata rispetto al registro &lt;code&gt;rip&lt;/code&gt;, l&#39;Instruction Pointer.&lt;/p&gt;
&lt;p&gt;Per caricare l&#39;indirizzo corretto si usa l&#39;istruzione &lt;code&gt;lea&lt;/code&gt;, Load Effective Address, e non &lt;code&gt;mov&lt;/code&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;mov rdi, [rip + path]&lt;/code&gt; è errato: legge il contenuto della memoria a quell&#39;indirizzo, quindi &lt;code&gt;rdi&lt;/code&gt; riceverebbe i primi otto byte della stringa ASCII anziché il suo puntatore.&lt;/li&gt;
&lt;li&gt;&lt;code&gt;lea rdi, [rip + path]&lt;/code&gt; è corretto: calcola l&#39;indirizzo di memoria in cui risiede la stringa e lo salva in &lt;code&gt;rdi&lt;/code&gt;, fornendo al kernel il puntatore necessario per la &lt;code&gt;open&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
lea rdi,[rip+path]
mov rax,2
mov rsi,0
syscall
mov rdi,rax
mov rax,0
mov rsi,rsp
mov rdx,256
syscall
mov rdx,rax
mov rax,1
mov rdi,1
mov rsi,rsp
syscall
mov rdi,42
mov rax,60
syscall
path:
.asciz &amp;quot;/flag&amp;quot;

&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;riepilogo-delle-syscall-trattate&quot;&gt;Riepilogo delle syscall trattate&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Syscall&lt;/th&gt; &lt;th&gt;Valore rax&lt;/th&gt; &lt;th&gt;Funzione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;read&lt;/td&gt; &lt;td&gt;0&lt;/td&gt; &lt;td&gt;Legge byte da un file descriptor in un buffer di memoria&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;write&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;td&gt;Scrive byte da un buffer di memoria verso un file descriptor&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;open&lt;/td&gt; &lt;td&gt;2&lt;/td&gt; &lt;td&gt;Apre un file su disco e restituisce un nuovo file descriptor&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;exit&lt;/td&gt; &lt;td&gt;60&lt;/td&gt; &lt;td&gt;Termina il processo restituendo un codice di stato&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;errori-comuni-da-evitare&quot;&gt;Errori comuni da evitare&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dimenticare la exit finale&lt;/strong&gt;: senza la syscall &lt;code&gt;sys_exit&lt;/code&gt;, la CPU continua a eseguire le locazioni di memoria successive alla &lt;code&gt;write&lt;/code&gt; come se fossero istruzioni valide, causando un crash del programma.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Invertire l&#39;ordine tra &lt;code&gt;mov rdx, rax&lt;/code&gt; e &lt;code&gt;mov rax, 1&lt;/code&gt;&lt;/strong&gt;: il valore restituito da &lt;code&gt;read&lt;/code&gt; in &lt;code&gt;rax&lt;/code&gt; va copiato in &lt;code&gt;rdx&lt;/code&gt; prima di sovrascrivere &lt;code&gt;rax&lt;/code&gt; con l&#39;ID della syscall &lt;code&gt;write&lt;/code&gt;, altrimenti il conteggio dei byte letti va perso.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dimenticare il byte nullo di terminazione&lt;/strong&gt;: quando un percorso file viene costruito manualmente nello stack, l&#39;assenza del terminatore a 0 fa sì che &lt;code&gt;open&lt;/code&gt; continui a leggere memoria oltre la stringa prevista, tentando di aprire un percorso corrotto o inesistente.&lt;/li&gt;
&lt;/ul&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="linux"/>
      
    
      
      
    <category term="low level programming"/>
      
    
      
      
    <category term="buffer managment"/>
      
    
  </entry>
  
  <entry>
    <title>Reverse engineering e debug binario in Linux</title>
    <link href="https://dino-996.github.io/blog/reverse-engineering-e-debug-binario-in-linux/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/reverse-engineering-e-debug-binario-in-linux/</id>
    <published>2026-09-18T00:00:00.000Z</published>
    <updated>2026-09-18T00:00:00.000Z</updated>
    
    <summary>Tutorial operativo per il reverse engineering e il debugging binario su Linux. Il testo analizza il disassemblaggio con objdump, il tracciamento delle system call con strace e l&#39;ispezione della memoria, dei registri e dello stack mediante GDB.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;analisi-statica-con-objdump&quot;&gt;Analisi statica con objdump&lt;/h2&gt;
&lt;h3 id=&quot;cose-il-disassemblaggio&quot;&gt;Cos&#39;è il disassemblaggio&lt;/h3&gt;
&lt;p&gt;Quando un software scritto in Assembly o C viene compilato, viene convertito in codice macchina, cioè sequenze di byte (opcode) interpretabili direttamente dalla CPU. Il disassemblaggio è il processo inverso, prende un file eseguibile binario e lo riconverte in istruzioni Assembly leggibili dall&#39;essere umano. È la tecnica fondamentale del reverse engineering e del debugging per analizzare programmi di cui non si possiede il codice sorgente.&lt;/p&gt;
&lt;h3 id=&quot;lo-strumento-objdump&quot;&gt;Lo strumento objdump&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;objdump&lt;/code&gt; è una delle utility a riga di comando più comuni in ambiente Linux per ispezionare i file ELF (eseguibili).&lt;/p&gt;
&lt;p&gt;Comando standard per disassemblare un binario in sintassi Intel.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;objdump -d -M intel /percorso/del/file
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Le due flag fondamentali sono:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;-d&lt;/code&gt;&lt;/strong&gt;: esegue il disassemblaggio delle sezioni contenenti codice eseguibile, come la sezione &lt;code&gt;.text&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;-M intel&lt;/code&gt;&lt;/strong&gt;: forza l&#39;output a usare la sintassi Intel (istruzione, destinazione, sorgente). Senza questa flag, &lt;code&gt;objdump&lt;/code&gt; usa la sintassi predefinita AT&amp;amp;T, che inverte l&#39;ordine degli operandi e aggiunge prefissi come &lt;code&gt;%&lt;/code&gt; e &lt;code&gt;$&lt;/code&gt;, rendendo la lettura più complessa per chi è abituato alla notazione Intel.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;anatomia-di-un-output-di-disassemblaggio&quot;&gt;Anatomia di un output di disassemblaggio&lt;/h3&gt;
&lt;p&gt;Analizzando un esempio di output.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;401000: 48 c7 c7 39 05 00 00 mov rdi,0x539
401007: 48 c7 c7 00 00 00 00 mov rdi,0
40100e: 48 c7 c0 3c 00 00 00 mov rax,0x3c
401015: 0f 05 syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Un rigo di disassemblaggio si divide in tre componenti principali.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Indirizzo di memoria&lt;/th&gt; &lt;th&gt;Opcode (byte grezzi in hex)&lt;/th&gt; &lt;th&gt;Istruzione Assembly (Intel)&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;401000:&lt;/td&gt; &lt;td&gt;48 c7 c7 39 05 00 00&lt;/td&gt; &lt;td&gt;mov rdi, 0x539&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;401007:&lt;/td&gt; &lt;td&gt;48 c7 c7 00 00 00 00&lt;/td&gt; &lt;td&gt;mov rdi, 0&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;40100e:&lt;/td&gt; &lt;td&gt;48 c7 c0 3c 00 00 00&lt;/td&gt; &lt;td&gt;mov rax, 0x3c (syscall 60, sys_exit)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;401015:&lt;/td&gt; &lt;td&gt;0f 05&lt;/td&gt; &lt;td&gt;syscall&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Due osservazioni sono importanti in fase di analisi statica:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Tutti i valori immediati sono in esadecimale&lt;/strong&gt;. Ad esempio, &lt;code&gt;0x3c&lt;/code&gt; corrisponde a &lt;code&gt;60&lt;/code&gt; in decimale, cioè l&#39;ID della syscall &lt;code&gt;sys_exit&lt;/code&gt;, mentre &lt;code&gt;0x539&lt;/code&gt; corrisponde a &lt;code&gt;1337&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Analisi statica e segreti nascosti&lt;/strong&gt;. Leggendo il disassemblaggio è possibile notare valori caricati temporaneamente nei registri, come &lt;code&gt;0x539&lt;/code&gt; in &lt;code&gt;rdi&lt;/code&gt;, anche se vengono sovrascritti subito dopo con &lt;code&gt;mov rdi, 0&lt;/code&gt; e quindi risulterebbero invisibili osservando solo l&#39;esecuzione standard del programma.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;procedura-pratica&quot;&gt;Procedura pratica&lt;/h3&gt;
&lt;p&gt;Un caso tipico di esercizio guidato consiste nel disassemblare un binario, individuare il valore esadecimale caricato in un registro prima che venga sovrascritto, e usarlo come risposta.&lt;/p&gt;
&lt;p&gt;Disassembla il binario con sintassi Intel.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;objdump -d -M intel /assembly/disassemble
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Cerca poi la funzione &lt;code&gt;_start&lt;/code&gt; e individua la riga in cui viene eseguito il primo &lt;code&gt;mov rdi, &amp;lt;valore_hex&amp;gt;&lt;/code&gt; immediatamente prima dell&#39;istruzione &lt;code&gt;mov rdi, 0&lt;/code&gt;. Il valore trovato può essere inviato sia in formato esadecimale sia convertito in decimale.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;/assembly/submit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;analisi-dinamica-con-strace&quot;&gt;Analisi dinamica con strace&lt;/h2&gt;
&lt;h3 id=&quot;cose-e-come-funziona&quot;&gt;Cos&#39;è e come funziona&lt;/h3&gt;
&lt;p&gt;Mentre &lt;code&gt;objdump&lt;/code&gt; effettua un&#39;analisi statica, &lt;code&gt;strace&lt;/code&gt; effettua un&#39;analisi dinamica: esegue il programma e intercetta in tempo reale tutte le chiamate di sistema (syscall) effettuate dal binario verso il kernel Linux, mostrando i parametri forniti e i valori restituiti.&lt;/p&gt;
&lt;p&gt;L&#39;output di &lt;code&gt;strace&lt;/code&gt; utilizza una sintassi in stile linguaggio C.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;nome_syscall(parametro_1, parametro_2, ...) = valore_ritorno
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Un esempio concreto.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;execve(&amp;quot;/tmp/program&amp;quot;, [&amp;quot;/tmp/program&amp;quot;], 0x7ffd48ae28b0 ...) = 0
exit(42) = ?
+++ exited with 42 +++
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;execve(...)&lt;/code&gt;&lt;/strong&gt;: è la syscall usata dal sistema operativo per lanciare ed eseguire un nuovo programma, e appare all&#39;inizio perché &lt;code&gt;strace&lt;/code&gt; la rileva durante la fase di avvio del processo.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;exit(42)&lt;/code&gt;&lt;/strong&gt;: il programma invoca la syscall di terminazione fornendo &lt;code&gt;42&lt;/code&gt; come argomento.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;+++ exited with 42 +++&lt;/code&gt;&lt;/strong&gt;: è una notifica di &lt;code&gt;strace&lt;/code&gt;, non parte dell&#39;output del programma, che indica il codice di uscita finale restituito alla shell.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;la-syscall-alarm&quot;&gt;La syscall alarm&lt;/h3&gt;
&lt;p&gt;La chiamata di sistema &lt;code&gt;alarm(seconds)&lt;/code&gt;, syscall numero 37 su architettura x86_64, viene utilizzata nei sistemi Linux per impostare un timer. Trascorso il numero di secondi specificato come argomento, il kernel invia al processo un segnale di terminazione &lt;code&gt;SIGALRM&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Nelle sfide di reverse engineering, &lt;code&gt;strace&lt;/code&gt; è indispensabile per leggere i parametri di syscall altrimenti &amp;quot;invisibili&amp;quot; come &lt;code&gt;alarm&lt;/code&gt; o &lt;code&gt;read&lt;/code&gt;/&lt;code&gt;write&lt;/code&gt;, senza dover analizzare l&#39;intero disassembly.&lt;/p&gt;
&lt;h2 id=&quot;gdb-gnu-debugger&quot;&gt;GDB (GNU Debugger)&lt;/h2&gt;
&lt;h3 id=&quot;analisi-statica-dinamica-e-controllo-completo&quot;&gt;Analisi statica, dinamica e controllo completo&lt;/h3&gt;
&lt;p&gt;Dopo l&#39;analisi statica con &lt;code&gt;objdump&lt;/code&gt; e il tracciamento delle syscall con &lt;code&gt;strace&lt;/code&gt;, GDB (GNU Debugger) rappresenta lo strumento di analisi dinamica e controllo completo del processo. Mentre &lt;code&gt;strace&lt;/code&gt; rileva solo i punti di contatto tra il programma e il kernel, cioè le syscall, GDB permette di entrare all&#39;interno dello spazio di indirizzamento del processo per fermare l&#39;esecuzione, ispezionare i registri della CPU, modificare la memoria e analizzare ogni singola istruzione Assembly in tempo reale.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Strumento&lt;/th&gt; &lt;th&gt;Tipo di analisi&lt;/th&gt; &lt;th&gt;Livello di visibilità&lt;/th&gt; &lt;th&gt;Uso principale&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;objdump&lt;/td&gt; &lt;td&gt;Statica (file su disco)&lt;/td&gt; &lt;td&gt;Codice macchina disassemblato&lt;/td&gt; &lt;td&gt;Analizzare il codice senza eseguirlo&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;strace&lt;/td&gt; &lt;td&gt;Dinamica (interfaccia kernel)&lt;/td&gt; &lt;td&gt;Chiamate di sistema e argomenti&lt;/td&gt; &lt;td&gt;Capire cosa chiede il processo al sistema operativo&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;gdb&lt;/td&gt; &lt;td&gt;Dinamica (spazio di memoria)&lt;/td&gt; &lt;td&gt;Registri, stack, RAM, flusso di esecuzione&lt;/td&gt; &lt;td&gt;Ispezionare e manipolare lo stato interno del processo tramite la syscall ptrace&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;avvio-e-sintassi-di-base&quot;&gt;Avvio e sintassi di base&lt;/h3&gt;
&lt;p&gt;L&#39;avvio di GDB si effettua passando il percorso del file eseguibile come argomento.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /percorso/del/binario
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Una volta avviato, GDB apre la propria shell interattiva, riconoscibile dal prompt.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I due comandi fondamentali per la navigazione di base sono:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;quit&lt;/code&gt;&lt;/strong&gt; (o &lt;code&gt;q&lt;/code&gt;): esce da GDB e torna alla shell del sistema operativo.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;run&lt;/code&gt;&lt;/strong&gt; (o &lt;code&gt;r&lt;/code&gt;): avvia l&#39;esecuzione del programma caricato all&#39;interno del debugger.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;rilevamento-del-debugger-tramite-ptrace&quot;&gt;Rilevamento del debugger tramite ptrace&lt;/h3&gt;
&lt;p&gt;Quando si lancia &lt;code&gt;gdb&lt;/code&gt; su un eseguibile, il sistema operativo abilita il tracciamento tramite la syscall &lt;code&gt;ptrace&lt;/code&gt;. Il programma può quindi verificare se è controllato da un debugger e, in alcuni casi, stampare a schermo un valore proprio in reazione a quella condizione, prima di arrestarsi o terminare.&lt;/p&gt;
&lt;h2 id=&quot;controllo-del-processo&quot;&gt;Controllo del processo&lt;/h2&gt;
&lt;h3 id=&quot;avvio-personalizzato-con-starti&quot;&gt;Avvio personalizzato con starti&lt;/h3&gt;
&lt;p&gt;Diversamente dal comando &lt;code&gt;run&lt;/code&gt;, che esegue il programma fino alla fine o al primo breakpoint, &lt;code&gt;starti&lt;/code&gt; inserisce un punto di arresto temporaneo sull&#39;istruzione iniziale assoluta del binario e ne interrompe immediatamente l&#39;esecuzione. Questo permette di analizzare lo stato del programma prima di qualsiasi modifica al contesto.&lt;/p&gt;
&lt;h3 id=&quot;ispezionare-il-codice-in-esecuzione-con-disassemble&quot;&gt;Ispezionare il codice in esecuzione con disassemble&lt;/h3&gt;
&lt;p&gt;Digitando &lt;code&gt;disassemble&lt;/code&gt; all&#39;interno della shell di GDB, viene mostrato il codice Assembly della funzione corrente.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) disassemble
Dump of assembler code for function _start:
=&amp;gt; 0x0000000000401000 &amp;lt;+0&amp;gt;: mov rdi,0x539
0x0000000000401007 &amp;lt;+7&amp;gt;: mov rdi,0x0
0x000000000040100e &amp;lt;+14&amp;gt;: mov rax,0x3c
0x0000000000401015 &amp;lt;+21&amp;gt;: syscall
End of assembler dump.

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Alcuni elementi chiave dell&#39;output:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Il simbolo &lt;code&gt;=&amp;gt;&lt;/code&gt; (instruction pointer)&lt;/strong&gt;: indica l&#39;esatta istruzione che la CPU sta per eseguire in quel momento, puntata dal registro &lt;code&gt;RIP&lt;/code&gt; su architettura x86_64.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Indirizzi e offset&lt;/strong&gt;: a sinistra è presente l&#39;indirizzo di memoria virtuale, ad esempio &lt;code&gt;0x0000000000401000&lt;/code&gt;, seguito dall&#39;offset relativo all&#39;inizio della funzione, ad esempio &lt;code&gt;&amp;lt;+0&amp;gt;&lt;/code&gt; o &lt;code&gt;&amp;lt;+7&amp;gt;&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Valori immediati esadecimali&lt;/strong&gt;: i valori caricati nei registri o passati alle istruzioni appaiono in notazione esadecimale, ad esempio &lt;code&gt;0x539&lt;/code&gt; corrisponde al valore decimale &lt;code&gt;1337&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;codice-censurato&quot;&gt;Codice censurato&lt;/h2&gt;
&lt;h3 id=&quot;il-limite-dellanalisi-statica&quot;&gt;Il limite dell&#39;analisi statica&lt;/h3&gt;
&lt;p&gt;Nella sezione precedente bastava disassemblare il codice per leggere visivamente il valore caricato in un registro. L&#39;analisi statica ha però dei limiti evidenti in presenza di codice offuscato o censurato: se l&#39;output del disassemblatore mostra un valore nascosto, ad esempio &lt;code&gt;mov rdi, CENSORED&lt;/code&gt;, non è più possibile scoprire il segreto semplicemente leggendo il testo disassemblato.&lt;/p&gt;
&lt;p&gt;La CPU, tuttavia, deve per forza ricevere ed eseguire i byte binari reali. Anche se la stringa viene censurata a schermo nel disassemblatore, quando la CPU esegue l&#39;istruzione &lt;code&gt;mov rdi, &amp;lt;segreto&amp;gt;&lt;/code&gt; il registro &lt;code&gt;rdi&lt;/code&gt; viene effettivamente popolato con il valore reale, osservabile a runtime.&lt;/p&gt;
&lt;h3 id=&quot;il-comando-stepi&quot;&gt;Il comando stepi&lt;/h3&gt;
&lt;p&gt;Per avanzare l&#39;esecuzione di una singola istruzione Assembly alla volta si utilizza il comando &lt;code&gt;stepi&lt;/code&gt;, abbreviabile in &lt;code&gt;si&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) stepi
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;È utile distinguere due comandi di avanzamento simili:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;stepi&lt;/code&gt; / &lt;code&gt;si&lt;/code&gt;&lt;/strong&gt;: avanza di una singola istruzione macchina (Assembly).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;step&lt;/code&gt; / &lt;code&gt;s&lt;/code&gt;&lt;/strong&gt;: avanza di una riga di codice sorgente ad alto livello, ad esempio C o C++, che potrebbe corrispondere a decine di istruzioni assembly. In assenza di sorgenti disponibili, si comporta come &lt;code&gt;stepi&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;flusso-operativo&quot;&gt;Flusso operativo&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) starti
(gdb) stepi
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Eseguendo &lt;code&gt;stepi&lt;/code&gt;, l&#39;istruzione &lt;code&gt;mov rdi, CENSORED&lt;/code&gt; viene processata dalla CPU e il valore reale entra nel registro &lt;code&gt;rdi&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;ispezione-manuale-dei-registri&quot;&gt;Ispezione manuale dei registri&lt;/h2&gt;
&lt;h3 id=&quot;dalla-lettura-assistita-alla-lettura-autonoma&quot;&gt;Dalla lettura assistita alla lettura autonoma&lt;/h3&gt;
&lt;p&gt;Fino a questo punto è stato sufficiente osservare il valore mostrato automaticamente da GDB dopo uno &lt;code&gt;stepi&lt;/code&gt;. Il passo successivo è interrogare direttamente i registri della CPU tramite la shell del debugger, una volta che il valore reale è entrato nell&#39;architettura hardware.&lt;/p&gt;
&lt;h3 id=&quot;il-comando-print-e-il-prefisso-dollar&quot;&gt;Il comando print e il prefisso $&lt;/h3&gt;
&lt;p&gt;Per stampare a schermo il contenuto di un registro, o il valore di un&#39;espressione, si utilizza il comando &lt;code&gt;print&lt;/code&gt;, abbreviabile con &lt;code&gt;p&lt;/code&gt;. I nomi dei registri CPU all&#39;interno di GDB devono sempre essere preceduti dal simbolo &lt;code&gt;$&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) print $rdi
$1 = 1337
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La struttura dell&#39;output è la seguente:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;$1&lt;/code&gt;&lt;/strong&gt;: rappresenta una variabile temporanea creata da GDB per memorizzare la cronologia dei valori stampati, riutilizzabile nei comandi successivi.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;1337&lt;/code&gt;&lt;/strong&gt;: è il valore effettivo memorizzato nel registro &lt;code&gt;rdi&lt;/code&gt;, mostrato di default in notazione decimale.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Flusso operativo tipico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) starti
(gdb) stepi
(gdb) print $rdi
(gdb) quit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;modifica-dei-registri&quot;&gt;Modifica dei registri&lt;/h2&gt;
&lt;h3 id=&quot;manipolazione-dinamica-dello-stato-della-cpu&quot;&gt;Manipolazione dinamica dello stato della CPU&lt;/h3&gt;
&lt;p&gt;Osservare lo stato del processo con &lt;code&gt;disassemble&lt;/code&gt; e &lt;code&gt;print&lt;/code&gt; è un uso passivo di GDB. La vera potenza di un debugger risiede nella capacità di intervenire e modificare l&#39;esecuzione al volo, alterando direttamente i dati contenuti nei registri della CPU prima che le istruzioni successive li utilizzino. Questo permette, ad esempio, di bypassare controlli, forzare un determinato ramo di esecuzione o iniettare valori arbitrari per testare il comportamento del software.&lt;/p&gt;
&lt;h3 id=&quot;il-comando-set&quot;&gt;Il comando set&lt;/h3&gt;
&lt;p&gt;Per assegnare un nuovo valore a un registro durante una pausa dell&#39;esecuzione si utilizza il comando &lt;code&gt;set&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) set $nome_registro = valore
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Alcuni esempi di utilizzo:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;set $rdi = 1337&lt;/code&gt;&lt;/strong&gt;: sovrascrive il registro &lt;code&gt;rdi&lt;/code&gt; con il valore decimale &lt;code&gt;1337&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;set $rax = 0x3c&lt;/code&gt;&lt;/strong&gt;: imposta il registro &lt;code&gt;rax&lt;/code&gt; al valore esadecimale &lt;code&gt;0x3c&lt;/code&gt;, cioè &lt;code&gt;60&lt;/code&gt; in decimale.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Come per &lt;code&gt;print&lt;/code&gt;, anche con &lt;code&gt;set&lt;/code&gt; il nome del registro deve sempre essere preceduto dal prefisso &lt;code&gt;$&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Flusso operativo tipico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) starti
(gdb) stepi
(gdb) set $rdi = 1337
(gdb) stepi
(gdb) quit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;debug-cooperativo&quot;&gt;Debug cooperativo&lt;/h2&gt;
&lt;h3 id=&quot;debugging-preventivo-contro-debugging-cooperativo&quot;&gt;Debugging preventivo contro debugging cooperativo&lt;/h3&gt;
&lt;p&gt;Nelle sezioni precedenti è stato utilizzato il debugging preventivo: GDB prende il controllo del programma all&#39;avvio, tramite &lt;code&gt;starti&lt;/code&gt; o breakpoint esterni, e ne arresta l&#39;esecuzione senza che il codice ne sia consapevole.&lt;/p&gt;
&lt;p&gt;Con il debugging cooperativo, è il programma stesso a decidere dove e quando cedere il controllo al debugger, attraverso un&#39;istruzione Assembly dedicata.&lt;/p&gt;
&lt;p&gt;Il flusso logico è il seguente: durante l&#39;esecuzione standard, quando viene raggiunta l&#39;istruzione &lt;code&gt;int3&lt;/code&gt;, viene generato un trap con segnale &lt;code&gt;SIGTRAP&lt;/code&gt;. Se il processo gira sotto GDB tramite &lt;code&gt;run&lt;/code&gt;, l&#39;esecuzione si interrompe e il controllo torna al prompt &lt;code&gt;(gdb)&lt;/code&gt;. Se invece il processo gira fuori da GDB, direttamente in shell, il sistema operativo lo termina restituendo un errore di tipo &amp;quot;Trace/breakpoint trap&amp;quot;.&lt;/p&gt;
&lt;h3 id=&quot;listruzione-int3&quot;&gt;L&#39;istruzione int3&lt;/h3&gt;
&lt;p&gt;Sull&#39;architettura x86_64, l&#39;istruzione &lt;code&gt;int3&lt;/code&gt;, il cui opcode esadecimale è &lt;code&gt;0xCC&lt;/code&gt;, genera un&#39;interruzione di tipo breakpoint trap, cioè un segnale &lt;code&gt;SIGTRAP&lt;/code&gt; inviato al kernel.&lt;/p&gt;
&lt;p&gt;Le differenze principali nell&#39;uso di GDB sono:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;starti&lt;/code&gt;&lt;/strong&gt;: blocca l&#39;esecuzione prima ancora di eseguire la prima istruzione.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;run&lt;/code&gt; (&lt;code&gt;r&lt;/code&gt;)&lt;/strong&gt;: lascia girare il programma a piena velocità finché non incontra un&#39;istruzione &lt;code&gt;int3&lt;/code&gt;, un breakpoint impostato manualmente dall&#39;utente, oppure finché il processo non termina.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;esempio-di-codice-con-int3&quot;&gt;Esempio di codice con int3&lt;/h3&gt;
&lt;p&gt;Un piccolo programma in sintassi Intel che imposta un valore in un registro e invochi immediatamente &lt;code&gt;int3&lt;/code&gt;, cedendo il controllo al debugger.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
	mov rdi, 1337
	int3
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;passare-argomenti-da-riga-di-comando-in-gdb&quot;&gt;Passare argomenti da riga di comando in GDB&lt;/h2&gt;
&lt;h3 id=&quot;il-concetto-gli-argomenti-dellinferiore&quot;&gt;Il concetto: gli argomenti dell&#39;inferiore&lt;/h3&gt;
&lt;p&gt;Un programma legge gli argomenti della riga di comando tramite l&#39;array &lt;code&gt;argv&lt;/code&gt; (&lt;code&gt;argv[1]&lt;/code&gt;, &lt;code&gt;argv[2]&lt;/code&gt;, e così via). Quando si esegue un programma fuori da GDB, gli argomenti vengono specificati direttamente dopo il nome dell&#39;eseguibile nella shell.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;dino@localhost:~$ /assembly/program hello
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;All&#39;interno dell&#39;ambiente interattivo di GDB, per passare argomenti al programma in esecuzione, detto anche &amp;quot;inferiore&amp;quot;, è sufficiente scriverli direttamente dopo il comando &lt;code&gt;run&lt;/code&gt; (o l&#39;abbreviazione &lt;code&gt;r&lt;/code&gt;). Qualsiasi stringa o valore digitato dopo &lt;code&gt;run&lt;/code&gt; viene inoltrato al programma esattamente come se fosse stato lanciato da riga di comando bash.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) run hello
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;due-modalita-di-configurazione&quot;&gt;Due modalità di configurazione&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Passaggio diretto con run&lt;/strong&gt;, per passare gli argomenti al momento dell&#39;avvio.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) run hello
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Impostazione preventiva con set args&lt;/strong&gt;, utile quando serve usare &lt;code&gt;starti&lt;/code&gt; o si preferisce impostare gli argomenti prima di lanciare il programma.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) set args hello
(gdb) starti
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;leggere-lo-stato-di-runtime-dallo-stack&quot;&gt;Leggere lo stato di runtime dallo stack&lt;/h2&gt;
&lt;h3 id=&quot;dati-dinamici-nello-stack&quot;&gt;Dati dinamici nello stack&lt;/h3&gt;
&lt;p&gt;Abbiamo visto finora che il valore era cablato direttamente nell&#39;istruzione, ad esempio &lt;code&gt;mov rdi, 0x1337&lt;/code&gt;. In uno scenario diverso, il valore non si trova nel codice binario su disco, ma deriva dallo stato di runtime:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;argc&lt;/code&gt; (argument count)&lt;/strong&gt;: rappresenta il numero di argomenti passati al programma quando viene avviato dalla shell.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Inizializzazione dello stack&lt;/strong&gt;: all&#39;avvio di un eseguibile Linux x86_64, il sistema operativo deposita in cima allo stack il valore di &lt;code&gt;argc&lt;/code&gt;, seguito dagli argomenti &lt;code&gt;argv&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Istruzione &lt;code&gt;pop rdi&lt;/code&gt;&lt;/strong&gt;: preleva il valore in cima allo stack, cioè &lt;code&gt;argc&lt;/code&gt;, e lo carica nel registro &lt;code&gt;rdi&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Poiché il valore di &lt;code&gt;argc&lt;/code&gt; dipende da come il programma viene lanciato, nessun disassemblatore statico può rivelare il segreto, è indispensabile eseguirlo sotto debugger.&lt;/p&gt;
&lt;p&gt;Il programma tipico di questo scenario è progettato per distruggere il segreto un istante dopo averlo letto.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;pop rdi
mov rdi, 0x0
mov rax, 0x3c
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La finestra temporale utile per catturare il valore è quella tra la prima e la seconda istruzione: prima di &lt;code&gt;pop rdi&lt;/code&gt; il valore si trova ancora nello stack, subito dopo &lt;code&gt;pop rdi&lt;/code&gt; si trova temporaneamente nel registro &lt;code&gt;rdi&lt;/code&gt;, mentre dopo &lt;code&gt;mov rdi, 0x0&lt;/code&gt; è perso per sempre.&lt;/p&gt;
&lt;p&gt;Flusso operativo tipico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) starti
(gdb) stepi
(gdb) print $rdi
(gdb) quit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;esaminare-la-memoria-con-il-comando-x&quot;&gt;Esaminare la memoria con il comando x&lt;/h2&gt;
&lt;h3 id=&quot;leggere-la-ram-senza-passare-dai-registri&quot;&gt;Leggere la RAM senza passare dai registri&lt;/h3&gt;
&lt;p&gt;In alcuni scenari il programma non legge mai il dato in un registro.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rdi, 0x0
mov rax, 0x3c
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Tuttavia, all&#39;avvio del programma il kernel Linux deposita comunque &lt;code&gt;argc&lt;/code&gt; in cima allo stack. Poiché il registro &lt;code&gt;$rsp&lt;/code&gt; (stack pointer) punta esattamente a quell&#39;indirizzo di memoria, è possibile usare GDB per esaminare la RAM direttamente, ignorando il fatto che il programma non usi quel dato.&lt;/p&gt;
&lt;h3 id=&quot;il-comando-x-examine&quot;&gt;Il comando x (examine)&lt;/h3&gt;
&lt;p&gt;Il comando &lt;code&gt;x&lt;/code&gt; di GDB permette di leggere il contenuto di una determinata area di memoria a partire da un indirizzo o da un registro puntatore.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) x &amp;lt;indirizzo_o_registro&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per impostazione predefinita &lt;code&gt;x&lt;/code&gt; mostra i dati in esadecimale. È possibile aggiungere una lettera di formato subito dopo la barra.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Formato di visualizzazione&lt;/th&gt; &lt;th&gt;Esempio output&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/d $rsp&lt;/td&gt; &lt;td&gt;Decimale (d = decimal)&lt;/td&gt; &lt;td&gt;0x7fffffffd540: 1&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/x $rsp&lt;/td&gt; &lt;td&gt;Esadecimale (x = hex)&lt;/td&gt; &lt;td&gt;0x7fffffffd540: 0x00000001&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;L&#39;indirizzo a sinistra, ad esempio &lt;code&gt;0x7fffffffd540:&lt;/code&gt;, indica dove si trova il dato nella RAM, mentre il valore a destra è il dato contenuto in quel punto, in questo caso &lt;code&gt;argc&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Flusso operativo tipico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) starti
(gdb) x/d $rsp
(gdb) quit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;estrarre-puntatori-e-stringhe-dallo-stack&quot;&gt;Estrarre puntatori e stringhe dallo stack&lt;/h2&gt;
&lt;h3 id=&quot;anatomia-dello-stack-allavvio-del-processo&quot;&gt;Anatomia dello stack all&#39;avvio del processo&lt;/h3&gt;
&lt;p&gt;Quando un programma viene lanciato su Linux x86_64, il sistema operativo organizza la parte superiore dello stack, a partire da &lt;code&gt;$rsp&lt;/code&gt;, secondo uno schema preciso che contiene le informazioni di avvio.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Posizione&lt;/th&gt; &lt;th&gt;Contenuto&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;$rsp + 0&lt;/td&gt; &lt;td&gt;argc, il numero di argomenti&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;$rsp + 8&lt;/td&gt; &lt;td&gt;argv[0], puntatore al nome del programma&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;$rsp + 16&lt;/td&gt; &lt;td&gt;argv[1], puntatore al primo argomento&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Lo stack a &lt;code&gt;$rsp + 16&lt;/code&gt; non contiene direttamente il testo cercato, ma un indirizzo di memoria, cioè un &lt;strong&gt;puntatore&lt;/strong&gt;, che indica il punto preciso della RAM in cui si trova memorizzata la stringa corrispondente.&lt;/p&gt;
&lt;h3 id=&quot;nuovi-modificatori-del-comando-x&quot;&gt;Nuovi modificatori del comando x&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Modificatore&lt;/th&gt; &lt;th&gt;Descrizione operativa&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/a $rsp+16&lt;/td&gt; &lt;td&gt;/a (address)&lt;/td&gt; &lt;td&gt;Legge il valore memorizzato a $rsp + 16 e lo mostra come indirizzo di memoria, cioè come puntatore&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/s 0x...&lt;/td&gt; &lt;td&gt;/s (string)&lt;/td&gt; &lt;td&gt;Va all&#39;indirizzo ottenuto e legge i byte come una stringa di testo terminata da null&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Leggere il contenuto puntato da &lt;code&gt;argv[1]&lt;/code&gt; richiede quindi due passaggi: prima si ottiene l&#39;indirizzo con &lt;code&gt;x/a&lt;/code&gt;, poi si legge la stringa a quell&#39;indirizzo con &lt;code&gt;x/s&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) x/a $rsp+16
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il valore restituito è l&#39;indirizzo del puntatore, ad esempio &lt;code&gt;0x7ffc001c4750&lt;/code&gt;, da usare come argomento del passo successivo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) x/s 0x7ffc001c4750
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;reindirizzare-lo-standard-input-in-gdb&quot;&gt;Reindirizzare lo standard input in GDB&lt;/h2&gt;
&lt;h3 id=&quot;input-da-file-e-redirezione-stdin&quot;&gt;Input da file e redirezione stdin&lt;/h3&gt;
&lt;p&gt;I programmi in ambiente Linux possono ricevere dati in due modi principali: come argomenti da riga di comando (&lt;code&gt;argv&lt;/code&gt;), oppure come standard input, il flusso letto dal programma durante l&#39;esecuzione tramite il file descriptor &lt;code&gt;0&lt;/code&gt;, di default dalla tastiera ma reindirizzabile da file o pipeline.&lt;/p&gt;
&lt;p&gt;All&#39;interno di GDB, l&#39;eseguibile sottoposto a tracciamento viene chiamato &amp;quot;inferior&amp;quot;. GDB permette di reindirizzare lo stdin dell&#39;inferiore applicando l&#39;operatore di redirezione &lt;code&gt;&amp;lt;&lt;/code&gt; direttamente al comando &lt;code&gt;run&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) run &amp;lt; /percorso/del/file
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Quando si esegue questo comando, GDB avvia il programma dal suo punto di inizio, e tutto ciò che il programma tenta di leggere da stdin, ad esempio tramite funzioni come &lt;code&gt;read&lt;/code&gt;, &lt;code&gt;scanf&lt;/code&gt; o &lt;code&gt;fgets&lt;/code&gt;, viene letto automaticamente dal file indicato anziché attendere un input manuale da tastiera.&lt;/p&gt;
&lt;p&gt;Flusso operativo tipico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb /assembly/program
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) run &amp;lt; /challenge/secret
(gdb) quit
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;tabella-riassuntiva-di-tutti-i-comandi-gdb&quot;&gt;Tabella riassuntiva di tutti i comandi GDB&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Abbreviazione&lt;/th&gt; &lt;th&gt;Descrizione operativa&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;gdb &lt;file&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Avvia GDB caricando il file eseguibile specificato&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;starti&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Avvia il processo e lo blocca alla primissima istruzione in memoria (entry point)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;run &lt;arg1&gt; &lt;arg2&gt;&lt;/td&gt; &lt;td&gt;r &lt;arg1&gt; &lt;arg2&gt;&lt;/td&gt; &lt;td&gt;Avvia l&#39;esecuzione passando eventuali argomenti, e lascia girare il programma fino al termine o al primo breakpoint&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;set args &lt;arg1&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Imposta gli argomenti predefiniti per tutte le esecuzioni successive di run o starti&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;show args&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Mostra gli argomenti attualmente configurati in GDB&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;run &lt; file&lt;/td&gt; &lt;td&gt;r &lt; file&lt;/td&gt; &lt;td&gt;Avvia il programma reindirizzando il contenuto del file nello stdin&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;disassemble&lt;/td&gt; &lt;td&gt;disas&lt;/td&gt; &lt;td&gt;Disassembla il blocco di codice o funzione attualmente in esecuzione&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;stepi&lt;/td&gt; &lt;td&gt;si&lt;/td&gt; &lt;td&gt;Esegue esattamente l&#39;istruzione corrente e si ferma subito a quella successiva&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;print $reg&lt;/td&gt; &lt;td&gt;p $reg&lt;/td&gt; &lt;td&gt;Stampa il valore contenuto nel registro specificato&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;set $reg = val&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Modifica il valore del registro specificato con il nuovo valore assegnato&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/d &lt;addr&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Esamina la memoria all&#39;indirizzo indicato mostrando il valore in formato decimale&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/x &lt;addr&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Esamina la memoria all&#39;indirizzo indicato mostrando il valore in formato esadecimale&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/a &lt;addr&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Esamina la memoria interpretando il valore letto come un indirizzo o puntatore&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;x/s &lt;addr&gt;&lt;/td&gt; &lt;td&gt;nessuna&lt;/td&gt; &lt;td&gt;Esamina la memoria all&#39;indirizzo indicato interpretandola come stringa C terminata da null&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;quit&lt;/td&gt; &lt;td&gt;q&lt;/td&gt; &lt;td&gt;Termina la sessione del debugger e ritorna alla shell&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;buone-pratiche-e-strumenti-complementari&quot;&gt;Buone pratiche e strumenti complementari&lt;/h2&gt;
&lt;p&gt;Oltre ai comandi illustrati finora, chi lavora regolarmente con reverse engineering e debug binario su Linux trova utili alcuni strumenti e comandi aggiuntivi.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Estensioni GDB per il reverse engineering&lt;/strong&gt;: framework come &lt;code&gt;pwndbg&lt;/code&gt; e &lt;code&gt;GEF&lt;/code&gt; arricchiscono l&#39;interfaccia standard di GDB con visualizzazioni avanzate di memoria, stack e registri, pensate specificamente per attività di exploit development e CTF. Sono un naturale passo successivo una volta acquisita familiarità con i comandi base illustrati in questo articolo, incluso l&#39;uso combinato di &lt;code&gt;print&lt;/code&gt;, &lt;code&gt;set&lt;/code&gt; e &lt;code&gt;x&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Ispezionare lo stato completo dei registri&lt;/strong&gt;: quando il valore cercato non è nella posizione attesa, può essere utile visualizzare l&#39;intero set di registri della CPU in un colpo solo, invece di interrogarli uno alla volta con &lt;code&gt;print&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) info registers
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Disassemblare solo la sezione di interesse&lt;/strong&gt;: quando un binario è grande, può essere utile limitare l&#39;output di &lt;code&gt;objdump&lt;/code&gt; a una singola sezione ELF invece di stampare l&#39;intero file.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;objdump -d -M intel -j .text /percorso/del/file
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Breakpoint manuali come alternativa a int3&lt;/strong&gt;: oltre a modificare il codice sorgente per inserire &lt;code&gt;int3&lt;/code&gt;, GDB permette di impostare un breakpoint direttamente su un indirizzo di memoria, senza dover ricompilare nulla.&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;(gdb) break *0x401000
&lt;/code&gt;&lt;/pre&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="debugging"/>
      
    
      
      
    <category term="reverse engineering"/>
      
    
      
      
    <category term="GDB"/>
      
    
      
      
    <category term="linux"/>
      
    
  </entry>
  
  <entry>
    <title>Rappresentazione dei dati e aritmetica binaria</title>
    <link href="https://dino-996.github.io/blog/rappresentazione-dei-dati-e-aritmetica-binaria/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/rappresentazione-dei-dati-e-aritmetica-binaria/</id>
    <published>2026-09-16T00:00:00.000Z</published>
    <updated>2026-09-16T00:00:00.000Z</updated>
    
    <summary>Un&#39;analisi approfondita della rappresentazione dello stack e della gestione dei dati in memoria. L&#39;articolo esplora la linearizzazione degli offset tramite il registro rsp, il funzionamento dei sistemi binario ed esadecimale e la logica del complemento a due per i numeri con segno a 8, 16 e 32 bit.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;rappresentazione-lineare-dello-stack-e-offset-relativi&quot;&gt;Rappresentazione lineare dello stack e offset relativi&lt;/h2&gt;
&lt;p&gt;Pensare agli indirizzi di memoria come a una linea numerica orizzontale semplifica il ragionamento sullo stack rispetto ai classici diagrammi verticali.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;Indirizzi minori &amp;lt; ------------------------------------------------ &amp;gt; Indirizzi maggiori
... --- [rsp - 0x10] --- [rsp - 0x08] --- [rsp] --- [rsp + 0x08] --- [rsp + 0x10] --- ...

&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Sinistra&lt;/strong&gt;: indirizzi numerici più piccoli, ad esempio &lt;code&gt;rsp - 8&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Centro&lt;/strong&gt;: il punto attuale indicato dallo stack pointer (&lt;code&gt;rsp&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Destra&lt;/strong&gt;: indirizzi numerici più grandi, ad esempio &lt;code&gt;rsp + 8&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;direzionalita-degli-offset-rsp&quot;&gt;Direzionalità degli offset (rsp)&lt;/h3&gt;
&lt;p&gt;La direzione dello spostamento in memoria dipende dal segno dell&#39;offset applicato al registro &lt;code&gt;rsp&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Sintassi&lt;/th&gt; &lt;th&gt;Spostamento sulla linea&lt;/th&gt; &lt;th&gt;Indirizzo di destinazione&lt;/th&gt; &lt;th&gt;Descrizione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;[rsp + N]&lt;/td&gt; &lt;td&gt;Verso destra&lt;/td&gt; &lt;td&gt;Indirizzi maggiori&lt;/td&gt; &lt;td&gt;Legge dati posizionati dopo lo stack pointer&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;[rsp - N]&lt;/td&gt; &lt;td&gt;Verso sinistra&lt;/td&gt; &lt;td&gt;Indirizzi minori&lt;/td&gt; &lt;td&gt;Legge dati posizionati prima dello stack pointer&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;disposizione-dei-dati-di-avvio-del-kernel&quot;&gt;Disposizione dei dati di avvio del kernel&lt;/h3&gt;
&lt;p&gt;All&#39;avvio del programma, il kernel Linux posiziona le informazioni di bootstrap, come &lt;code&gt;argc&lt;/code&gt; e i puntatori &lt;code&gt;argv&lt;/code&gt;, a partire dall&#39;indirizzo puntato da &lt;code&gt;rsp&lt;/code&gt;. Tutti questi dati iniziali si estendono verso destra sulla linea numerica, occupando indirizzi progressivamente più grandi (&lt;code&gt;[rsp + 0x08]&lt;/code&gt;, &lt;code&gt;[rsp + 0x10]&lt;/code&gt;, e così via).&lt;/p&gt;
&lt;h3 id=&quot;dalla-teoria-alla-pratica-lettura-di-argc&quot;&gt;Dalla teoria alla pratica: lettura di argc&lt;/h3&gt;
&lt;p&gt;Applicando il modello della linea numerica, il primo valore utile che si trova esattamente in &lt;code&gt;[rsp]&lt;/code&gt;, senza alcun offset, è tipicamente il conteggio degli argomenti (&lt;code&gt;argc&lt;/code&gt;) passati al programma.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rax, [rsp]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Da questo punto in poi, muovendosi verso destra con offset multipli di 8 byte, la dimensione di un puntatore a 64 bit, si trovano in sequenza i puntatori &lt;code&gt;argv[0]&lt;/code&gt;, &lt;code&gt;argv[1]&lt;/code&gt; e così via, fino al terminatore NULL che segna la fine dell&#39;array degli argomenti.&lt;/p&gt;
&lt;h2 id=&quot;rappresentazione-dei-dati-binario-decimale-ed-esadecimale&quot;&gt;Rappresentazione dei dati: binario, decimale ed esadecimale&lt;/h2&gt;
&lt;h3 id=&quot;bit-e-byte&quot;&gt;Bit e byte&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Bit&lt;/strong&gt;: la singola cifra binaria (0 o 1), l&#39;unità fondamentale del computer. N bit possono rappresentare 2 elevato a N combinazioni possibili (1 bit = 2 valori, 4 bit = 16 valori, 8 bit = 256 valori).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Byte&lt;/strong&gt;: un gruppo di 8 bit, uno standard convenzionale scelto storicamente, derivato da potenze di 2.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;il-problema-del-sistema-decimale-base-10&quot;&gt;Il problema del sistema decimale (base 10)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Mancanza di confini puliti&lt;/strong&gt;: il sistema decimale non si allinea bene con i bit, perché servono circa 3,32 bit per rappresentare ogni singola cifra decimale (logaritmo in base 2 di 10).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Difficoltà di conversione&lt;/strong&gt;: calcolare a colpo d&#39;occhio l&#39;equivalente binario di un numero decimale è complesso, perché non esiste un rapporto 1 a 1 tra cifre decimali e blocchi di bit.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;la-soluzione-sistema-esadecimale-base-16&quot;&gt;La soluzione: sistema esadecimale (base 16)&lt;/h3&gt;
&lt;p&gt;L&#39;esadecimale si allinea perfettamente con la memoria binaria.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Simboli&lt;/strong&gt;: utilizza 16 cifre, da 0 a 9 e da a a f.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Regola dei 4 bit&lt;/strong&gt;: 1 cifra esadecimale equivale a esattamente 4 bit.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;1 byte = 2 cifre hex&lt;/strong&gt;: 8 bit si rappresentano in modo compatto con sole 2 cifre esadecimali, ad esempio &lt;code&gt;0x3e&lt;/code&gt; equivale a &lt;code&gt;0011 1110&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Semplicità&lt;/strong&gt;: per convertire a mente basta ricordare le potenze di 2 associate a 4 bit, cioè 8, 4, 2, 1.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;notazione-delle-costanti-numeriche&quot;&gt;Notazione delle costanti numeriche&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Sistema numerico&lt;/th&gt; &lt;th&gt;Prefisso&lt;/th&gt; &lt;th&gt;Esempio&lt;/th&gt; &lt;th&gt;Valore decimale equivalente&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Decimale&lt;/td&gt; &lt;td&gt;Nessuno&lt;/td&gt; &lt;td&gt;11&lt;/td&gt; &lt;td&gt;11&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Binario&lt;/td&gt; &lt;td&gt;0b&lt;/td&gt; &lt;td&gt;0b1011&lt;/td&gt; &lt;td&gt;11&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Esadecimale&lt;/td&gt; &lt;td&gt;0x&lt;/td&gt; &lt;td&gt;0xb&lt;/td&gt; &lt;td&gt;11&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt; &lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Esempio pratico: il numero decimale 17 si scrive 0b10001 in binario e 0x11 in esadecimale. &lt;/div&gt;
&lt;h3 id=&quot;le-potenze-di-2-da-ricordare-a-memoria&quot;&gt;Le potenze di 2 da ricordare a memoria&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Posizione del bit&lt;/th&gt; &lt;th&gt;Peso (potenza di 2)&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;1&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;2&lt;/td&gt; &lt;td&gt;2&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;3&lt;/td&gt; &lt;td&gt;4&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;4&lt;/td&gt; &lt;td&gt;8&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;numeri-negativi-e-complemento-a-due&quot;&gt;Numeri negativi e complemento a due&lt;/h2&gt;
&lt;h3 id=&quot;perche-si-usa-il-complemento-a-due&quot;&gt;Perché si usa il complemento a due&lt;/h3&gt;
&lt;p&gt;Nelle CPU, l&#39;ALU, l&#39;unità logica aritmetica, ha bisogno di un sistema efficiente per gestire i numeri negativi. Il complemento a due è lo standard universale perché risolve due grandi difetti del bit di segno tradizionale.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Elimina il doppio zero&lt;/strong&gt;: non esistono più +0 e -0, ma un solo zero (&lt;code&gt;00000000&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Circuito aritmetico unico&lt;/strong&gt;: la CPU esegue le stesse identiche operazioni di addizione e sottrazione sia per i numeri signed sia per quelli unsigned.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;la-regola-doro-griglia-dei-pesi-valore-unsigned&quot;&gt;La regola d&#39;oro: griglia dei pesi (valore unsigned)&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;128&lt;/th&gt; &lt;th&gt;64&lt;/th&gt; &lt;th&gt;32&lt;/th&gt; &lt;th&gt;16&lt;/th&gt; &lt;th&gt;8&lt;/th&gt; &lt;th&gt;4&lt;/th&gt; &lt;th&gt;2&lt;/th&gt; &lt;th&gt;1&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;0&lt;/td&gt; &lt;td&gt;0&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;td&gt;0&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;td&gt;1&lt;/td&gt; &lt;td&gt;0&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt; &lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Calcolo del valore unsigned: prendi i pesi in corrispondenza del valore 1 e sommali. Nell&#39;esempio 00110110, 32 + 16 + 4 + 2 = 54. &lt;/div&gt;
&lt;h3 id=&quot;la-regola-del-bit-di-segno-signed&quot;&gt;La regola del bit di segno (signed)&lt;/h3&gt;
&lt;p&gt;Nel sistema signed, in complemento a due, il primo bit a sinistra, quello con peso 128, funge da interruttore per determinare il segno del numero.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Caso A, il primo bit è 0 (numeri positivi)&lt;/strong&gt;: il valore signed è identico al valore unsigned. Esempio (&lt;code&gt;00110110&lt;/code&gt;): 32 + 16 + 4 + 2, cioè +54.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Caso B, il primo bit è 1 (numeri negativi)&lt;/strong&gt;: valore signed = somma unsigned - 256, cioè meno 2 elevato a 8. Esempio (&lt;code&gt;11100100&lt;/code&gt;): somma dei pesi 228, quindi 228 - 256 = -28.&lt;/p&gt;
&lt;h3 id=&quot;casi-limite-eccezioni-estreme&quot;&gt;Casi limite (eccezioni estreme)&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;10000000&lt;/code&gt; (il minimo assoluto)&lt;/strong&gt;: somma unsigned 128, quindi 128 - 256 = -128, il numero negativo più basso rappresentabile in 8 bit.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;11111111&lt;/code&gt; (il valore -1)&lt;/strong&gt;: somma unsigned 255, quindi 255 - 256 = -1. In qualsiasi ampiezza di bit, 8, 16, 32 o 64, una sequenza formata da soli 1 rappresenta sempre il valore -1.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;schema-decisionale-rapido&quot;&gt;Schema decisionale rapido&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;Numero binario: [X] X X X X X X X
E&#39; 0? SI: POSITIVO, fai solo la somma dei pesi.
E&#39; 1? SI: NEGATIVO, fai la somma dei pesi e sottrai 256.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;perche-non-basta-invertire-il-segno&quot;&gt;Perché non basta invertire il segno&lt;/h3&gt;
&lt;p&gt;Un dubbio comune è: perché la CPU non usa semplicemente un bit dedicato al segno, lasciando invariati gli altri 7 bit, come accade con il segno meno in decimale. Il problema, come accennato sopra, è che questo approccio produce due rappresentazioni distinte per lo zero, complicando sia i circuiti di confronto sia le operazioni aritmetiche. Il complemento a due elimina questa ambiguità e permette all&#39;ALU di trattare addizione e sottrazione con un unico circuito, indipendentemente dal segno degli operandi.&lt;/p&gt;
&lt;h2 id=&quot;complemento-a-due-a-16-bit-e-calcolo-tramite-python&quot;&gt;Complemento a due a 16 bit e calcolo tramite Python&lt;/h2&gt;
&lt;h3 id=&quot;scalabilita-del-complemento-a-due&quot;&gt;Scalabilità del complemento a due&lt;/h3&gt;
&lt;p&gt;Il meccanismo del complemento a due si applica identicamente a qualsiasi ampiezza di bit, come 16, 32 o 64 bit, ad esempio nei registri &lt;code&gt;rax&lt;/code&gt;, &lt;code&gt;rdi&lt;/code&gt; e simili.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;8 bit&lt;/strong&gt;: costante di sottrazione 2 elevato a 8, cioè 256.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;16 bit&lt;/strong&gt;: costante di sottrazione 2 elevato a 16, cioè 65536.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;64 bit&lt;/strong&gt;: costante 2 elevato a 64.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;lintervallo-dei-valori-a-16-bit&quot;&gt;L&#39;intervallo dei valori a 16 bit&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Modalità&lt;/th&gt; &lt;th&gt;Intervallo di valori&lt;/th&gt; &lt;th&gt;Rappresentazione binaria minima / massima&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Unsigned (senza segno)&lt;/td&gt; &lt;td&gt;Da 0 a 65535&lt;/td&gt; &lt;td&gt;0000000000000000 -&gt; 1111111111111111&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Signed (con segno)&lt;/td&gt; &lt;td&gt;Da -32768 a +32767&lt;/td&gt; &lt;td&gt;1000000000000000 -&gt; 0111111111111111&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Valori chiave: &lt;code&gt;0111111111111111&lt;/code&gt; è il massimo signed (+32767); &lt;code&gt;1000000000000000&lt;/code&gt; è il minimo signed, calcolato come 32768 - 65536 = -32768; &lt;code&gt;1111111111111111&lt;/code&gt; rappresenta sempre -1, calcolato come 65535 - 65536 = -1.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Rilevanza in cybersecurity, in particolare nel pwn: scambiare o interpretare erroneamente un numero con segno per uno senza segno porta a bug critici come l&#39;integer overflow, errori nel controllo delle dimensioni dei buffer e vulnerabilità sfruttabili negli exploit. &lt;/div&gt;
&lt;h3 id=&quot;calcolo-pratico-con-python&quot;&gt;Calcolo pratico con Python&lt;/h3&gt;
&lt;p&gt;Dato che i numeri a 16 bit sono complessi da calcolare a mente, si utilizza la shell interattiva IPython. Il prefisso &lt;code&gt;0b&lt;/code&gt; indica a Python che il numero è scritto in notazione binaria.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;hacker@dojo$ ipython
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Si inserisce il numero binario per ottenere il valore unsigned, poi si sottrae 65536 poiché il numero inizia con 1 ed è quindi un negativo a 16 bit.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;In [1]: 0b1001111101011100
Out[1]: 40796
In [2]: 40796 - 65536
Out[2]: -24740
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Un&#39;alternativa comoda per verificare il risultato nella direzione opposta è usare le funzioni built-in &lt;code&gt;bin()&lt;/code&gt; e &lt;code&gt;hex()&lt;/code&gt; di Python per ottenere la rappresentazione binaria o esadecimale a partire da un intero, incluso un valore negativo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;In [3]: bin(-24740)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;soluzione-della-sfida-16-bit&quot;&gt;Soluzione della sfida (16 bit)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;/challenge/decode
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;complemento-a-due-a-32-bit-4-byte-e-intervalli-di-memoria&quot;&gt;Complemento a due a 32 bit (4 byte) e intervalli di memoria&lt;/h2&gt;
&lt;h3 id=&quot;estensione-della-logica-a-32-bit&quot;&gt;Estensione della logica a 32 bit&lt;/h3&gt;
&lt;p&gt;Il principio del complemento a due rimane identico passando a 32 bit, pari a 4 byte di memoria, tipicamente ospitati nei registri a 32 bit come &lt;code&gt;eax&lt;/code&gt;, la porzione bassa del registro a 64 bit &lt;code&gt;rax&lt;/code&gt;.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dimensione&lt;/strong&gt;: 32 bit consentono 2 elevato a 32, cioè 4.294.967.296 combinazioni possibili.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Costante di sottrazione&lt;/strong&gt;: quando il MSB, il 32esimo bit da destra, è impostato a 1, il numero è negativo. Per ottenere il valore con segno si sottrae 2 elevato a 32, cioè 4.294.967.296.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;intervalli-e-valori-chiave-a-32-bit&quot;&gt;Intervalli e valori chiave a 32 bit&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Modalità&lt;/th&gt; &lt;th&gt;Formula dell&#39;intervallo&lt;/th&gt; &lt;th&gt;Intervallo numerico decimale&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Unsigned (senza segno)&lt;/td&gt; &lt;td&gt;Da 0 a 2 elevato a 32 - 1&lt;/td&gt; &lt;td&gt;Da 0 a 4.294.967.295&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Signed (con segno)&lt;/td&gt; &lt;td&gt;Da -2 elevato a 31 a +2 elevato a 31 - 1&lt;/td&gt; &lt;td&gt;Da -2.147.483.648 a +2.147.483.647&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt; &lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Rappresentazione binaria (32 bit)&lt;/th&gt; &lt;th&gt;Valore unsigned&lt;/th&gt; &lt;th&gt;Valore signed (complemento a due)&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;01111111 11111111 11111111 11111111&lt;/td&gt; &lt;td&gt;2.147.483.647&lt;/td&gt; &lt;td&gt;+2.147.483.647 (massimo signed)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;10000000 00000000 00000000 00000000&lt;/td&gt; &lt;td&gt;2.147.483.648&lt;/td&gt; &lt;td&gt;-2.147.483.648 (minimo signed)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;11111111 11111111 11111111 11111111&lt;/td&gt; &lt;td&gt;4.294.967.295&lt;/td&gt; &lt;td&gt;-1&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;calcolo-pratico-con-python-1&quot;&gt;Calcolo pratico con Python&lt;/h3&gt;
&lt;p&gt;Per evitare errori nei calcoli manuali su stringhe a 32 bit, si usa la stessa calcolatrice IPython: si inserisce il numero binario anteponendo &lt;code&gt;0b&lt;/code&gt; per ricavare il valore unsigned, poi si verifica il bit di segno e, se la sequenza inizia con 1, si sottrae &lt;code&gt;2**32&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;hacker@dojo$ ipython
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;In [1]: val = 0b10000000000000000000000000000001
In [2]: val
Out[2]: 2147483649
In [3]: val - (2**32)
Out[3]: -2147483647
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;soluzione-della-sfida-32-bit&quot;&gt;Soluzione della sfida (32 bit)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;/challenge/decode
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;conversione-da-decimale-a-binario-a-8-bit&quot;&gt;Conversione da decimale a binario a 8 bit&lt;/h2&gt;
&lt;p&gt;Per convertire un numero decimale, ad esempio 42, nel corrispondente valore binario a 8 bit (&lt;code&gt;00101010&lt;/code&gt;), si segue un procedimento in tre fasi: divisioni successive, lettura dei resti e aggiunta degli zeri di riempimento, detto padding.&lt;/p&gt;
&lt;h3 id=&quot;passo-1-divisioni-successive-per-2&quot;&gt;Passo 1: divisioni successive per 2&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;42 / 2 = 21 con resto 0 (bit meno significativo, LSB, a destra)&lt;/li&gt;
&lt;li&gt;21 / 2 = 10 con resto 1&lt;/li&gt;
&lt;li&gt;10 / 2 = 5 con resto 0&lt;/li&gt;
&lt;li&gt;5 / 2 = 2 con resto 1&lt;/li&gt;
&lt;li&gt;2 / 2 = 1 con resto 0&lt;/li&gt;
&lt;li&gt;1 / 2 = 0 con resto 1 (bit più significativo, MSB, a sinistra)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;passo-2-lettura-dei-resti-dal-basso-verso-lalto&quot;&gt;Passo 2: lettura dei resti (dal basso verso l&#39;alto)&lt;/h3&gt;
&lt;p&gt;Raccogliendo i resti dall&#39;ultima divisione effettuata fino alla prima, si ottiene il numero binario puro a 6 bit: &lt;code&gt;101010&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;passo-3-padding-a-8-bit&quot;&gt;Passo 3: padding a 8 bit&lt;/h3&gt;
&lt;p&gt;Il risultato ottenuto contiene solo 6 cifre. Poiché la memoria del computer lavora a blocchi di byte (8 bit), si aggiungono due zeri a sinistra per completare la larghezza di 8 bit, ottenendo &lt;code&gt;00101010&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;schema-riassuntivo&quot;&gt;Schema riassuntivo&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Passaggio&lt;/th&gt; &lt;th&gt;Operazione&lt;/th&gt; &lt;th&gt;Risultato&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;1. Calcolo dei resti&lt;/td&gt; &lt;td&gt;Divisioni consecutive per 2&lt;/td&gt; &lt;td&gt;0, 1, 0, 1, 0, 1 (dal primo all&#39;ultimo)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;2. Inversione dell&#39;ordine&lt;/td&gt; &lt;td&gt;Leggi i resti dal basso verso l&#39;alto&lt;/td&gt; &lt;td&gt;101010 (6 bit)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;3. Aggiunta zeri (padding)&lt;/td&gt; &lt;td&gt;Aggiungi zeri a sinistra fino a 8 bit&lt;/td&gt; &lt;td&gt;00101010 (1 byte completo)&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;conversione-da-binario-a-esadecimale-metodo-della-somma-pesata-8-4-2-1&quot;&gt;Conversione da binario a esadecimale (metodo della somma pesata 8-4-2-1)&lt;/h2&gt;
&lt;h3 id=&quot;il-principio-dei-4-bit-nibble&quot;&gt;Il principio dei 4 bit (nibble)&lt;/h3&gt;
&lt;p&gt;Un byte, 8 bit, si divide esattamente in 2 gruppi da 4 bit, detti nibble. Ogni nibble possiede una propria griglia di pesi basata sulle potenze di 2: 8, 4, 2, 1.&lt;/p&gt;
&lt;h3 id=&quot;regola-della-somma-pesata-passo-passo&quot;&gt;Regola della somma pesata passo-passo&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Separazione&lt;/strong&gt;: dividi il byte a 8 bit a metà, ottenendo due gruppi da 4 bit, sinistra e destra.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Somma pesata&lt;/strong&gt;: per ciascun gruppo, somma i pesi (8, 4, 2, 1) in corrispondenza dei bit impostati a 1.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Mappatura esadecimale&lt;/strong&gt;: se la somma è tra 0 e 9, scrivi la cifra così com&#39;è; se la somma è tra 10 e 15, convertila nella lettera corrispondente (10 = a, 11 = b, 12 = c, 13 = d, 14 = e, 15 = f).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Unione e prefisso&lt;/strong&gt;: concatena i due risultati e anteponi il prefisso &lt;code&gt;0x&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&quot;esempi-pratici&quot;&gt;Esempi pratici&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Esempio 1, &lt;code&gt;00010110&lt;/code&gt;&lt;/strong&gt;: separazione &lt;code&gt;0001&lt;/code&gt; e &lt;code&gt;0110&lt;/code&gt;. Primo gruppo, solo il peso 1 attivo, uguale a 1. Secondo gruppo, pesi 4 + 2 attivi, uguale a 6. Risultato: &lt;code&gt;0x16&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Esempio 2, &lt;code&gt;11100011&lt;/code&gt;&lt;/strong&gt;: separazione &lt;code&gt;1110&lt;/code&gt; e &lt;code&gt;0011&lt;/code&gt;. Primo gruppo, pesi 8 + 4 + 2 = 14, uguale a e. Secondo gruppo, pesi 2 + 1 = 3. Risultato: &lt;code&gt;0xe3&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;esecuzione-della-sfida&quot;&gt;Esecuzione della sfida&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;/challenge/convert
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;decodifica-da-esadecimale-a-binario-espansione-a-4-bit&quot;&gt;Decodifica da esadecimale a binario (espansione a 4 bit)&lt;/h2&gt;
&lt;h3 id=&quot;il-principio-inverso-da-hex-a-binario&quot;&gt;Il principio inverso: da hex a binario&lt;/h3&gt;
&lt;p&gt;La decodifica esegue l&#39;operazione opposta rispetto alla codifica, mappando ogni simbolo esadecimale direttamente nel suo blocco di bit corrispondente: 1 cifra esadecimale si espande in esattamente 4 bit; 2 cifre esadecimali, 1 byte, si espandono in esattamente 8 bit.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Regola fondamentale: ogni cifra esadecimale deve essere rappresentata sempre da 4 bit interi, mantenendo gli zeri iniziali all&#39;interno di ciascun blocco da 4 bit. Ad esempio, la cifra 0 diventa 0000, non un singolo 0. &lt;/div&gt;
&lt;h3 id=&quot;tabella-di-lookup-esadecimale-binario&quot;&gt;Tabella di lookup esadecimale-binario&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Cifra hex&lt;/th&gt; &lt;th&gt;Valore decimale&lt;/th&gt; &lt;th&gt;4 bit&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;0&lt;/td&gt;&lt;td&gt;0&lt;/td&gt;&lt;td&gt;0000&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;1&lt;/td&gt;&lt;td&gt;1&lt;/td&gt;&lt;td&gt;0001&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;2&lt;/td&gt;&lt;td&gt;2&lt;/td&gt;&lt;td&gt;0010&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;3&lt;/td&gt;&lt;td&gt;3&lt;/td&gt;&lt;td&gt;0011&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;4&lt;/td&gt;&lt;td&gt;4&lt;/td&gt;&lt;td&gt;0100&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;5&lt;/td&gt;&lt;td&gt;5&lt;/td&gt;&lt;td&gt;0101&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;6&lt;/td&gt;&lt;td&gt;6&lt;/td&gt;&lt;td&gt;0110&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;7&lt;/td&gt;&lt;td&gt;7&lt;/td&gt;&lt;td&gt;0111&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;8&lt;/td&gt;&lt;td&gt;8&lt;/td&gt;&lt;td&gt;1000&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;9&lt;/td&gt;&lt;td&gt;9&lt;/td&gt;&lt;td&gt;1001&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;a&lt;/td&gt;&lt;td&gt;10&lt;/td&gt;&lt;td&gt;1010&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;b&lt;/td&gt;&lt;td&gt;11&lt;/td&gt;&lt;td&gt;1011&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;c&lt;/td&gt;&lt;td&gt;12&lt;/td&gt;&lt;td&gt;1100&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;d&lt;/td&gt;&lt;td&gt;13&lt;/td&gt;&lt;td&gt;1101&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;e&lt;/td&gt;&lt;td&gt;14&lt;/td&gt;&lt;td&gt;1110&lt;/td&gt;&lt;/tr&gt; &lt;tr&gt;&lt;td class=&quot;fw-semibold&quot;&gt;f&lt;/td&gt;&lt;td&gt;15&lt;/td&gt;&lt;td&gt;1111&lt;/td&gt;&lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;esempio-pratico-decodifica-di-0x0a&quot;&gt;Esempio pratico: decodifica di 0x0a&lt;/h3&gt;
&lt;p&gt;Scomponendo il valore &lt;code&gt;0x0a&lt;/code&gt;: la cifra &lt;code&gt;0&lt;/code&gt; vale 0, nessun peso attivo, quindi &lt;code&gt;0000&lt;/code&gt;; la cifra &lt;code&gt;a&lt;/code&gt; vale 10, pesi 8 e 2 attivi, quindi &lt;code&gt;1010&lt;/code&gt;. Concatenando i due blocchi si ottiene il risultato a 8 bit: &lt;code&gt;00001010&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;soluzione-della-sfida&quot;&gt;Soluzione della sfida&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;/challenge/convert
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;sintesi-operativa-le-4-interpretazioni-dello-stesso-valore&quot;&gt;Sintesi operativa: le 4 interpretazioni dello stesso valore&lt;/h2&gt;
&lt;h3 id=&quot;il-concetto-chiave&quot;&gt;Il concetto chiave&lt;/h3&gt;
&lt;p&gt;La memoria del computer memorizza esclusivamente sequenze di bit (0 e 1). Un singolo blocco di bit non ha un significato intrinseco: la CPU e il programmatore possono interpretarlo in 4 modi diversi, a partire dallo stesso valore &lt;code&gt;11100100&lt;/code&gt;: come binario puro, come coppia esadecimale (&lt;code&gt;0xe4&lt;/code&gt;), come decimale unsigned (228, sempre positivo) e come decimale signed in complemento a due (-28).&lt;/p&gt;
&lt;h3 id=&quot;matrice-di-conversione-8-bit&quot;&gt;Matrice di conversione (8 bit)&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Interpretazione&lt;/th&gt; &lt;th&gt;Come si calcola / formato&lt;/th&gt; &lt;th&gt;Esempio (11100100)&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;1. Binario&lt;/td&gt; &lt;td&gt;Sequenza completa di 8 bit, con padding&lt;/td&gt; &lt;td&gt;11100100&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;2. Esadecimale&lt;/td&gt; &lt;td&gt;Separazione in 2 nibble (4 bit) + pesi 8-4-2-1&lt;/td&gt; &lt;td&gt;1110 (14, e) e 0100 (4, 4), risultato 0xe4&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;3. Decimale unsigned&lt;/td&gt; &lt;td&gt;Somma diretta dei pesi&lt;/td&gt; &lt;td&gt;128 + 64 + 32 + 4 = 228&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;4. Decimale signed&lt;/td&gt; &lt;td&gt;Se il primo bit è 1: unsigned - 256&lt;/td&gt; &lt;td&gt;228 - 256 = -28&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;esercizi-di-riepilogo-svolti-passo-passo&quot;&gt;Esercizi di riepilogo svolti (passo-passo)&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Esercizio 1, partendo dal binario &lt;code&gt;10110110&lt;/code&gt; (8 bit)&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Esadecimale: separazione &lt;code&gt;1011&lt;/code&gt; e &lt;code&gt;0110&lt;/code&gt;. Blocco 1, 8 + 2 + 1 = 11, uguale a b. Blocco 2, 4 + 2 = 6. Risultato hex: &lt;code&gt;0xb6&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Decimale unsigned: somma pesi 128 + 32 + 16 + 4 + 2 = 182.&lt;/li&gt;
&lt;li&gt;Decimale signed: il primo bit è 1, quindi 182 - 256 = -74.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Esercizio 2, partendo dall&#39;esadecimale &lt;code&gt;0xff00&lt;/code&gt; (16 bit)&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Binario: ogni cifra hex diventa 4 bit, f = 1111, f = 1111, 0 = 0000, 0 = 0000. Risultato: &lt;code&gt;1111111100000000&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Decimale unsigned: 65280.&lt;/li&gt;
&lt;li&gt;Decimale signed: il primo bit è 1, ampiezza 16 bit (2 elevato a 16 = 65536), quindi 65280 - 65536 = -256.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Esercizio 3, partendo dal decimale signed &lt;code&gt;-42&lt;/code&gt; (8 bit)&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Decimale unsigned: formula inversa, signed + 256, quindi -42 + 256 = 214.&lt;/li&gt;
&lt;li&gt;Binario: 214 sui pesi 128, 64, 32, 16, 8, 4, 2, 1, uguale a 128 + 64 + 16 + 4 + 2, risultato &lt;code&gt;11010110&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Esadecimale: separazione &lt;code&gt;1101&lt;/code&gt; e &lt;code&gt;0110&lt;/code&gt;. &lt;code&gt;1101&lt;/code&gt; (8 + 4 + 1 = 13, d), &lt;code&gt;0110&lt;/code&gt; (4 + 2 = 6). Risultato hex: &lt;code&gt;0xd6&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;palestra-autonoma-di-esercitazione&quot;&gt;Palestra autonoma di esercitazione&lt;/h3&gt;
&lt;p&gt;Prova a risolvere questi 3 casi compilando tutte e 4 le interpretazioni.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Caso A (8 bit)&lt;/strong&gt;: dato il valore binario &lt;code&gt;11001001&lt;/code&gt;, calcola hex, unsigned e signed.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Caso B (8 bit)&lt;/strong&gt;: dato il valore esadecimale &lt;code&gt;0x3f&lt;/code&gt;, calcola binario, unsigned e signed.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Caso C (8 bit)&lt;/strong&gt;: dato il valore decimale signed &lt;code&gt;-15&lt;/code&gt;, calcola unsigned, binario ed esadecimale.&lt;/li&gt;
&lt;/ul&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="reverse engineering"/>
      
    
      
      
    <category term="memory management"/>
      
    
      
      
    <category term="linux"/>
      
    
      
      
    <category term="binary representation"/>
      
    
  </entry>
  
  <entry>
    <title>Address Resolution Protocol</title>
    <link href="https://dino-996.github.io/blog/address-resolution-protocol/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/address-resolution-protocol/</id>
    <published>2026-09-11T00:00:00.000Z</published>
    <updated>2026-09-11T00:00:00.000Z</updated>
    
    <summary>Guida tecnica al protocollo ARP (Address Resolution Protocol): scopri come traduce gli indirizzi IP in indirizzi MAC all&#39;interno della rete locale, come interagiscono le tabelle NAT e ARP nei router e quali sono i rischi di sicurezza legati agli attacchi ARP Spoofing.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;p&gt;Arp (Address Resolution Protocol) è un protocollo che opera a cavallo tra il livello 2 (collegamento) e il livello 3 (rete) del modello OSI. Il suo compito è tradurre un indirizzo IP nel corrispondente indirizzo fisico (MAC), passaggio necessario per incapsulare i dati in un frame Ethernet.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Nota editoriale: i frame ARP non vengono incapsulati in un pacchetto IP, ma trasmessi direttamente come frame Ethernet con un proprio EtherType dedicato. Questo dettaglio, non presente nella bozza originale, è segnalato come &quot;Da verificare&quot; nel report di revisione. &lt;/div&gt;
&lt;p&gt;Quando un host deve inviare dati al router, deve conoscere il MAC della scheda di rete del router. Viceversa, quando il router deve consegnare dati a un host, deve conoscerne il MAC. All&#39;interno della stessa subnet, dal punto di vista della risoluzione degli indirizzi, il router si comporta esattamente come un host qualsiasi verso gli altri dispositivi della LAN.&lt;/p&gt;
&lt;h2 id=&quot;il-formato-del-pacchetto-arp&quot;&gt;Il formato del pacchetto ARP&lt;/h2&gt;
&lt;p&gt;Un pacchetto ARP è strutturato in campi ben definiti. Conoscerli aiuta a interpretare correttamente una cattura di rete, ad esempio con Wireshark, durante il debug.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Campo&lt;/th&gt; &lt;th&gt;Descrizione&lt;/th&gt; &lt;th&gt;Dimensione tipica&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Hardware type&lt;/td&gt; &lt;td&gt;Tipo di rete fisica utilizzata, es. Ethernet&lt;/td&gt; &lt;td&gt;2 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Protocol type&lt;/td&gt; &lt;td&gt;Protocollo di livello 3 da risolvere, es. IPv4&lt;/td&gt; &lt;td&gt;2 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Hardware address length&lt;/td&gt; &lt;td&gt;Lunghezza dell&#39;indirizzo MAC&lt;/td&gt; &lt;td&gt;1 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Protocol address length&lt;/td&gt; &lt;td&gt;Lunghezza dell&#39;indirizzo IP&lt;/td&gt; &lt;td&gt;1 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Operation (opcode)&lt;/td&gt; &lt;td&gt;1 per Request, 2 per Reply&lt;/td&gt; &lt;td&gt;2 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Sender MAC address&lt;/td&gt; &lt;td&gt;MAC del mittente&lt;/td&gt; &lt;td&gt;6 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Sender IP address&lt;/td&gt; &lt;td&gt;IP del mittente&lt;/td&gt; &lt;td&gt;4 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Target MAC address&lt;/td&gt; &lt;td&gt;MAC del destinatario, vuoto in una request&lt;/td&gt; &lt;td&gt;6 byte&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Target IP address&lt;/td&gt; &lt;td&gt;IP del destinatario&lt;/td&gt; &lt;td&gt;4 byte&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt; &lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt; Da verificare prima della pubblicazione: dimensioni esatte dei campi (in byte) e valori numerici degli opcode indicati in tabella. &lt;/div&gt;
&lt;h2 id=&quot;le-due-tabelle-del-router-la-tabella-nat-e-arp&quot;&gt;Le due tabelle del router, la tabella NAT e ARP&lt;/h2&gt;
&lt;p&gt;Il router mantiene due tabelle distinte, con scopi diversi.&lt;/p&gt;
&lt;h3 id=&quot;tabella-nat-network-address-translation&quot;&gt;Tabella NAT (Network Address Translation)&lt;/h3&gt;
&lt;p&gt;Tutti i dispositivi di una subnet condividono lo stesso IP pubblico. Il router usa quindi le porte TCP/UDP per distinguere le connessioni dei singoli host e sapere a chi inoltrare le risposte in arrivo da Internet.&lt;/p&gt;
&lt;p&gt;Esempio pratico:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;L&#39;host A (&lt;code&gt;192.168.1.50&lt;/code&gt;) apre una pagina su un server esterno (&lt;code&gt;93.184.216.34&lt;/code&gt;). Il pacchetto in uscita ha intestazione &lt;code&gt;192.168.1.50:51234 → 93.184.216.34:443&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Prima di inviarlo su Internet, il router sostituisce l&#39;IP privato con il proprio IP pubblico, es. &lt;code&gt;2.3.4.5&lt;/code&gt;, e assegna una porta pubblica univoca, es. &lt;code&gt;10500&lt;/code&gt;. Salva quindi in tabella l&#39;associazione: &lt;code&gt;2.3.4.5:10500 ↔ 192.168.1.50:51234 ↔ 93.184.216.34:443&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Il server web risponde a &lt;code&gt;2.3.4.5:10500&lt;/code&gt;. Il router riceve il pacchetto, legge la porta di destinazione, consulta la tabella NAT, riscrive l&#39;IP con quello privato dell&#39;host A e lo inoltra sulla rete interna.&lt;/li&gt;
&lt;/ol&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;&lt;th&gt;IP/Porta dell&#39;indirizzo privato&lt;/th&gt;
        &lt;th&gt;IP/Porta dell&#39;indirizzo pubblico&lt;/th&gt;
        &lt;th&gt;IP/Porta di destinazione&lt;/th&gt;&lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td &gt;192.168.50:51234&lt;/td&gt;
        &lt;td&gt;2.3.4.5:10500&lt;/td&gt;
        &lt;td&gt;93.184.216.34:443&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;
&lt;h3 id=&quot;tabella-arp&quot;&gt;Tabella ARP&lt;/h3&gt;
&lt;p&gt;Serve a mappare gli IP della rete locale ai relativi MAC address. Gli host la usano per comunicare tra loro sulla stessa subnet; il router la usa quando deve recapitare a un host locale un pacchetto arrivato da Internet, dopo averne trovato la destinazione nella tabella NAT.&lt;/p&gt;
&lt;h2 id=&quot;chi-possiede-una-tabella-arp&quot;&gt;Chi possiede una tabella arp&lt;/h2&gt;
&lt;p&gt;Non tutti i dispositivi di rete la gestiscono, perché dipende dal livello OSI a cui operano.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Dispositivo&lt;/th&gt; &lt;th&gt;Tabella arp&lt;/th&gt; &lt;th&gt;Note&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Host (pc, smartphone, server)&lt;/td&gt; &lt;td&gt;Sì&lt;/td&gt; &lt;td&gt;Serve per costruire il frame Ethernet verso IP locali&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Router&lt;/td&gt; &lt;td&gt;Sì&lt;/td&gt; &lt;td&gt;Una tabella per ogni interfaccia collegata a una subnet&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Switch di livello 2 (standard)&lt;/td&gt; &lt;td&gt;No&lt;/td&gt; &lt;td&gt;Usa una tabella MAC (CAM): MAC, porta fisica&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Switch gestibili / livello 3&lt;/td&gt; &lt;td&gt;Solo se fa routing o per l&#39;interfaccia di management&lt;/td&gt; &lt;td&gt;Es. SSH, Web UI&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;meccanismo-di-risoluzione-arp&quot;&gt;Meccanismo di risoluzione ARP&lt;/h2&gt;
&lt;p&gt;Scenario: l&#39;host A (&lt;code&gt;192.168.1.10&lt;/code&gt;, MAC &lt;code&gt;AA:AA&lt;/code&gt;) deve comunicare con l&#39;host B (&lt;code&gt;192.168.1.20&lt;/code&gt;, MAC &lt;code&gt;BB:BB&lt;/code&gt;), sulla stessa rete locale.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Controllo cache locale.&lt;/strong&gt; A verifica se &lt;code&gt;192.168.1.20 → BB:BB&lt;/code&gt; è già nella propria tabella ARP. Se sì, incapsula subito i dati nel frame Ethernet.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;ARP Request.&lt;/strong&gt; Se l&#39;associazione manca, A mette in pausa l&#39;invio e genera una richiesta: &amp;quot;Chi ha l&#39;IP 192.168.1.20? Dillo a 192.168.1.10, MAC AA:AA&amp;quot;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Invio in broadcast.&lt;/strong&gt; Il frame ha destinazione &lt;code&gt;FF:FF:FF:FF:FF:FF&lt;/code&gt;. Lo switch lo inoltra su tutte le porte attive della LAN.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Filtro sugli host.&lt;/strong&gt; Tutti ricevono la richiesta; chi non ha l&#39;IP &lt;code&gt;192.168.1.20&lt;/code&gt; la scarta. Solo B la riconosce come propria.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;ARP Reply.&lt;/strong&gt; B salva preventivamente i dati di A nella propria tabella, poi risponde in unicast al MAC &lt;code&gt;AA:AA&lt;/code&gt;: &amp;quot;L&#39;IP 192.168.1.20 è associato al MAC BB:BB&amp;quot;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Aggiornamento e invio.&lt;/strong&gt; A riceve la risposta, salva &lt;code&gt;192.168.1.20 → BB:BB&lt;/code&gt;, con un TTL di scadenza, e trasmette finalmente il pacchetto dati originale.&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id=&quot;comunicazione-locale-vs-verso-internet&quot;&gt;Comunicazione locale vs verso Internet&lt;/h2&gt;
&lt;p&gt;ARP funziona solo all&#39;interno dello &lt;strong&gt;stesso dominio di broadcast&lt;/strong&gt;, cioè della stessa &lt;strong&gt;subnet&lt;/strong&gt;.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Destinazione locale&lt;/strong&gt; (&lt;code&gt;192.168.1.X&lt;/code&gt;): la richiesta ARP cerca direttamente il MAC dell&#39;host di destinazione.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Destinazione esterna&lt;/strong&gt; (es. &lt;code&gt;8.8.8.8&lt;/code&gt;): l&#39;host riconosce che l&#39;IP non appartiene alla rete locale. Non fa una ARP Request per &lt;code&gt;8.8.8.8&lt;/code&gt;, ma per l&#39;IP del default gateway, es. &lt;code&gt;192.168.1.1&lt;/code&gt;. Il frame va al MAC del router, che si occupa di instradare il pacchetto verso l&#39;esterno.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;consultare-e-gestire-la-tabella-arp-da-riga-di-comando&quot;&gt;Consultare e gestire la tabella ARP da riga di comando&lt;/h2&gt;
&lt;p&gt;Per uno sviluppatore junior o un IT manager, è utile sapere come ispezionare la propria tabella ARP direttamente da terminale.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Sistema operativo&lt;/th&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Descrizione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Windows&lt;/td&gt; &lt;td&gt;&lt;code&gt;arp -a&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Mostra la tabella ARP corrente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Linux (legacy)&lt;/td&gt; &lt;td&gt;&lt;code&gt;arp -a&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Richiede il pacchetto net-tools&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Linux (moderno)&lt;/td&gt; &lt;td&gt;&lt;code&gt;ip neigh show&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Equivalente attuale tramite iproute2&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;macOS&lt;/td&gt; &lt;td&gt;&lt;code&gt;arp -a&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Mostra la tabella ARP corrente&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Un esempio di output su Linux con lo strumento moderno:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;$ ip neigh show
192.168.1.1 dev eth0 lladdr aa:bb:cc:dd:ee:ff REACHABLE
192.168.1.20 dev eth0 lladdr 11:22:33:44:55:66 STALE
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;arp-e-ipv6&quot;&gt;ARP e IPv6&lt;/h2&gt;
&lt;p&gt;In IPv6, ARP non viene utilizzato. La risoluzione degli indirizzi è affidata a NDP (Neighbor Discovery Protocol), che si appoggia su ICMPv6 invece che su un protocollo di livello 2 a sé stante. Il principio di fondo, cioè associare un indirizzo di rete a un indirizzo fisico, resta lo stesso: cambia il meccanismo sottostante.&lt;/p&gt;
&lt;h2 id=&quot;la-vulnerabilita-alla-base&quot;&gt;La vulnerabilità alla base&lt;/h2&gt;
&lt;p&gt;Il protocollo ARP soffre di due limiti strutturali:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Assenza di autenticazione.&lt;/strong&gt; Non verifica se l&#39;emittente del pacchetto è il reale proprietario dell&#39;indirizzo IP dichiarato.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Natura stateless.&lt;/strong&gt; I dispositivi accettano e memorizzano pacchetti di risposta ARP Reply anche se non hanno mai inviato alcuna richiesta (il cosiddetto &lt;strong&gt;Gratuitous ARP&lt;/strong&gt;) sovrascrivendo la propria tabella senza alcun controllo.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Questi due limiti sono la base tecnica su cui si fonda l&#39;ARP Spoofing, una tecnica di attacco &lt;strong&gt;man-in-the-middle&lt;/strong&gt; a livello locale.&lt;/p&gt;
&lt;h2 id=&quot;le-fasi-dellattacco&quot;&gt;Le fasi dell&#39;attacco&lt;/h2&gt;
&lt;p&gt;Immaginiamo una rete locale con questi attori:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Vittima:&lt;/strong&gt; IP &lt;code&gt;192.168.1.50&lt;/code&gt;, MAC &lt;code&gt;AA:AA&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Router:&lt;/strong&gt; IP &lt;code&gt;192.168.1.1&lt;/code&gt;, MAC &lt;code&gt;RR:RR&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Attaccante:&lt;/strong&gt; IP &lt;code&gt;192.168.1.99&lt;/code&gt;, MAC &lt;code&gt;XX:XX&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Lo schema seguente riassume il risultato dell&#39;attacco, con entrambe le vittime convinte di parlare direttamente tra loro mentre in realtà passano per l&#39;attaccante:&lt;/p&gt;
&lt;figure style=&quot;border: 1px solid #ddd; border-radius: 5px; padding: 10px; display: inline-block; max-width: 620px; margin-top: 15px; margin-bottom: 15px;&quot;&gt;
  &lt;img src=&quot;https://res.cloudinary.com/dcro2pbqr/image/upload/v1789150406/ARP_poisoning_drawio_0608cf6493.png&quot; alt=&quot;Anatomia di un attacco ARP Spoofing&quot; width=&quot;1920&quot; height=&quot;150&quot; loading=&quot;lazy&quot; style=&quot;max-width: 100%; border-radius: 5px&quot;&gt;
&lt;/figure&gt;
&lt;h3 id=&quot;fase-1-invio-dei-pacchetti-falsificati-avvelenamento&quot;&gt;Fase 1: invio dei pacchetti falsificati (avvelenamento)&lt;/h3&gt;
&lt;p&gt;L&#39;attaccante invia in continuazione &lt;strong&gt;pacchetti ARP malevoli&lt;/strong&gt; a entrambi gli estremi della comunicazione:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Alla vittima invia una risposta ARP dicendo: &amp;quot;L&#39;IP 192.168.1.1, il router, ha MAC XX:XX&amp;quot;, cioè il MAC dell&#39;attaccante.&lt;/li&gt;
&lt;li&gt;Al router invia una risposta ARP dicendo: &amp;quot;L&#39;IP 192.168.1.50, la vittima, ha MAC XX:XX&amp;quot;, cioè ancora il MAC dell&#39;attaccante.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;fase-2-aggiornamento-delle-tabelle-arp-e-reindirizzamento&quot;&gt;Fase 2: aggiornamento delle tabelle arp e reindirizzamento&lt;/h3&gt;
&lt;p&gt;I due dispositivi aggiornano le rispettive tabelle ARP locali memorizzando le informazioni false. Da questo momento, quando lo switch di rete deve smistare il traffico di livello 2:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Quando la vittima invia un frame indirizzato all&#39;IP del router, lo invia al MAC &lt;code&gt;XX:XX&lt;/code&gt;. Lo switch inoltra il frame direttamente alla porta dell&#39;attaccante.&lt;/li&gt;
&lt;li&gt;Quando il router risponde alla vittima, invia il frame al MAC &lt;code&gt;XX:XX&lt;/code&gt;. Lo switch lo consegna di nuovo all&#39;attaccante.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;fase-3-ip-forwarding-inoltro-del-traffico&quot;&gt;Fase 3: ip forwarding (inoltro del traffico)&lt;/h3&gt;
&lt;p&gt;Per non far cadere la connessione e non insospettire la vittima, l&#39;attaccante attiva l&#39;IP forwarding sul proprio sistema operativo. Quando la sua scheda di rete riceve i pacchetti intercettati, li legge e li rinvia al destinatario reale, router o vittima, mantenendo la comunicazione apparentemente intatta.&lt;/p&gt;
&lt;h2 id=&quot;conseguenze-dellattacco&quot;&gt;Conseguenze dell&#39;attacco&lt;/h2&gt;
&lt;p&gt;Collocandosi al centro del flusso dati, l&#39;attaccante può:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Eseguire sniffing.&lt;/strong&gt; Leggere in chiaro il traffico non crittografato, come HTTP, FTP, Telnet, credenziali e cookie di sessione.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Manipolare i dati.&lt;/strong&gt; Alterare i contenuti dei pacchetti prima di inoltrarli, ad esempio con iniezione di codice malevolo o reindirizzamento DNS.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Eseguire denial of service (DoS).&lt;/strong&gt; Scartare i pacchetti ricevuti anziché inoltrarli, interrompendo la connessione della vittima a Internet.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Queste tre conseguenze spiegano perché l&#39;ARP Spoofing sia considerato uno degli attacchi più insidiosi nelle reti locali. Non richiede accesso privilegiato, solo la possibilità di inviare pacchetti sulla stessa LAN della vittima.&lt;/p&gt;
&lt;h2 id=&quot;difese-contro-larp-spoofing&quot;&gt;Difese contro l&#39;arp spoofing&lt;/h2&gt;
&lt;p&gt;Conoscere il meccanismo dell&#39;attacco è utile solo se accompagnato dalle contromisure che un amministratore di rete può adottare:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dynamic ARP Inspection (DAI).&lt;/strong&gt; Funzionalità disponibile su molti switch gestibili che valida le risposte ARP confrontandole con un database di associazioni IP-MAC affidabili, scartando quelle sospette.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Voci ARP statiche.&lt;/strong&gt; Su segmenti di rete critici, come server e gateway, è possibile fissare manualmente le associazioni IP-MAC per impedirne la sovrascrittura automatica.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Port security sugli switch.&lt;/strong&gt; Limita il numero di indirizzi MAC ammessi su ciascuna porta, rendendo più difficile per un dispositivo non autorizzato inserirsi nella LAN.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Segmentazione tramite VLAN.&lt;/strong&gt; Riduce l&#39;ampiezza del dominio di broadcast e, di conseguenza, la superficie di attacco raggiungibile con tecniche di ARP spoofing.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Strumenti di monitoraggio.&lt;/strong&gt; Software come &lt;a href=&quot;https://www.kali.org/tools/arpwatch/&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferer&quot;&gt;arpwatch&lt;/a&gt; osservano le variazioni nelle associazioni IP-MAC della rete e segnalano anomalie sospette agli amministratori.&lt;/li&gt;
&lt;/ul&gt;


]]></content>
    
    
    
      
      
    <category term="ARP"/>
      
    
      
      
    <category term="networking"/>
      
    
      
      
    <category term="cybersecurity"/>
      
    
      
      
    <category term="LAN"/>
      
    
      
      
    <category term="ARP spoofing"/>
      
    
  </entry>
  
  <entry>
    <title>Implementazione di un server e un client TCP in Java</title>
    <link href="https://dino-996.github.io/blog/implementazione-di-un-server-e-un-client-tcp-in-java/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/implementazione-di-un-server-e-un-client-tcp-in-java/</id>
    <published>2026-09-07T00:00:00.000Z</published>
    <updated>2026-09-07T00:00:00.000Z</updated>
    
    <summary>Guida al funzionamento del protocollo TCP e all&#39;uso dei socket in Java. Analizza il ciclo di vita delle connessioni e confronta le architetture client/server single-thread e multithread con esempi di codice pronti all&#39;uso.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;Oggi vediamo le fondamenta su cui si basa l&#39;Internet moderna. TCP è la base di quasi tutte le comunicazioni di rete affidabili: anche quando si lavora con framework ad alto livello come HTTP, WebSocket o gRPC, dietro le quinte c&#39;è sempre un socket TCP che gestisce la connessione. Capire come funziona dà un vantaggio enorme in termini di comprensione dei sistemi che usiamo ogni giorno.&lt;/p&gt;
&lt;p&gt;Per capire quanto sia fondamentale la comprensione del protocollo TCP e della sua implementazione, ecco qualche esempio di uso comune.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Web server e API REST&lt;/strong&gt;: HTTP è un protocollo costruito sopra TCP. Quando scrivi un server web, sotto il cofano c&#39;è un &lt;code&gt;ServerSocket&lt;/code&gt; che accetta connessioni e parla TCP. Capire come funziona può aiutarti nel debugging dei problemi di rete, dei timeout, del keep-alive e così via.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Database&lt;/strong&gt;: qualsiasi database comunica con i client tramite protocolli basati su TCP.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Applicazioni di messaggistica e chat&lt;/strong&gt;: WhatsApp, Slack e Telegram utilizzano connessioni TCP persistenti per inviare e ricevere messaggi in tempo reale.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Email&lt;/strong&gt;: SMTP, IMAP e POP3 sono protocolli TCP. I server di posta sono server TCP specializzati.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Ce ne sarebbero molti altri, ma ci fermiamo qui: nelle aziende, la conoscenza dei fondamenti è molto apprezzata anche nei colloqui tecnici.&lt;/p&gt;
&lt;h2 id=&quot;il-ciclo-di-vita-di-una-richiesta-http-su-tcp&quot;&gt;Il ciclo di vita di una richiesta HTTP su TCP&lt;/h2&gt;
&lt;p&gt;L&#39;architettura client-server del Web si basa su un modello asimmetrico richiesta-risposta governato dal protocollo HTTP. Il client avvia attivamente la comunicazione per ottenere una risorsa, mentre il server ascolta passivamente, elabora la richiesta, invia la risorsa e chiude la connessione TCP.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Client&lt;/strong&gt;: è il processo (browser, script cURL, applicazione mobile) che conosce l&#39;indirizzo della controparte. Apre un socket TCP verso l&#39;IP di destinazione e invia un messaggio di testo formattato (richiesta HTTP) indicando un metodo, ad esempio &lt;code&gt;GET /index.html&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Server&lt;/strong&gt;: è un processo in esecuzione su una macchina con un IP raggiungibile, di solito statico. Resta in stato di ascolto su una porta specifica (80 per HTTP, 443 per HTTPS) e non avvia mai la comunicazione verso il client.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Il ciclo di vita si compone essenzialmente di cinque fasi.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Risoluzione DNS&lt;/strong&gt;: il client converte il nome di dominio nell&#39;indirizzo IP del server.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Handshake TCP&lt;/strong&gt;: il client instaura una connessione a livello di trasporto (layer 4) tramite il meccanismo del three-way handshake TCP (SYN, SYN-ACK, ACK).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Richiesta&lt;/strong&gt;: il client trasmette l&#39;header HTTP, specificando la risorsa voluta e i parametri accessori.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Elaborazione e risposta&lt;/strong&gt;: il server processa la richiesta, recupera il file dal disco o lo genera dinamicamente tramite un backend, e risponde con un codice di stato seguito dal payload.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Teardown TCP&lt;/strong&gt;: terminata la trasmissione, il server avvia la chiusura formale della connessione inviando un pacchetto FIN.&lt;/li&gt;
&lt;/ol&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Three-way handshake.&lt;/strong&gt; Serve a stabilire una connessione affidabile e sincronizzata tra client e server prima dell&#39;inizio dello scambio dati, in tre fasi distinte: il client invia un pacchetto SYN includendo il proprio numero di sequenza iniziale, il server risponde con SYN-ACK riconoscendo la richiesta e inviando il proprio numero di sequenza, infine il client invia un pacchetto ACK per confermare la ricezione, dopodiché la connessione è stabilita.
&lt;/div&gt;
&lt;div class=&quot;alert alert-secondary&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Payload.&lt;/strong&gt; È il contenuto effettivo della richiesta, cioè i dati scambiati tra client e server, distinti dai metadati contenuti solo nell&#39;intestazione (header).
&lt;/div&gt;
&lt;p&gt;Per approfondire il meccanismo del protocollo TCP, il &lt;a href=&quot;https://www.cloudflare.com/learning/&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferer&quot;&gt;Cloudflare Learning Center&lt;/a&gt; offre una spiegazione tecnica accurata e aggiornata.&lt;/p&gt;
&lt;h2 id=&quot;cose-una-socket&quot;&gt;Cos&#39;è una socket&lt;/h2&gt;
&lt;p&gt;Prima di passare al codice, vale la pena spendere due parole sul concetto di socket: per molti può sembrare una banalità, ma è doveroso vista la centralità che ha nell&#39;implementazione che vedremo.&lt;/p&gt;
&lt;p&gt;Una socket non è altro che un&#39;interfaccia per un processo. Un processo classico vive sulla tua macchina, ma quando ha bisogno di comunicare con altre macchine in rete ha bisogno di un meccanismo, appunto le socket, che gli permetta di scambiare messaggi e dati.&lt;/p&gt;
&lt;p&gt;Per dirla in modo più preciso, una socket è il punto di accesso al canale di comunicazione tra processi in rete, e si identifica attraverso la combinazione di indirizzo IP e numero di porta.&lt;/p&gt;
&lt;h2 id=&quot;client-e-server-tcp-single-thread&quot;&gt;Client e server TCP single-thread&lt;/h2&gt;
&lt;p&gt;Iniziamo dalla versione più semplice: un server che accetta una sola connessione alla volta e un client che dialoga con lui riga per riga, implementando un classico servizio di echo (il server rispedisce al client lo stesso messaggio ricevuto, preceduto da &amp;quot;Echo:&amp;quot;).&lt;/p&gt;
&lt;h3 id=&quot;il-client-tcp-single-thread&quot;&gt;Il client TCP single-thread&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;package it.dev.tcp.v1.client;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.Socket;
public class ClientSinglethreadTCP {
    private static final int PORTA = 50000;
    private static final String INDIRIZZO_SERVER = &amp;quot;127.0.0.1&amp;quot;;
    public static void main(String[] args) {
        try {
            Socket connessione = new Socket(INDIRIZZO_SERVER, PORTA);
            BufferedReader console = new BufferedReader(new InputStreamReader(System.in));
            System.out.print(&amp;quot;Connesso al server. Scrivi un messaggio (&#39;exit&#39; per uscire): &amp;quot;);
            String messaggioDigitato = console.readLine();
            BufferedReader ingressoServer = new BufferedReader(new InputStreamReader(connessione.getInputStream()));
            PrintWriter uscitaServer = new PrintWriter(connessione.getOutputStream(), true);
            while (messaggioDigitato != null) {
                if (&amp;quot;exit&amp;quot;.equalsIgnoreCase(messaggioDigitato)) {
                    break;
                }
                uscitaServer.println(messaggioDigitato);
                String risposta = ingressoServer.readLine();
                if (risposta == null) {
                    System.out.println(&amp;quot;Il server ha chiuso la connessione&amp;quot;);
                    break;
                }
                System.out.println(&amp;quot;Risposta dal server: &amp;quot; + risposta);
                System.out.print(&amp;quot;&#92;nNuovo messaggio: &amp;quot;);
                messaggioDigitato = console.readLine();
            }
            console.close();
            uscitaServer.close();
            ingressoServer.close();
            connessione.close();
        } catch (IOException e) {
            System.out.println(&amp;quot;Errore: &amp;quot; + e.getMessage());
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Vediamo cosa succede riga per riga, perché ogni scelta ha un motivo preciso.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;new Socket(INDIRIZZO_SERVER, PORTA)&lt;/code&gt;&lt;/strong&gt;: è il punto di partenza di tutto. Creare un oggetto &lt;code&gt;Socket&lt;/code&gt; con indirizzo e porta esegue implicitamente il three-way handshake visto sopra: se il server non è in ascolto su quella porta, questa riga lancia un&#39;eccezione (catturata più sotto dal blocco &lt;code&gt;catch&lt;/code&gt;).&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;BufferedReader console&lt;/code&gt;&lt;/strong&gt;: legge l&#39;input digitato dall&#39;utente da tastiera. &lt;code&gt;System.in&lt;/code&gt; è lo stream grezzo, &lt;code&gt;InputStreamReader&lt;/code&gt; lo converte in caratteri leggibili, &lt;code&gt;BufferedReader&lt;/code&gt; aggiunge un buffer che permette di leggere comodamente riga per riga con &lt;code&gt;readLine()&lt;/code&gt;. Il risultato di ogni lettura viene salvato in &lt;code&gt;messaggioDigitato&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;ingressoServer&lt;/code&gt; e &lt;code&gt;uscitaServer&lt;/code&gt;&lt;/strong&gt;: sono gli stream associati alla socket &lt;code&gt;connessione&lt;/code&gt;, non alla console. &lt;code&gt;ingressoServer&lt;/code&gt; legge ciò che arriva dal server, &lt;code&gt;uscitaServer&lt;/code&gt; scrive ciò che va inviato al server. Nota il secondo parametro &lt;code&gt;true&lt;/code&gt; passato a &lt;code&gt;PrintWriter&lt;/code&gt;: attiva l&#39;auto-flush, cioè ogni &lt;code&gt;println()&lt;/code&gt; viene inviato immediatamente sulla rete invece di restare bloccato in un buffer interno in attesa di essere svuotato. Senza questo parametro il messaggio potrebbe non arrivare mai al server finché il buffer non si riempie.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Il ciclo &lt;code&gt;while (messaggioDigitato != null)&lt;/code&gt;&lt;/strong&gt;: è il cuore della logica. Finché l&#39;utente non digita &lt;code&gt;exit&lt;/code&gt; (controllato con &lt;code&gt;equalsIgnoreCase&lt;/code&gt; per non essere sensibili a maiuscole/minuscole) e finché il flusso di input non termina, il client invia &lt;code&gt;messaggioDigitato&lt;/code&gt; tramite &lt;code&gt;uscitaServer&lt;/code&gt;, attende la risposta con &lt;code&gt;ingressoServer.readLine()&lt;/code&gt; e la stampa a video.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;if (risposta == null)&lt;/code&gt;&lt;/strong&gt;: &lt;code&gt;readLine()&lt;/code&gt; restituisce &lt;code&gt;null&lt;/code&gt; quando il flusso in lettura è terminato, cioè quando la controparte ha chiuso la connessione. È il modo standard in Java per rilevare la disconnessione senza bisogno di eccezioni.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;I quattro &lt;code&gt;close()&lt;/code&gt; finali&lt;/strong&gt;: chiudono ordinatamente &lt;code&gt;console&lt;/code&gt;, &lt;code&gt;uscitaServer&lt;/code&gt;, &lt;code&gt;ingressoServer&lt;/code&gt; e &lt;code&gt;connessione&lt;/code&gt;. È buona pratica farlo sempre, anche se il programma sta per terminare, per evitare di lasciare file descriptor o socket aperti a livello di sistema operativo.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;il-server-tcp-single-thread&quot;&gt;Il server TCP single-thread&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;package it.dev.tcp.v1.server;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.ServerSocket;
import java.net.Socket;
public class ServerSingleThreadTCP {
    private static final int PORTA = 50000;
    public static void main(String[] args) {
        try {
            ServerSocket socketAscolto = new ServerSocket(PORTA);
            System.out.println(&amp;quot;Server in ascolto sulla porta &amp;quot; + PORTA);
            Socket connessioneClient = socketAscolto.accept();
            BufferedReader ingressoClient = new BufferedReader(new InputStreamReader(connessioneClient.getInputStream()));
            PrintWriter uscitaClient = new PrintWriter(connessioneClient.getOutputStream(), true);
            String messaggio = ingressoClient.readLine();
            while (messaggio != null) {
                System.out.println(&amp;quot;Messaggio ricevuto: &amp;quot; + messaggio);
                uscitaClient.println(&amp;quot;Echo: &amp;quot; + messaggio);
                messaggio = ingressoClient.readLine();
            }
            ingressoClient.close();
            uscitaClient.close();
            connessioneClient.close();
        } catch (IOException e) {
            System.out.println(&amp;quot;500 - Internal Server Error&amp;quot;);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Nota tecnica sul metodo &lt;code&gt;main&lt;/code&gt;.&lt;/strong&gt; Perché la JVM riconosca ed esegua automaticamente il metodo come punto di ingresso dell&#39;applicazione tramite il comando &lt;code&gt;java&lt;/code&gt;, deve essere dichiarato &lt;code&gt;public static void main(String[] args)&lt;/code&gt;. Se manca il modificatore &lt;code&gt;public&lt;/code&gt;, il metodo non è visibile all&#39;esterno del package e l&#39;esecuzione della classe come programma standalone fallisce.
&lt;/div&gt;
&lt;p&gt;Analizziamo la logica specifica del server, che differisce da quella del client in punti chiave.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;new ServerSocket(PORTA)&lt;/code&gt;&lt;/strong&gt;: a differenza del &lt;code&gt;Socket&lt;/code&gt; del client, un &lt;code&gt;ServerSocket&lt;/code&gt; non si connette a nessuno: apre semplicemente &lt;code&gt;socketAscolto&lt;/code&gt;, una porta in ascolto sulla macchina locale, pronta ad accettare richieste in entrata.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;socketAscolto.accept()&lt;/code&gt;&lt;/strong&gt;: questa è la riga più importante da capire. È una chiamata &lt;strong&gt;bloccante&lt;/strong&gt;: il thread principale del programma si ferma qui e non prosegue finché un client non si connette. Quando arriva una connessione, &lt;code&gt;accept()&lt;/code&gt; restituisce un nuovo oggetto &lt;code&gt;Socket&lt;/code&gt;, salvato in &lt;code&gt;connessioneClient&lt;/code&gt;, distinto da &lt;code&gt;socketAscolto&lt;/code&gt;, che rappresenta il canale di comunicazione dedicato a quel singolo client.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Perché questo server è &amp;quot;single-thread&amp;quot; nel senso limitativo del termine&lt;/strong&gt;: &lt;code&gt;accept()&lt;/code&gt; viene chiamato una sola volta, fuori da qualsiasi ciclo. Questo significa che il server gestisce un solo client per tutta la durata dell&#39;esecuzione: se un secondo client prova a connettersi mentre il primo è ancora attivo, la sua richiesta di connessione resta in coda a livello di sistema operativo (nel cosiddetto backlog) finché la connessione corrente non viene chiusa, dopodiché il programma termina comunque, perché il flusso di esecuzione esce dal blocco &lt;code&gt;try&lt;/code&gt; senza richiamare di nuovo &lt;code&gt;accept()&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Il ciclo &lt;code&gt;while (messaggio != null)&lt;/code&gt;&lt;/strong&gt;: è speculare a quello visto nel client. Ogni riga ricevuta tramite &lt;code&gt;ingressoClient&lt;/code&gt; viene stampata in console e rispedita al client tramite &lt;code&gt;uscitaClient&lt;/code&gt;, con il prefisso &lt;code&gt;&amp;quot;Echo: &amp;quot;&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Le due porzioni di codice viste finora illustrano l&#39;implementazione client/server TCP a singolo thread: il server non accetta connessioni multiple, e se proviamo a connetterci con un secondo client la connessione resta bloccata in coda finché il primo client non si disconnette.&lt;/p&gt;
&lt;h2 id=&quot;client-e-server-tcp-multithread&quot;&gt;Client e server TCP multithread&lt;/h2&gt;
&lt;p&gt;Passiamo ora all&#39;implementazione multithread, che risolve il limite principale della versione precedente.&lt;/p&gt;
&lt;h3 id=&quot;il-client-tcp-multithread&quot;&gt;Il client TCP multithread&lt;/h3&gt;
&lt;p&gt;Il client multithread è utile, ad esempio, nelle applicazioni di messaggistica, per inviare e ricevere messaggi con un thread di ricezione separato, in modo asincrono: l&#39;utente può scrivere un nuovo messaggio anche mentre è in attesa di una risposta dal server, senza che l&#39;interfaccia (in questo caso la console) resti bloccata.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;package it.dev.tcp.v1.client;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.Socket;
public class ClientMultithreadTCP {
    private final static Logger logger = LoggerFactory.getLogger(ClientMultithreadTCP.class);
    private final static String INDIRIZZO_SERVER = &amp;quot;127.0.0.1&amp;quot;;
    private final static int PORTA = 50000;
    public static void main(String[] args) {
        try {
            Socket connessione = new Socket(INDIRIZZO_SERVER, PORTA);
            PrintWriter uscitaServer = new PrintWriter(connessione.getOutputStream(), true);
            BufferedReader ingressoServer = new BufferedReader(new InputStreamReader(connessione.getInputStream()));
            BufferedReader console = new BufferedReader(new InputStreamReader(System.in));
            avviaThread(ingressoServer);
            System.out.println(&amp;quot;Connesso al server (&#39;exit&#39; per uscire)&amp;quot;);
            System.out.print(&amp;quot;Nuovo messaggio: &amp;quot;);
            String messaggioDigitato = console.readLine();
            while (messaggioDigitato != null) {
                if (&amp;quot;exit&amp;quot;.equalsIgnoreCase(messaggioDigitato)) {
                    break;
                }
                uscitaServer.println(messaggioDigitato);
                messaggioDigitato = console.readLine();
            }
            console.close();
            uscitaServer.close();
            ingressoServer.close();
            connessione.close();
        } catch (IOException e) {
            logger.error(&amp;quot;400 - Bad Request: {}&amp;quot;, e.getMessage());
        }
    }
    private static void avviaThread(BufferedReader ingressoServer) {
        Runnable compito = () -&amp;gt; gestisciServer(ingressoServer);
        Thread threadRicezione = new Thread(compito);
        threadRicezione.start();
    }
    private static void gestisciServer(BufferedReader ingressoServer) {
        try {
            String risposta = ingressoServer.readLine();
            while (risposta != null) {
                System.out.println(&amp;quot;&#92;nRisposta dal server: &amp;quot; + risposta);
                System.out.print(&amp;quot;Nuovo messaggio: &amp;quot;);
                risposta = ingressoServer.readLine();
            }
            System.out.println(&amp;quot;&#92;nIl server ha chiuso la connessione&amp;quot;);
        } catch (IOException e) {
            logger.error(&amp;quot;400 - Bad Request: {}&amp;quot;, e.getMessage());
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Le differenze chiave rispetto al client single-thread sono concentrate in due punti.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;avviaThread(ingressoServer)&lt;/code&gt;&lt;/strong&gt;: viene chiamato subito dopo l&#39;apertura della socket, prima ancora che l&#39;utente digiti qualcosa. Crea un &lt;code&gt;Runnable&lt;/code&gt; chiamato &lt;code&gt;compito&lt;/code&gt; che punta al metodo &lt;code&gt;gestisciServer&lt;/code&gt; e lo esegue su un &lt;code&gt;Thread&lt;/code&gt; dedicato (&lt;code&gt;threadRicezione&lt;/code&gt;), distinto dal thread principale (&lt;code&gt;main&lt;/code&gt;). Da questo momento in poi, il thread principale gestisce solo l&#39;input da console tramite &lt;code&gt;messaggioDigitato&lt;/code&gt;, mentre &lt;code&gt;threadRicezione&lt;/code&gt; gestisce solo la ricezione dal server: i due flussi lavorano in parallelo e in modo indipendente.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;gestisciServer(BufferedReader ingressoServer)&lt;/code&gt;&lt;/strong&gt;: è il metodo eseguito dal thread secondario. Contiene un ciclo che legge continuamente dal server con &lt;code&gt;ingressoServer.readLine()&lt;/code&gt; e stampa a schermo ogni risposta, finché la connessione non viene chiusa (&lt;code&gt;readLine()&lt;/code&gt; restituisce &lt;code&gt;null&lt;/code&gt;). Il vantaggio pratico è che, se il server impiega tempo a rispondere, il thread principale non resta bloccato in attesa: l&#39;utente può continuare a scrivere.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;logger&lt;/code&gt; al posto di &lt;code&gt;System.out&lt;/code&gt;&lt;/strong&gt;: qui viene introdotta la libreria SLF4J per il logging. A differenza di &lt;code&gt;System.out.println&lt;/code&gt;, un logger strutturato permette di distinguere il livello di gravità del messaggio (&lt;code&gt;info&lt;/code&gt;, &lt;code&gt;error&lt;/code&gt; e così via) e di configurare in modo centralizzato dove e come questi messaggi vengono scritti, un aspetto importante quando l&#39;applicazione cresce oltre il semplice esempio didattico.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;il-server-tcp-multithread&quot;&gt;Il server TCP multithread&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;package it.dev.tcp.v1.server;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.ServerSocket;
import java.net.Socket;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class ServerMultithreadTCP {
    private static final Logger logger = LoggerFactory.getLogger(ServerMultithreadTCP.class);
    private final static int PORTA = 50000;
    public static void main(String[] args) {
        try {
            ServerSocket socketAscolto = new ServerSocket(PORTA);
            logger.info(&amp;quot;Server in ascolto sulla porta {}&amp;quot;, PORTA);
            while (true) {
                Socket connessioneClient = socketAscolto.accept();
                logger.info(&amp;quot;Nuovo client connesso: {}&amp;quot;, connessioneClient.getRemoteSocketAddress());
                avviaThread(connessioneClient);
            }
        } catch (IOException e) {
            logger.error(&amp;quot;500 - Internal Server Error: {}&amp;quot;, e.getMessage());
        }
    }
    private static void avviaThread(Socket connessioneClient) {
        Runnable compito = () -&amp;gt; gestisciClient(connessioneClient);
        Thread thread = new Thread(compito);
        thread.start();
    }
    private static void gestisciClient(Socket connessioneClient) {
        String infoClient = connessioneClient.getRemoteSocketAddress().toString();
        try {
            BufferedReader ingressoClient = new BufferedReader(new InputStreamReader(connessioneClient.getInputStream()));
            String messaggio = ingressoClient.readLine();
            PrintWriter uscitaClient = new PrintWriter(connessioneClient.getOutputStream(), true);
            while (messaggio != null) {
                logger.info(&amp;quot;[{}] Messaggio ricevuto: {}&amp;quot;, infoClient, messaggio);
                uscitaClient.println(&amp;quot;Echo: &amp;quot; + messaggio);
                messaggio = ingressoClient.readLine();
            }
            ingressoClient.close();
            uscitaClient.close();
        } catch (IOException e) {
            logger.info(&amp;quot;Errore nella comunicazione con {}: {}&amp;quot;, infoClient, e.getMessage());
        } finally {
            logger.error(&amp;quot;Connessione terminata: {}&amp;quot;, infoClient);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questa è la classe più importante dell&#39;intero articolo, perché è quella che risolve concretamente il limite del server single-thread.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;while (true) { ... socketAscolto.accept(); ... }&lt;/code&gt;&lt;/strong&gt;: la differenza fondamentale rispetto alla versione precedente sta tutta qui. &lt;code&gt;accept()&lt;/code&gt; non viene più chiamato una sola volta, ma dentro un ciclo infinito: appena una connessione viene accettata e salvata in &lt;code&gt;connessioneClient&lt;/code&gt;, delegata a un thread separato, il ciclo torna immediatamente a chiamare &lt;code&gt;accept()&lt;/code&gt; e resta pronto ad accogliere il client successivo, senza dover aspettare che il precedente si disconnetta.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;avviaThread(connessioneClient)&lt;/code&gt;&lt;/strong&gt;: per ogni client accettato viene creato un nuovo &lt;code&gt;Thread&lt;/code&gt; dedicato che esegue &lt;code&gt;gestisciClient&lt;/code&gt;. Questo pattern si chiama comunemente &amp;quot;thread-per-connessione&amp;quot;: ogni connessione vive la sua vita indipendentemente dalle altre, su un thread proprio, permettendo al server di conversare in parallelo con più client contemporaneamente.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;&lt;code&gt;gestisciClient(Socket connessioneClient)&lt;/code&gt;&lt;/strong&gt;: contiene la stessa logica di echo vista nel server single-thread (leggi riga tramite &lt;code&gt;ingressoClient&lt;/code&gt;, rispondi con &amp;quot;Echo:&amp;quot; tramite &lt;code&gt;uscitaClient&lt;/code&gt;, ripeti), ma ora questa logica viene eseguita in isolamento su ogni thread, uno per client. &lt;code&gt;connessioneClient.getRemoteSocketAddress()&lt;/code&gt; viene salvato in &lt;code&gt;infoClient&lt;/code&gt; e usato nei log per identificare quale client ha inviato o ricevuto cosa, un dettaglio utile quando più client sono connessi simultaneamente.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Il blocco &lt;code&gt;finally&lt;/code&gt;&lt;/strong&gt;: garantisce che il messaggio di terminazione connessione venga loggato indipendentemente dal fatto che il ciclo sia terminato normalmente (client disconnesso senza errori) o a causa di un&#39;eccezione catturata nel &lt;code&gt;catch&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&quot;alert alert-secondary&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Suggerimento di buona pratica.&lt;/strong&gt; Nel blocco &lt;code&gt;catch&lt;/code&gt; di &lt;code&gt;gestisciClient&lt;/code&gt;, l&#39;errore di comunicazione viene registrato con &lt;code&gt;logger.info&lt;/code&gt;, mentre la normale terminazione della connessione nel blocco &lt;code&gt;finally&lt;/code&gt; usa &lt;code&gt;logger.error&lt;/code&gt;. I livelli sono invertiti rispetto alla convenzione: un errore dovrebbe essere loggato come &lt;code&gt;error&lt;/code&gt;, e una chiusura regolare della connessione come &lt;code&gt;info&lt;/code&gt;. Vale la pena rivedere questi due livelli prima di considerare il codice pronto per un ambiente diverso da quello didattico.
&lt;/div&gt;
&lt;h2 id=&quot;confronto-tra-le-due-architetture&quot;&gt;Confronto tra le due architetture&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Aspetto&lt;/th&gt;
        &lt;th&gt;Server single-thread&lt;/th&gt;
        &lt;th&gt;Server multithread&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Client gestiti in parallelo&lt;/td&gt;
        &lt;td&gt;Uno solo alla volta&lt;/td&gt;
        &lt;td&gt;Più client contemporaneamente&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Complessità del codice&lt;/td&gt;
        &lt;td&gt;Minima, adatta a scopi didattici&lt;/td&gt;
        &lt;td&gt;Maggiore, richiede gestione della concorrenza&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Scalabilità&lt;/td&gt;
        &lt;td&gt;Nulla oltre il singolo client&lt;/td&gt;
        &lt;td&gt;Buona, limitata dal numero di thread creabili dal sistema&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Caso d&#39;uso tipico&lt;/td&gt;
        &lt;td&gt;Test locali, apprendimento dei socket&lt;/td&gt;
        &lt;td&gt;Servizi reali con più utenti connessi (chat, giochi multiplayer, protocolli custom)&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;
&lt;h2 id=&quot;come-tcp-distingue-le-connessioni-sulla-stessa-porta&quot;&gt;Come TCP distingue le connessioni sulla stessa porta&lt;/h2&gt;
&lt;p&gt;Un dubbio comune, soprattutto per chi si avvicina per la prima volta a questi concetti, è capire come un server multithread possa ricevere più connessioni sulla stessa porta (&lt;code&gt;50000&lt;/code&gt; in questo esempio) senza che i messaggi dei diversi client si mescolino tra loro.&lt;/p&gt;
&lt;p&gt;La risposta sta nel modo in cui TCP identifica una connessione: non basta la sola porta di destinazione, ma serve una combinazione di quattro valori, spesso chiamata tupla a quattro elementi (4-tuple).&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;IP sorgente (il client)&lt;/li&gt;
&lt;li&gt;Porta sorgente (assegnata dinamicamente dal sistema operativo del client)&lt;/li&gt;
&lt;li&gt;IP destinazione (il server)&lt;/li&gt;
&lt;li&gt;Porta destinazione (&lt;code&gt;50000&lt;/code&gt;, la porta su cui &lt;code&gt;socketAscolto&lt;/code&gt; è in ascolto)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Ogni volta che un nuovo client si connette, il suo sistema operativo gli assegna automaticamente una porta sorgente diversa e temporanea. Il risultato è che, anche se tutti i client si collegano allo stesso IP e alla stessa porta del server, la combinazione dei quattro valori resta unica per ciascuna connessione. È proprio questa unicità che permette a &lt;code&gt;socketAscolto&lt;/code&gt; di restituire, ad ogni chiamata di &lt;code&gt;accept()&lt;/code&gt;, un &lt;code&gt;Socket&lt;/code&gt; distinto e indipendente per ciascun client, ognuno instradato correttamente dal sistema operativo verso il thread che lo sta gestendo.&lt;/p&gt;
&lt;h2 id=&quot;oltre-il-thread-per-connessione&quot;&gt;Oltre il thread-per-connessione&lt;/h2&gt;
&lt;p&gt;Il modello &amp;quot;un thread per client&amp;quot; utilizzato nel server multithread è un ottimo punto di partenza didattico, ma ha un limite pratico: ogni thread tradizionale in Java consuma memoria e risorse del sistema operativo, quindi con un numero molto alto di client connessi contemporaneamente le prestazioni possono degradare. Due direzioni comuni per evolvere questo modello in un contesto più vicino alla produzione sono:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;l&#39;uso di un pool di thread predefinito tramite &lt;code&gt;ExecutorService&lt;/code&gt;, che riutilizza un numero limitato di thread invece di crearne uno nuovo per ogni connessione;&lt;/li&gt;
&lt;li&gt;l&#39;adozione dei thread virtuali introdotti in Java (Da verificare: numero di versione preciso), pensati proprio per rendere sostenibile il modello thread-per-connessione anche con un numero molto elevato di connessioni simultanee, senza dover riscrivere la logica in stile asincrono/reattivo.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Un esempio illustrativo (non presente nella bozza originale, valore della dimensione del pool puramente esemplificativo) potrebbe essere:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;ExecutorService pool = Executors.newFixedThreadPool(10);
pool.submit(() -&amp;gt; gestisciClient(connessioneClient));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;come-testare-i-server&quot;&gt;Come testare i server&lt;/h2&gt;
&lt;p&gt;Prima di scrivere un client Java completo, è possibile verificare rapidamente il comportamento di entrambi i server usando uno strumento a riga di comando presente sulla maggior parte dei sistemi operativi (Da verificare: comando introdotto ex novo, non presente nella bozza originale).&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;telnet 127.0.0.1 50000
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In alternativa, dove &lt;code&gt;telnet&lt;/code&gt; non è disponibile, si può usare &lt;code&gt;nc&lt;/code&gt; (netcat) con la stessa sintassi di base (Da verificare: comando introdotto ex novo, non presente nella bozza originale).&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;nc 127.0.0.1 50000
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Con il server single-thread avviato, la prima connessione funzionerà normalmente; una seconda connessione aperta in un&#39;altra finestra di terminale resterà invece in attesa. Ripetendo lo stesso test con il server multithread avviato, entrambe le connessioni riceveranno risposta in modo indipendente: è il modo più immediato per toccare con mano la differenza tra le due implementazioni viste in questo articolo.&lt;/p&gt;


]]></content>
    
    
    
      
      
    <category term="java"/>
      
    
      
      
    <category term="server"/>
      
    
      
      
    <category term="client"/>
      
    
      
      
    <category term="TCP"/>
      
    
      
      
    <category term="IP"/>
      
    
      
      
    <category term="multithreading"/>
      
    
      
      
    <category term="reti"/>
      
    
  </entry>
  
  <entry>
    <title>Guida ai livelli RAID</title>
    <link href="https://dino-996.github.io/blog/redundant-array-of-independent-disks/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/redundant-array-of-independent-disks/</id>
    <published>2026-09-06T00:00:00.000Z</published>
    <updated>2026-09-06T00:00:00.000Z</updated>
    
    <summary>Il RAID combina più unità di archiviazione per garantire ridondanza e migliorare le prestazioni. Questa guida analizza il funzionamento dei livelli principali, i rischi durante la fase di rebuild, la distinzione tra RAID hardware e software e le pratiche di monitoraggio indispensabili in ambiente di produzione.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;raid-redundant-array-of-independent-disks&quot;&gt;RAID: Redundant Array of Independent Disks&lt;/h2&gt;
&lt;p&gt;Il RAID è una tecnologia che combina più unità di archiviazione fisiche, hard disk o SSD, in una o più unità logiche. È un concetto fondamentale per chiunque si occupi di infrastrutture, dai sistemisti Linux ai DevOps che gestiscono storage per database e macchine virtuali.&lt;/p&gt;
&lt;p&gt;I due obiettivi principali di un array RAID sono:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti (ridondanza)&lt;/strong&gt;: mantenere disponibili i dati quando si guasta uno o più dischi, a seconda del livello RAID scelto.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Prestazioni&lt;/strong&gt;: in alcune configurazioni, aumentare la velocità di lettura e scrittura distribuendo il carico su più dischi.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Di seguito una panoramica completa delle configurazioni principali, dei relativi pro e contro e delle differenze architetturali, con alcuni approfondimenti pratici pensati per chi deve scegliere o gestire un array in produzione.&lt;/p&gt;
&lt;h2 id=&quot;i-livelli-raid-principali&quot;&gt;I livelli RAID principali&lt;/h2&gt;
&lt;h3 id=&quot;prima-di-scegliere-un-raid&quot;&gt;Prima di scegliere un RAID&lt;/h3&gt;
&lt;p&gt;Quando si parla di RAID, è facile concentrarsi subito sul numero di dischi che possono rompersi prima di perdere i dati. Ed è comprensibile: leggere che un RAID 5 può perdere l&#39;intero array se si guasta un secondo disco può far sembrare questa configurazione una sorta di bomba a orologeria, soprattutto a chi si avvicina per la prima volta all&#39;argomento.&lt;/p&gt;
&lt;p&gt;Ma c&#39;è un errore di fondo in questo ragionamento: &lt;strong&gt;rompere un disco non è come tagliare un foglio di carta a metà&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;Un&#39;unità di archiviazione può presentare malfunzionamenti per molte ragioni, e non tutti i problemi si manifestano come una morte improvvisa e definitiva. Nel caso di un hard disk meccanico possono verificarsi guasti al motore, ai cuscinetti o al gruppo testine, danni alla superficie magnetica dei piatti, settori illeggibili, errori della scheda elettronica, oppure un degrado progressivo del supporto.&lt;/p&gt;
&lt;p&gt;Anche le condizioni esterne possono contribuire a un guasto: &lt;strong&gt;temperature elevate, alimentazione instabile, sbalzi o interruzioni di corrente, vibrazioni, urti e sollecitazioni meccaniche&lt;/strong&gt; riducono l&#39;affidabilità dell&#39;unità e dell&#39;intero sistema.&lt;/p&gt;
&lt;p&gt;Gli SSD non hanno parti meccaniche, ma non sono immuni dai guasti. Le celle di memoria flash hanno una resistenza limitata alle scritture, e controller, firmware, memoria NAND e componenti elettronici possono presentare difetti o malfunzionamenti. Anche in questo caso, un dispositivo può mostrare segnali di degrado, errori di lettura o scrittura, timeout, blocchi improvvisi, dati SMART anomali, prima di diventare completamente inutilizzabile.&lt;/p&gt;
&lt;p&gt;È importante anche ricordare che i guasti non sono sempre indipendenti. Dischi acquistati insieme, installati nello stesso chassis ed esposti alle stesse temperature, vibrazioni o problemi di alimentazione possono condividere gli stessi fattori di rischio. Per questo la progettazione dello storage non si riduce al solo livello RAID: contano anche monitoraggio, qualità dell&#39;alimentazione, raffreddamento, dischi di ricambio, procedure di sostituzione e backup.&lt;/p&gt;
&lt;p&gt;E qui arriviamo a un punto fondamentale: &lt;strong&gt;un disco guasto non fa necessariamente sparire istantaneamente l&#39;array&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;In un RAID 5, per esempio, la perdita del primo disco viene gestita proprio dalla ridondanza dell&#39;array. I dati continuano a essere accessibili, anche se l&#39;array opera in modalità degradata e può essere più lento; le informazioni mancanti vengono ricostruite dai dati presenti sugli altri dischi e dalle informazioni di parità.&lt;/p&gt;
&lt;p&gt;Il vero problema nasce durante il &lt;em&gt;rebuild&lt;/em&gt;, cioè nella fase in cui l&#39;array ricostruisce sul disco sostitutivo i dati che erano contenuti nell&#39;unità guasta. È questa la finestra di vulnerabilità che rende importante valutare RAID 6, soprattutto con dischi molto capienti, array numerosi o carichi di lavoro che non possono essere sospesi.&lt;/p&gt;
&lt;p&gt;Dire che durante questa fase esiste il rischio di un secondo guasto, però, &lt;strong&gt;non significa che un secondo disco sia destinato a rompersi&lt;/strong&gt;. Significa che, dopo il primo guasto, l&#39;array ha esaurito la propria ridondanza residua: un ulteriore guasto o un errore di lettura non recuperabile può avere conseguenze molto più gravi.&lt;/p&gt;
&lt;p&gt;Per questo non ha senso stabilire a priori che &lt;strong&gt;RAID 6 sia sempre migliore di RAID 5&lt;/strong&gt;. RAID 6 offre maggiore tolleranza ai guasti, ma richiede l&#39;equivalente della capacità di due dischi per la parità e comporta un costo maggiore nelle scritture. La scelta dipende dal contesto: numero e capacità dei dischi, tipo di carico, prestazioni richieste, valore dei dati, tempi di ripristino accettabili e livello di rischio che si è disposti ad accettare.&lt;/p&gt;
&lt;p&gt;C&#39;è infine una distinzione da tenere sempre presente: &lt;strong&gt;RAID non significa backup&lt;/strong&gt;. La ridondanza protegge principalmente dal guasto fisico di una o più unità, ma non protegge da cancellazioni accidentali, corruzione dei dati, ransomware, errori amministrativi o altri eventi che possono propagarsi all&#39;intero array.&lt;/p&gt;
&lt;p&gt;Con queste premesse, vediamo come i diversi livelli RAID distribuiscono e proteggono i dati.&lt;/p&gt;
&lt;h3 id=&quot;raid-0-striping&quot;&gt;Raid 0 (striping)&lt;/h3&gt;
&lt;p&gt;Nel RAID 0 i dati vengono spezzati in blocchi e scritti in parallelo su tutti i dischi dell&#39;array, con un minimo di 2 dischi richiesti.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Efficienza spazio&lt;/strong&gt;: 100% della capacità complessiva dei dischi.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti&lt;/strong&gt;: nulla. Se anche un solo disco si guasta, l&#39;intero volume diventa inutilizzabile e si perdono tutti i dati non disponibili altrove.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: prestazioni di lettura e scrittura molto elevate, in particolare nei carichi sequenziali.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: rischio altissimo di perdita dati, perché ogni disco aggiunto aumenta il numero di possibili punti di guasto.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Uso tipico&lt;/strong&gt;: file temporanei, cache, dati rigenerabili, elaborazioni non critiche ed editing video con materiale già protetto da backup.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;raid-1-mirroring&quot;&gt;Raid 1 (mirroring)&lt;/h3&gt;
&lt;p&gt;Nel RAID 1 i dati vengono duplicati identicamente su due o più dischi, con un minimo di 2 dischi richiesti.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Efficienza spazio&lt;/strong&gt;: con due dischi è del 50%; in generale, se tutte le copie fanno parte dello stesso mirror, lo spazio utile corrisponde alla capacità di un solo disco.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti&lt;/strong&gt;: in un mirror composto da N dischi può resistere alla rottura di N-1 dischi, purché rimanga almeno una copia integra.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: ottima protezione dal guasto di un disco e buone prestazioni in lettura, perché il sistema può leggere da più copie.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: costoso in termini di spazio utile; ogni scrittura deve essere completata su tutte le copie del mirror.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Uso tipico&lt;/strong&gt;: sistemi operativi, piccoli server critici, volumi applicativi e carichi in cui semplicità e tempi di rebuild rapidi sono prioritari.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;raid-5-striping-con-parita-distribuita&quot;&gt;Raid 5 (striping con parità distribuita)&lt;/h3&gt;
&lt;p&gt;Nel RAID 5 i dati e le informazioni di parità, calcolate tramite l&#39;operazione logica XOR, vengono distribuiti su tutti i dischi dell&#39;array, con un minimo di 3 dischi richiesti.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Efficienza spazio&lt;/strong&gt;: &lt;code&gt;(N - 1) × capacità del disco più piccolo&lt;/code&gt;. Con 4 dischi da 1 TB, lo spazio utile è 3 TB, mentre l&#39;equivalente di 1 TB è destinato alla parità.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti&lt;/strong&gt;: resiste al guasto di un solo disco. Dopo il guasto, i dati restano accessibili in modalità degradata e possono essere ricostruiti utilizzando parità e dischi superstiti.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: buon compromesso tra costo, spazio disponibile e prestazioni di lettura.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: le scritture, soprattutto quelle piccole e casuali, risentono del calcolo e dell&#39;aggiornamento della parità, fenomeno noto come &lt;strong&gt;write penalty&lt;/strong&gt;. Durante il rebuild le prestazioni possono peggiorare sensibilmente e l&#39;array non dispone più di ridondanza: se si guasta un secondo disco prima del completamento della ricostruzione, l&#39;array non è recuperabile con le sole informazioni RAID.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Uso tipico&lt;/strong&gt;: file server, NAS aziendali, archivi con prevalenza di letture e carichi di lavoro che accettano il compromesso fra capacità, costo e ridondanza.&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Attenzione al rebuild su dischi di grande capacità.&lt;/strong&gt; Con dischi meccanici di taglia molto elevata, il tempo necessario al rebuild di un RAID 5 può estendersi per molte ore, o anche più a lungo in presenza di carico. Durante questa fase l&#39;array non ha alcuna ridondanza residua. Per questo motivo, su array con dischi di grandi dimensioni si preferisce spesso il RAID 6.
&lt;/div&gt;
&lt;h3 id=&quot;raid-6-striping-con-doppia-parita-distribuita&quot;&gt;Raid 6 (striping con doppia parità distribuita)&lt;/h3&gt;
&lt;p&gt;Il RAID 6 funziona in modo simile al RAID 5, ma utilizza due informazioni di parità indipendenti, distribuite sui dischi dell&#39;array. Richiede un minimo di 4 dischi.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Efficienza spazio&lt;/strong&gt;: &lt;code&gt;(N - 2) × capacità del disco più piccolo&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti&lt;/strong&gt;: resiste al guasto di due dischi, anche se avvengono in momenti diversi prima che il primo rebuild sia completato.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: offre maggiore protezione negli array con molti dischi o unità di grande capacità, dove i tempi di rebuild sono più lunghi e la finestra di vulnerabilità di RAID 5 può essere rilevante.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: le scritture sono generalmente più onerose rispetto al RAID 5, perché devono essere gestite due informazioni di parità.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Uso tipico&lt;/strong&gt;: storage ad alta capacità, NAS e file server con dischi SATA o SAS molto capienti, soprattutto quando la disponibilità dei dati è più importante delle prestazioni di scrittura assolute.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;raid-10-o-raid-10-stripe-di-mirror&quot;&gt;Raid 10 (o raid 1+0, stripe di mirror)&lt;/h3&gt;
&lt;p&gt;Il RAID 10 è una configurazione nidificata: crea più coppie in RAID 1, cioè mirror, e le unisce successivamente in RAID 0, cioè in striping. Richiede un minimo di 4 dischi e un numero pari di unità.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Efficienza spazio&lt;/strong&gt;: 50%.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tolleranza ai guasti&lt;/strong&gt;: può resistere alla rottura di più dischi, purché non si guastino entrambi i dischi della stessa coppia mirror.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: prestazioni molto elevate sia in lettura sia in scrittura e rebuild spesso più rapido, perché la ricostruzione consiste principalmente nel copiare i dati dalla copia mirror integra, senza ricalcolare la parità.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: è costoso in termini di spazio disponibile, poiché metà della capacità totale è destinata alla replica.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Uso tipico&lt;/strong&gt;: database transazionali, macchine virtuali ad alte prestazioni, sistemi soggetti a molte operazioni di scrittura casuale.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;confronto-rapido-tra-i-livelli&quot;&gt;Confronto rapido tra i livelli&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Livello&lt;/th&gt;
        &lt;th&gt;Dischi minimi&lt;/th&gt;
        &lt;th&gt;Spazio utile&lt;/th&gt;
        &lt;th&gt;Tolleranza guasti&lt;/th&gt;
        &lt;th&gt;Caso d&#39;uso tipico&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;RAID 0&lt;/td&gt;
        &lt;td&gt;2&lt;/td&gt;
        &lt;td&gt;100%&lt;/td&gt;
        &lt;td&gt;Nessuna&lt;/td&gt;
        &lt;td&gt;Cache, dati non critici&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;RAID 1&lt;/td&gt;
        &lt;td&gt;2&lt;/td&gt;
        &lt;td&gt;50% con due dischi&lt;/td&gt;
        &lt;td&gt;N-1 dischi&lt;/td&gt;
        &lt;td&gt;Sistemi operativi, piccoli server critici&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;RAID 5&lt;/td&gt;
        &lt;td&gt;3&lt;/td&gt;
        &lt;td&gt;(N-1)/N&lt;/td&gt;
        &lt;td&gt;1 disco&lt;/td&gt;
        &lt;td&gt;File server, NAS&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;RAID 6&lt;/td&gt;
        &lt;td&gt;4&lt;/td&gt;
        &lt;td&gt;(N-2)/N&lt;/td&gt;
        &lt;td&gt;2 dischi&lt;/td&gt;
        &lt;td&gt;Storage ad alta capacità&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;RAID 10&lt;/td&gt;
        &lt;td&gt;4 (pari)&lt;/td&gt;
        &lt;td&gt;50%&lt;/td&gt;
        &lt;td&gt;Più dischi, se non nella stessa coppia mirror&lt;/td&gt;
        &lt;td&gt;Database, VM ad alte prestazioni&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;
&lt;h2 id=&quot;livelli-nested-e-legacy&quot;&gt;Livelli nested e legacy&lt;/h2&gt;
&lt;p&gt;Oltre ai livelli principali esistono configurazioni combinate o storiche, meno comuni ma ancora rilevanti in alcuni contesti enterprise.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;RAID 50 e RAID 60&lt;/strong&gt;: combinano uno stripe RAID 0 con più gruppi rispettivamente RAID 5 o RAID 6. RAID 50 richiede normalmente almeno due gruppi RAID 5, quindi almeno 6 dischi; RAID 60 richiede almeno due gruppi RAID 6, quindi almeno 8 dischi. Sono usati in grandi sistemi di storage per ottenere prestazioni, capacità e tolleranza ai guasti superiori a quelle di un singolo gruppo RAID 5 o RAID 6.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;JBOD (Just a Bunch Of Disks)&lt;/strong&gt;: non è un vero livello RAID. Il termine può indicare semplicemente dischi esposti singolarmente al sistema operativo oppure un volume lineare che concatena più dischi. Non offre ridondanza né un vantaggio prestazionale intrinseco. In un volume concatenato, il guasto di un disco può rendere inaccessibile una parte dei dati o compromettere l&#39;intero volume, a seconda dell&#39;implementazione e del filesystem.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RAID 2, 3 e 4&lt;/strong&gt;: livelli storici ormai rari. RAID 3 e RAID 4 usavano dischi dedicati alla parità, creando potenziali colli di bottiglia; RAID 2 utilizzava codici di correzione degli errori e richiedeva una configurazione complessa, oggi superata dalle capacità di correzione degli errori integrate nelle unità moderne.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;hardware-software-e-fake-raid&quot;&gt;Hardware, software e fake RAID&lt;/h2&gt;
&lt;h3 id=&quot;raid-hardware-dedicato&quot;&gt;Raid hardware dedicato&lt;/h3&gt;
&lt;p&gt;Il RAID hardware utilizza un controller fisico, spesso installato su PCIe, dotato di processore, firmware e memoria cache propri. Nei modelli professionali, la cache può essere protetta da batteria, supercondensatore o memoria flash per ridurre il rischio di perdita dei dati in caso di blackout. Il sistema operativo vede normalmente uno o più volumi logici anziché i singoli dischi dell&#39;array.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: gestione centralizzata dell&#39;array, cache dedicata, funzioni avanzate di monitoraggio e recovery; il controller può gestire in proprio alcune operazioni di parità e scrittura.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: costo più elevato e possibile dipendenza dal controller. In caso di guasto, il recupero è generalmente più semplice con un modello compatibile, soprattutto della stessa famiglia; compatibilità e metadati devono essere verificati prima di qualsiasi sostituzione.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;raid-software&quot;&gt;Raid software&lt;/h3&gt;
&lt;p&gt;Il RAID software è gestito dal sistema operativo, ad esempio tramite &lt;code&gt;mdadm&lt;/code&gt; su Linux o Storage Spaces su Windows.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Pro&lt;/strong&gt;: flessibile, spesso gratuito e meno vincolato a uno specifico controller. In molti casi i dischi possono essere spostati su un altro sistema compatibile e l&#39;array può essere riassemblato.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Contro&lt;/strong&gt;: utilizza risorse del sistema host e richiede una corretta progettazione di alimentazione, cache e protezione dalle interruzioni di corrente. Nei RAID con parità, una perdita di alimentazione durante una scrittura può causare incoerenze note come &lt;em&gt;write hole&lt;/em&gt;, se la soluzione adottata non dispone di adeguati meccanismi di protezione o journaling.&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt;
  Su Linux, la creazione di un array software si effettua tipicamente tramite lo strumento &lt;code&gt;mdadm&lt;/code&gt;. La sintassi esatta del comando, i parametri e le opzioni dipendono dalla distribuzione e dalla versione dello strumento installata: verificane i dettagli nella documentazione ufficiale prima di applicarla in produzione.
&lt;/div&gt;
&lt;h3 id=&quot;fake-raid-hostraid&quot;&gt;Fake raid (hostraid)&lt;/h3&gt;
&lt;p&gt;Il cosiddetto &lt;em&gt;fake RAID&lt;/em&gt;, spesso chiamato anche firmware RAID o HostRAID, è una funzionalità integrata in molte schede madri consumer. Può apparire come una soluzione hardware, ma gran parte del lavoro viene eseguito dal driver e dalla CPU del sistema operativo.&lt;/p&gt;
&lt;p&gt;È spesso sconsigliato in ambito professionale perché combina alcuni limiti del RAID software, come la dipendenza dalle risorse dell&#39;host, con una maggiore dipendenza da firmware, driver e compatibilità della scheda madre. Prima di affidargli dati importanti è opportuno verificare attentamente procedure di recupero, documentazione e compatibilità con il sistema operativo utilizzato.&lt;/p&gt;
&lt;h2 id=&quot;monitoraggio-e-manutenzione-dellarray&quot;&gt;Monitoraggio e manutenzione dell&#39;array&lt;/h2&gt;
&lt;p&gt;Scegliere il livello RAID giusto è solo metà del lavoro: senza monitoraggio, un array può restare silenziosamente degradato per settimane prima che qualcuno se ne accorga, azzerando di fatto il margine di sicurezza che il RAID dovrebbe garantire.&lt;/p&gt;
&lt;p&gt;Alcune pratiche operative di base, indipendenti dal livello scelto, aiutano a ridurre questo rischio:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Monitoraggio S.M.A.R.T.&lt;/strong&gt;: quasi tutti i dischi meccanici e gli SSD moderni espongono attributi S.M.A.R.T. che segnalano un degrado progressivo, come settori riallocati o errori di lettura, ben prima di un guasto completo. Integrare un controllo periodico di questi attributi permette di sostituire un disco prima che diventi un problema per l&#39;intero array.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scrub periodico&lt;/strong&gt;: molte implementazioni RAID software e i controller hardware più recenti supportano una verifica periodica della coerenza dei dati e della parità, spesso chiamata &lt;em&gt;scrub&lt;/em&gt; o &lt;em&gt;patrol read&lt;/em&gt;. Eseguirla con regolarità aiuta a individuare errori latenti su blocchi raramente letti, prima che si sommino a un guasto reale durante un rebuild.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dischi di scorta (hot spare)&lt;/strong&gt;: avere uno o più dischi di ricambio già inseriti nel sistema e pronti a entrare automaticamente in servizio riduce drasticamente il tempo tra il guasto e l&#39;inizio del rebuild, restringendo la finestra di vulnerabilità descritta in precedenza.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Notifiche automatiche&lt;/strong&gt;: un array degradato che nessuno nota non offre alcuna protezione pratica. Configurare l&#39;invio di notifiche, via email o verso un sistema di monitoraggio centralizzato, è essenziale per intervenire tempestivamente.&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Esempio indicativo (da verificare).&lt;/strong&gt; Su un sistema Linux con RAID software, lo stato di un array può essere controllato con un comando del tipo riportato di seguito. Sintassi, output e nome del demone di notifica variano a seconda della distribuzione e della versione di &lt;code&gt;mdadm&lt;/code&gt; installata: verificane i dettagli nella documentazione ufficiale prima di utilizzarlo in produzione.
&lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;mdadm --detail /dev/md0
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;unalternativa-moderna&quot;&gt;Un&#39;alternativa moderna&lt;/h2&gt;
&lt;p&gt;Accanto al RAID tradizionale si sono diffusi filesystem che integrano ridondanza e controllo dell&#39;integrità dei dati a un livello più profondo, come ZFS con i profili RAID-Z e Btrfs con i suoi profili RAID nativi.&lt;/p&gt;
&lt;p&gt;Questi filesystem calcolano checksum sui dati e, quando è presente una copia ridondante valida, possono rilevare e talvolta correggere la corruzione silenziosa, il cosiddetto &lt;em&gt;bit rot&lt;/em&gt;. Il RAID tradizionale, da solo, non dispone normalmente di informazioni sufficienti per stabilire quale copia di un blocco silenziosamente corrotto sia quella corretta.&lt;/p&gt;
&lt;p&gt;Questa caratteristica è particolarmente utile nei NAS e negli archivi a lungo termine. Anche qui, però, valgono le regole fondamentali: la ridondanza deve essere configurata correttamente, lo stato dei dischi va monitorato e i backup restano indispensabili.&lt;/p&gt;
&lt;h2 id=&quot;la-regola-doro-della-sicurezza-informatica&quot;&gt;La regola d&#39;oro della sicurezza informatica&lt;/h2&gt;
&lt;div class=&quot;alert alert-danger&quot; role=&quot;alert&quot;&gt;
  &lt;strong&gt;Il RAID non è un backup.&lt;/strong&gt; Protegge dalla perdita di disponibilità causata dal guasto di uno o più dischi, secondo il livello RAID scelto; non protegge dalla perdita logica dei dati.
&lt;/div&gt;
&lt;p&gt;Se un file viene cancellato accidentalmente, se un malware cripta i dati tramite un attacco ransomware, se un bug corrompe il filesystem oppure se un amministratore esegue un&#39;operazione errata, il RAID può replicare o distribuire quell&#39;operazione su tutto l&#39;array. Il RAID opera infatti a livello di blocchi e non comprende il significato logico dei dati che sta scrivendo.&lt;/p&gt;
&lt;p&gt;Il backup, al contrario, protegge dalla perdita logica dei dati perché conserva copie separate dal sistema primario, idealmente con versioning, controllo periodico del ripristino e almeno una copia isolata o fuori sede.&lt;/p&gt;
&lt;p&gt;Una buona strategia segue il principio del &lt;strong&gt;backup 3-2-1&lt;/strong&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;almeno tre copie dei dati;&lt;/li&gt;
&lt;li&gt;su almeno due supporti differenti;&lt;/li&gt;
&lt;li&gt;con almeno una copia conservata fuori sede o comunque isolata dal sistema principale.&lt;/li&gt;
&lt;/ul&gt;


]]></content>
    
    
    
      
      
    <category term="RAID"/>
      
    
      
      
    <category term="sysadmin"/>
      
    
      
      
    <category term="storage"/>
      
    
      
      
    <category term="linux"/>
      
    
      
      
    <category term="hardware"/>
      
    
  </entry>
  
  <entry>
    <title>Guida ai comandi Linux per la diagnostica di sistema, rete e hardware</title>
    <link href="https://dino-996.github.io/blog/guida-ai-comandi-linux-per-la-diagnostica-di-sistema-rete-e-hardware/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/guida-ai-comandi-linux-per-la-diagnostica-di-sistema-rete-e-hardware/</id>
    <published>2026-09-05T00:00:00.000Z</published>
    <updated>2026-09-05T00:00:00.000Z</updated>
    
    <summary>Una panoramica pratica dei principali comandi da terminale per il debug e il monitoraggio dei sistemi Linux e macOS: dall&#39;analisi dello spazio su disco al controllo di memoria, risorse e traffico di rete.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;Questa guida raccoglie i comandi da riga di comando più utili per la quasi totalità delle esigenze diagnostiche quotidiane su un sistema Linux, dal debug di un server in produzione alla semplice curiosità sulla propria macchina di sviluppo, organizzati per area tematica, con una breve spiegazione del funzionamento di ciascuno e, dove utile, dell&#39;alternativa moderna a strumenti ormai superati.&lt;/p&gt;
&lt;h3 id=&quot;compatibilita-con-macos&quot;&gt;Compatibilità con macOS&lt;/h3&gt;
&lt;p&gt;Buona parte di questa guida è pensata per Linux, ma vale la pena ricordare che macOS è a sua volta un sistema basato su UNIX (nello specifico deriva da BSD), per cui diversi comandi elencati funzionano invariati anche su un Mac aperto dal Terminale. Rientrano in questo gruppo &lt;code&gt;df -h&lt;/code&gt;, &lt;code&gt;du -sh&lt;/code&gt;, &lt;code&gt;uname -a&lt;/code&gt;, &lt;code&gt;uptime&lt;/code&gt;, &lt;code&gt;ping -c 4&lt;/code&gt;, &lt;code&gt;traceroute&lt;/code&gt;, &lt;code&gt;dig&lt;/code&gt; e &lt;code&gt;curl&lt;/code&gt;, oltre a strumenti installabili via Homebrew come &lt;code&gt;ncdu&lt;/code&gt;, &lt;code&gt;htop&lt;/code&gt;, &lt;code&gt;btop&lt;/code&gt; e &lt;code&gt;fastfetch&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Altri comandi della guida sono invece specifici del mondo Linux e non hanno un corrispettivo diretto su macOS, perché si appoggiano a sottosistemi che semplicemente non esistono fuori dal kernel Linux, come il filesystem virtuale &lt;code&gt;/proc&lt;/code&gt; (da cui dipendono &lt;code&gt;/proc/meminfo&lt;/code&gt;, &lt;code&gt;/proc/cpuinfo&lt;/code&gt;), lo stack &lt;code&gt;iproute2&lt;/code&gt; (&lt;code&gt;ip a&lt;/code&gt;, &lt;code&gt;ip r&lt;/code&gt;, &lt;code&gt;ss&lt;/code&gt;), systemd (&lt;code&gt;hostnamectl&lt;/code&gt;) o il pacchetto &lt;code&gt;lm-sensors&lt;/code&gt; (&lt;code&gt;sensors&lt;/code&gt;). Anche &lt;code&gt;lsblk&lt;/code&gt;, &lt;code&gt;lscpu&lt;/code&gt; e &lt;code&gt;lshw&lt;/code&gt; sono strumenti legati al mondo Linux, così come &lt;code&gt;lspci&lt;/code&gt;, non presente nativamente su macOS.&lt;/p&gt;
&lt;h2 id=&quot;disco-e-filesystem-spazio-e-partizioni&quot;&gt;Disco e filesystem, spazio e partizioni&lt;/h2&gt;
&lt;p&gt;Per avere una panoramica immediata dello spazio libero e occupato su tutti i filesystem montati si usa il comando seguente, dove il flag &lt;code&gt;-h&lt;/code&gt; (human-readable) converte i valori grezzi in blocchi in un formato leggibile espresso in GB o MB.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;df -h
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Quando invece serve capire quanto spazio occupa nello specifico una cartella e le sue sottocartelle, il comando di riferimento è &lt;code&gt;du&lt;/code&gt;, dove &lt;code&gt;-s&lt;/code&gt; produce un riassunto totale invece del dettaglio ricorsivo file per file e &lt;code&gt;-h&lt;/code&gt; rende l&#39;output leggibile.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;du -sh /percorso/della/cartella
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per elencare tutti i dispositivi di archiviazione collegati (dischi, partizioni, chiavette USB) e i rispettivi punti di montaggio si utilizza &lt;code&gt;lsblk&lt;/code&gt;, che con il flag &lt;code&gt;-f&lt;/code&gt; mostra anche il tipo di filesystem e l&#39;UUID di ciascuna partizione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;lsblk -f
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per la tabella delle partizioni completa di tutti i dischi, comprensiva di schema (MBR o GPT) e dimensioni esatte dei settori, serve invece &lt;code&gt;fdisk&lt;/code&gt; in modalità di sola lettura elenco, che richiede i privilegi di amministratore.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;sudo fdisk -l
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Infine, per chi deve esplorare interattivamente cosa occupa spazio su disco senza scorrere output statici, &lt;code&gt;ncdu&lt;/code&gt; è uno strumento a interfaccia testuale molto pratico: se non è già presente si installa con il gestore pacchetti della distribuzione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;sudo apt install ncdu
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;memoria-ram-e-swap&quot;&gt;Memoria RAM e swap&lt;/h2&gt;
&lt;p&gt;Lo strumento più immediato per controllare memoria RAM e swap libere, utilizzate e totali è &lt;code&gt;free&lt;/code&gt;, dove &lt;code&gt;-h&lt;/code&gt; formatta automaticamente i valori in KB, MB o GB a seconda della loro grandezza.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;free -h
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Se serve un monitoraggio continuo, &lt;code&gt;watch&lt;/code&gt; permette di rieseguire un comando a intervalli regolari, in questo caso ogni secondo, così da osservare in tempo reale l&#39;andamento della memoria.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;watch -n 1 free -h
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per chi ha bisogno del dettaglio più granulare possibile, direttamente dai contatori del kernel, la fonte primaria è il file virtuale seguente.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;cat /proc/meminfo
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;cpu-e-processore&quot;&gt;CPU e processore&lt;/h2&gt;
&lt;p&gt;Per visualizzare architettura della CPU, modello, numero di core fisici, thread logici e livelli di cache, il comando più sintetico è &lt;code&gt;lscpu&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;lscpu
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Chi ha bisogno delle informazioni dettagliate per ogni singolo core, comprese le frequenze correnti, può leggerle direttamente dal kernel.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;cat /proc/cpuinfo
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per il monitoraggio in tempo reale di processi e utilizzo di CPU e RAM, il comando storico è &lt;code&gt;top&lt;/code&gt;; sui sistemi moderni conviene però preferire alternative più leggibili e interattive come &lt;code&gt;htop&lt;/code&gt; o &lt;code&gt;btop&lt;/code&gt;, se disponibili.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;top
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;scheda-video-gpu-e-hardware-generale&quot;&gt;Scheda video (GPU) e hardware generale&lt;/h2&gt;
&lt;p&gt;Per identificare il modello esatto della scheda video installata (NVIDIA, AMD o Intel) si interroga il bus PCI filtrando per la classe video.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;lspci | grep -i vga
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Su sistemi con GPU dedicata secondaria, in configurazioni ibride o server con acceleratori grafici, la classe da cercare può invece essere quella 3D.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;lspci | grep -i 3d
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per informazioni più dettagliate sul controller grafico e sul driver effettivamente in uso, &lt;code&gt;lshw&lt;/code&gt; filtrato sulla classe display offre un output più ricco, sempre con privilegi elevati.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;sudo lshw -C display
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Chi lavora con schede NVIDIA dispone di uno strumento dedicato che mostra temperatura, memoria video occupata e processi attivi sulla GPU in tempo reale.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;nvidia-smi
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per un inventario hardware completo dell&#39;intera macchina, in forma sintetica, si usa &lt;code&gt;lshw&lt;/code&gt; con il flag di riassunto.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;sudo lshw -short
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;informazioni-di-sistema-kernel-e-dispositivo&quot;&gt;Informazioni di sistema, kernel e dispositivo&lt;/h2&gt;
&lt;p&gt;Per un quadro d&#39;insieme che comprende nome host, versione del kernel, distribuzione e architettura, &lt;code&gt;hostnamectl&lt;/code&gt; è oggi lo strumento di riferimento sulle distribuzioni basate su systemd.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;hostnamectl
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Se serve solo la versione esatta del kernel Linux in esecuzione, il comando dedicato è più mirato.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;uname -r
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per tutte le informazioni di sistema di base condensate in un&#39;unica riga, si aggiunge il flag &lt;code&gt;-a&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;uname -a
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I dettagli sulla distribuzione, nome, versione e identificativo, si leggono da un file standard presente su praticamente tutte le distribuzioni moderne.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;cat /etc/os-release
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per sapere da quanto tempo il sistema è acceso, quanti utenti sono collegati e qual è il carico medio (load average) su 1, 5 e 15 minuti, si usa &lt;code&gt;uptime&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;uptime
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per chi desidera anche un colpo d&#39;occhio estetico, con il logo della distribuzione in ASCII art accanto ai dati hardware, &lt;code&gt;neofetch&lt;/code&gt; è stato per anni lo strumento più diffuso. Il progetto originale non riceve più sviluppo attivo, per cui oggi conviene orientarsi su &lt;code&gt;fastfetch&lt;/code&gt;, il fork mantenuto attivamente e generalmente più veloce.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;fastfetch
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;rete-interfacce-e-connessioni&quot;&gt;Rete, interfacce e connessioni&lt;/h2&gt;
&lt;p&gt;Per elencare tutte le interfacce di rete, fisiche e virtuali, con i relativi indirizzi MAC e IP, lo strumento moderno è &lt;code&gt;ip&lt;/code&gt;, che ha sostituito il deprecato &lt;code&gt;ifconfig&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;ip a
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per la tabella di routing, utile a capire quale sia il gateway predefinito della macchina, si usa il sottocomando dedicato.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;ip r
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per verificare quali porte sono in ascolto e a quale processo appartengono, &lt;code&gt;ss&lt;/code&gt; ha sostituito il vecchio &lt;code&gt;netstat&lt;/code&gt;; i flag &lt;code&gt;-tulpn&lt;/code&gt; combinano socket TCP e UDP, mostrano solo quelli in ascolto (listening) e includono il nome del processo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;ss -tulpn
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per scoprire il proprio indirizzo IP pubblico interrogando un servizio esterno, &lt;code&gt;curl&lt;/code&gt; verso un endpoint dedicato restituisce direttamente il valore.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;curl ifconfig.me
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per informazioni geografiche e di provider più complete sullo stesso indirizzo pubblico, esiste un servizio equivalente più ricco.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;curl ipinfo.io
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per verificare la raggiungibilità di un host e misurarne la latenza, limitando l&#39;invio a un numero contenuto di pacchetti per non generare traffico inutile, si usa &lt;code&gt;ping&lt;/code&gt; con il flag &lt;code&gt;-c&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;ping -c 4 1.1.1.1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per visualizzare il percorso seguito dai pacchetti verso un server, utile in fase di diagnosi di problemi di instradamento, il comando classico è &lt;code&gt;traceroute&lt;/code&gt;; l&#39;alternativa interattiva &lt;code&gt;mtr&lt;/code&gt; combina traceroute e ping in un&#39;unica vista aggiornata in tempo reale.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;traceroute google.com
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per interrogare direttamente i server DNS e risolvere un dominio nel relativo indirizzo IP, gli strumenti di riferimento sono &lt;code&gt;dig&lt;/code&gt;, più verboso e dettagliato, e &lt;code&gt;nslookup&lt;/code&gt;, più sintetico.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;dig google.com
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;temperature-e-sensori&quot;&gt;Temperature e sensori&lt;/h2&gt;
&lt;p&gt;Per leggere le temperature della CPU e degli altri componenti hardware supportati, il comando &lt;code&gt;sensors&lt;/code&gt; è lo standard de facto sulle distribuzioni Linux, e richiede il pacchetto &lt;code&gt;lm-sensors&lt;/code&gt; installato e configurato tramite &lt;code&gt;sensors-detect&lt;/code&gt; al primo utilizzo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;sensors
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;tabella-riassuntiva-dei-comandi&quot;&gt;Tabella riassuntiva dei comandi&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Area&lt;/th&gt;
        &lt;th&gt;Comando principale&lt;/th&gt;
        &lt;th&gt;Scopo&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Disco&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;df -h&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Spazio libero e occupato sui filesystem montati&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Disco&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;du -sh&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Spazio occupato da una cartella specifica&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Memoria&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;free -h&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;RAM e swap libere, usate e totali&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;CPU&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;lscpu&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Architettura, core, thread e cache&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;GPU&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;lspci | grep -i vga&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Identificazione della scheda video&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Sistema&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;hostnamectl&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Hostname, kernel, distribuzione, architettura&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Rete&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;ip a&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Interfacce di rete e indirizzi IP/MAC&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Temperature&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;sensors&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Temperature CPU e componenti hardware&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;

]]></content>
    
    
    
      
      
    <category term="linux"/>
      
    
      
      
    <category term="sysadmin"/>
      
    
      
      
    <category term="terminale"/>
      
    
      
      
    <category term="networking"/>
      
    
      
      
    <category term="cli"/>
      
    
  </entry>
  
  <entry>
    <title>Lo stack in Assembly x86_64</title>
    <link href="https://dino-996.github.io/blog/lo-stack-in-assembly/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/lo-stack-in-assembly/</id>
    <published>2026-09-02T00:00:00.000Z</published>
    <updated>2026-09-02T00:00:00.000Z</updated>
    
    <summary>Lo stack è una delle strutture fondamentali della memoria di un processo. In questa parte vedremo come funziona, come viene organizzato all&#39;avvio del programma e come utilizzare rsp, push, pop e gli offset per accedere ai dati, compresi gli argomenti passati da riga di comando.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;A differenza degli indirizzi caricati manualmente o passati in altri registri, ogni processo possiede già una &lt;strong&gt;regione di memoria&lt;/strong&gt; pronta all’uso, lo &lt;strong&gt;stack&lt;/strong&gt; (&lt;strong&gt;pila&lt;/strong&gt;).&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Area temporanea&lt;/strong&gt;: lo stack è una regione di memoria utilizzata per memorizzare temporaneamente dati, variabili e informazioni sull’esecuzione del programma.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;rsp&lt;/strong&gt; (&lt;strong&gt;stack pointer&lt;/strong&gt;): è il registro della CPU che indica la posizione della cima (top) dello stack.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;lo-stato-dello-stack-allavvio-del-programma&quot;&gt;Lo stato dello stack all’avvio del programma&lt;/h2&gt;
&lt;p&gt;All’avvio di un programma in ambiente Linux, la &lt;strong&gt;cima dello stack&lt;/strong&gt; (&lt;code&gt;[rsp]&lt;/code&gt;) viene inizializzata dal sistema operativo con il conteggio degli argomenti da riga di comando (&lt;strong&gt;argc&lt;/strong&gt;).&lt;/p&gt;
&lt;p&gt;Il conteggio degli argomenti include anche il nome o il percorso del programma stesso, che viene considerato il &lt;strong&gt;primo elemento&lt;/strong&gt; del vettore &lt;code&gt;argv&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;accesso-agli-argomenti-da-riga-di-comando&quot;&gt;Accesso agli argomenti da riga di comando&lt;/h2&gt;
&lt;p&gt;Oltre al conteggio degli argomenti (&lt;code&gt;argc&lt;/code&gt;) in cima allo stack (&lt;code&gt;[rsp]&lt;/code&gt;), lo stack contiene un vettore di puntatori a stringhe, il classico &lt;code&gt;argv[]&lt;/code&gt; del linguaggio C, che indicano dove si trovano in memoria i testi degli argomenti.&lt;/p&gt;
&lt;p&gt;Ad esempio, eseguendo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;./programma hello world
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;avremo:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp]&lt;/code&gt; → &lt;strong&gt;argc&lt;/strong&gt;, cioè &lt;code&gt;3&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + 8]&lt;/code&gt; → &lt;strong&gt;argv[0]&lt;/strong&gt;, il puntatore al nome del programma&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + 16]&lt;/code&gt; → &lt;strong&gt;argv[1]&lt;/strong&gt;, il puntatore al primo argomento effettivo&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + 24]&lt;/code&gt; → &lt;strong&gt;argv[2]&lt;/strong&gt;, il puntatore al secondo argomento effettivo&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + 32]&lt;/code&gt; → &lt;strong&gt;NULL (0)&lt;/strong&gt;, che identifica la fine del vettore degli argomenti&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In generale, quindi:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;[rsp]                  → argc
[rsp + 8]              → argv[0]
[rsp + 16]             → argv[1]
...
[rsp + 8 × argc]       → argv[argc-1]
[rsp + 8 × (argc+1)]   → NULL
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;navigare-lo-stack-tramite-offset&quot;&gt;Navigare lo stack tramite offset&lt;/h3&gt;
&lt;p&gt;Lo stack contiene molteplici informazioni disposte in sequenza. Per accedere a valori posizionati più in profondità rispetto alla cima (&lt;code&gt;[rsp]&lt;/code&gt;), si utilizza un &lt;strong&gt;offset&lt;/strong&gt;, cioè una distanza espressa in byte.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp]&lt;/code&gt; legge il valore a &lt;strong&gt;offset 0&lt;/strong&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + 8]&lt;/code&gt; legge il valore posizionato &lt;strong&gt;8 byte dopo la cima&lt;/strong&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;code&gt;[rsp + N]&lt;/code&gt; legge il valore posizionato &lt;strong&gt;N byte dopo la cima&lt;/strong&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;In molti casi, lavorando con valori a 64 bit, gli offset che incontreremo nello stack saranno multipli di 8 (&lt;code&gt;+8&lt;/code&gt;, &lt;code&gt;+16&lt;/code&gt;, &lt;code&gt;+24&lt;/code&gt; e così via).&lt;/p&gt;
&lt;p&gt;Questo non significa che la memoria sia organizzata in blocchi di 8 byte: la memoria è sempre indirizzata &lt;strong&gt;byte per byte&lt;/strong&gt;. Il motivo per cui negli esempi vediamo incrementi di 8 byte è che indirizzi e altri valori a 64 bit occupano normalmente &lt;strong&gt;8 byte&lt;/strong&gt;.&lt;/p&gt;
&lt;h3 id=&quot;allineamento-dello-stack-e-abi&quot;&gt;Allineamento dello stack e ABI&lt;/h3&gt;
&lt;p&gt;Quando si passa dalla semplice lettura dello stack alla scrittura di funzioni che effettuano chiamate con &lt;code&gt;call&lt;/code&gt;, la &lt;strong&gt;System V AMD64 ABI&lt;/strong&gt; richiede che &lt;code&gt;rsp&lt;/code&gt; sia allineato a &lt;strong&gt;16 byte immediatamente prima dell&#39;istruzione ****&lt;code&gt;call&lt;/code&gt;&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;È una delle cause più frequenti di comportamenti anomali per chi inizia a scrivere Assembly x86-64, quindi vale la pena tenerlo a mente fin da questi primi esercizi.&lt;/p&gt;
&lt;h3 id=&quot;rappresentazione-visiva-dello-stack&quot;&gt;Rappresentazione visiva dello stack&lt;/h3&gt;
&lt;h3 id=&quot;listruzione-pop-e-la-gestione-dinamica-dello-stack&quot;&gt;L’istruzione &lt;code&gt;pop&lt;/code&gt; e la gestione dinamica dello stack&lt;/h3&gt;
&lt;p&gt;Lo stack è strutturato per consentire l’inserimento e l’estrazione dinamica dei dati dalla cima. L’istruzione &lt;code&gt;pop &amp;lt;registro&amp;gt;&lt;/code&gt; combina in una singola istruzione due operazioni:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Lettura del dato&lt;/strong&gt;: copia nel registro specificato il valore situato in cima allo stack, equivalente a un&#39;istruzione &lt;code&gt;mov &amp;lt;registro&amp;gt;, [rsp]&lt;/code&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Avanzamento del puntatore&lt;/strong&gt;: incrementa lo stack pointer &lt;code&gt;rsp&lt;/code&gt; di 8 byte, equivalente a &lt;code&gt;add rsp, 8&lt;/code&gt;, spostando la cima dello stack all&#39;elemento successivo.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Il dato estratto rimane fisicamente presente all&#39;indirizzo di memoria precedente, ma per la logica dello stack viene considerato rimosso, in quanto &lt;code&gt;rsp&lt;/code&gt; si è spostato oltre.&lt;/p&gt;
&lt;p&gt;L&#39;&lt;strong&gt;operazione complementare&lt;/strong&gt; a &lt;code&gt;pop&lt;/code&gt; è &lt;strong&gt;push&lt;/strong&gt;, che inserisce un nuovo valore in cima allo stack decrementando &lt;code&gt;rsp&lt;/code&gt; di 8 byte e scrivendo il valore nella nuova cima.&lt;/p&gt;
&lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;  
&lt;thead class=&quot;table-dark&quot;&gt;  
&lt;tr&gt;  
&lt;th&gt;Istruzione&lt;/th&gt;  
&lt;th&gt;Effetto su rdi&lt;/th&gt;  
&lt;th&gt;Effetto su rsp&lt;/th&gt;  
&lt;/tr&gt;  
&lt;/thead&gt;  
&lt;tbody&gt;  
&lt;tr&gt;  
&lt;td class=&quot;fw-semibold&quot;&gt;  
&lt;code&gt;mov rdi, [rsp]&lt;/code&gt;  
&lt;/td&gt;  
&lt;td&gt;Legge il valore in cima allo stack&lt;/td&gt;  
&lt;td&gt;Nessuna modifica&lt;/td&gt;  
&lt;/tr&gt;  
&lt;tr&gt;  
&lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;pop rdi&lt;/code&gt;&lt;/td&gt;  
&lt;td&gt;Legge il valore in cima allo stack&lt;/td&gt;  
&lt;td&gt;Avanza di 8 byte&lt;/td&gt;  
&lt;/tr&gt;  
&lt;tr&gt;  
&lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;push rdi&lt;/code&gt;&lt;/td&gt;  
&lt;td&gt;Inserisce il valore in cima allo stack&lt;/td&gt;  
&lt;td&gt;Indietreggia di 8 byte&lt;/td&gt;  
&lt;/tr&gt;  
&lt;/tbody&gt;  
&lt;/table&gt;
&lt;h2 id=&quot;ispezionare-lo-stack-con-gdb-gnu-debugger&quot;&gt;Ispezionare lo stack con GDB (GNU Debugger)&lt;/h2&gt;
&lt;p&gt;Per un lettore junior che si avvicina a questi concetti, verificarli empiricamente con un debugger è spesso più efficace della sola lettura teorica. GDB permette di osservare in tempo reale il contenuto dello stack e il valore di &lt;code&gt;rsp&lt;/code&gt; subito dopo l’avvio del programma.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;gdb -q ./nome_programma
break _start
run hello world
info registers rsp
x/5gx $rsp
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il comando &lt;code&gt;x/5gx $rsp&lt;/code&gt; mostra cinque valori a 64 bit (&lt;code&gt;g&lt;/code&gt;) in formato esadecimale (&lt;code&gt;x&lt;/code&gt;) a partire dall&#39;indirizzo puntato da &lt;code&gt;rsp&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Eseguendo il programma con &lt;code&gt;hello world&lt;/code&gt;, sarà quindi possibile osservare &lt;code&gt;argc&lt;/code&gt;, i tre puntatori di &lt;code&gt;argv&lt;/code&gt; e il &lt;code&gt;NULL&lt;/code&gt; che ne indica la fine.&lt;/p&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="stack"/>
      
    
      
      
    <category term="push"/>
      
    
      
      
    <category term="pop"/>
      
    
      
      
    <category term="offset"/>
      
    
  </entry>
  
  <entry>
    <title>Accesso alla memoria in Assembly x86_64</title>
    <link href="https://dino-996.github.io/blog/valori-indirizzi-e-deferenziamento/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/valori-indirizzi-e-deferenziamento/</id>
    <published>2026-09-01T00:00:00.000Z</published>
    <updated>2026-09-01T00:00:00.000Z</updated>
    
    <summary>Capire la differenza tra un valore e un indirizzo di memoria è uno dei passaggi fondamentali per iniziare a comprendere davvero l&#39;Assembly. In questa seconda parte vedremo come funzionano la dereferenziazione, i puntatori, gli offset e l&#39;ordine dei byte in memoria, con esempi pratici in Assembly x86-64.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;valori-indirizzi-e-dereferenziamento-in-assembly-x86-64&quot;&gt;Valori, indirizzi e dereferenziamento in assembly x86-64&lt;/h2&gt;
&lt;p&gt;Uno degli scogli concettuali più importanti per chi si avvicina alla programmazione a basso livello è capire la differenza tra un valore e un indirizzo di memoria. Nella sostanza sono la stessa cosa, un numero, ma il modo in cui l&#39;assembly le tratta cambia completamente il comportamento di un programma.&lt;/p&gt;
&lt;h3 id=&quot;la-memoria-come-indirizzario&quot;&gt;La memoria come indirizzario&lt;/h3&gt;
&lt;p&gt;La memoria di un computer può essere immaginata come una lunga via di case numerate in sequenza. Ogni casa corrisponde a un byte (8 bit) di memoria e possiede un indirizzo numerico univoco. Nei sistemi moderni, come discusso nel primo articolo della serie, lo spazio di indirizzamento è enorme e presenta ampie zone non mappate, esattamente come una via con numeri civici che saltano da un blocco all&#39;altro.&lt;/p&gt;
&lt;p&gt;Questa analogia torna utile per distinguere due concetti che in assembly si scrivono in modo molto simile ma significano cose opposte: conoscere il numero civico di una casa non equivale a sapere cosa contiene quella casa.&lt;/p&gt;
&lt;h3 id=&quot;valore-diretto-e-lettura-di-un-indirizzo&quot;&gt;Valore diretto e lettura di un indirizzo&lt;/h3&gt;
&lt;p&gt;Chiarito il concetto, possiamo usare &lt;code&gt;mov&lt;/code&gt; sia per assegnare un valore a un registro, sia per leggere il contenuto di un indirizzo di memoria.&lt;/p&gt;
&lt;p&gt;Per assegnare un valore a un registro, la sintassi corretta è la seguente:&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rdi, 13317
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La lettura, invece, avviene usando la sintassi &lt;code&gt;[valore]&lt;/code&gt;:&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rdi, [13317]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Le parentesi quadre indicano alla CPU che &lt;code&gt;13317&lt;/code&gt; va interpretato come un indirizzo di memoria. La CPU si reca a quell&#39;indirizzo, preleva il byte (o i byte) memorizzati e li copia nel registro &lt;code&gt;rdi&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;A questo punto qualcuno potrebbe provare ad assegnare a un registro generico un valore, per poi dereferenziare quel valore in &lt;code&gt;rdi&lt;/code&gt; e chiamare la syscall &lt;code&gt;exit&lt;/code&gt; restituendo il contenuto del registro generico. Ed è qui che si sbaglia più spesso.&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rbx, 10
mov rdi, [rbx]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questo &lt;strong&gt;non&lt;/strong&gt; assegna il valore &lt;code&gt;10&lt;/code&gt; a &lt;code&gt;rdi&lt;/code&gt;. Se vogliamo semplicemente copiare lo stesso valore, basta fare:&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rdi, rbx
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Letteralmente, stiamo dicendo alla CPU: prendi il valore contenuto in &lt;code&gt;rbx&lt;/code&gt; e copialo in &lt;code&gt;rdi&lt;/code&gt;. In questo caso:&lt;/p&gt;
&lt;p&gt;text&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;rdi = rbx = 10
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Con &lt;code&gt;mov rdi, [rbx]&lt;/code&gt;, invece, stiamo dicendo alla CPU: prendi il valore contenuto in &lt;code&gt;rbx&lt;/code&gt;, usalo come indirizzo di memoria, vai a quell&#39;indirizzo e copia in &lt;code&gt;rdi&lt;/code&gt; ciò che trovi lì. In altre parole:&lt;/p&gt;
&lt;p&gt;text&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;rbx      → valore contenuto nel registro
[rbx]    → valore contenuto nella memoria all&#39;indirizzo indicato da rbx
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;i-registri-come-puntatori&quot;&gt;I registri come puntatori&lt;/h3&gt;
&lt;p&gt;Nella programmazione reale è raro lavorare con indirizzi fissi scritti a mano nel codice. Più spesso un indirizzo viene calcolato o ricevuto a runtime e salvato in un registro, che a quel punto assume il ruolo di puntatore.&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rax, 133700
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Dopo questa istruzione, &lt;code&gt;rax&lt;/code&gt; contiene l&#39;indirizzo &lt;code&gt;133700&lt;/code&gt;. Per leggere il dato a cui punta, si applica la dereferenziazione tramite parentesi quadre:&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov rdi, [rax]
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Dereferenziare significa dire alla CPU: non prendere il numero scritto nel registro così com&#39;è, ma vai all&#39;indirizzo di memoria che quel numero rappresenta e leggi (o scrivi) il contenuto presente lì, come già visto nella sezione precedente. &lt;/div&gt;
&lt;p&gt;I registri della CPU, come &lt;code&gt;rax&lt;/code&gt; o &lt;code&gt;rdi&lt;/code&gt;, non hanno un ruolo semantico fisso. Lo stesso registro può contenere in momenti diversi l&#39;identificativo di una syscall, un puntatore a un&#39;area di memoria, o un semplice valore numerico. Per la CPU un registro è solo un contenitore di bit: il significato dipende esclusivamente dall&#39;istruzione che lo utilizza.&lt;/p&gt;
&lt;p&gt;La scelta del registro da usare come puntatore è arbitraria: qualsiasi registro general purpose (&lt;code&gt;rax&lt;/code&gt;, &lt;code&gt;rdi&lt;/code&gt;, &lt;code&gt;rbx&lt;/code&gt;, &lt;code&gt;rcx&lt;/code&gt; e così via) può contenere un indirizzo. Una conseguenza interessante è che un registro può dereferenziare se stesso, sovrascrivendo il proprio contenuto (l&#39;indirizzo) con il valore letto dalla memoria a quell&#39;indirizzo. Nel flusso di esecuzione lo stesso registro cambia quindi ruolo: da indirizzo diventa contenitore del dato finale.&lt;/p&gt;
&lt;h4 id=&quot;specificare-la-dimensione-delloperando&quot;&gt;Specificare la dimensione dell&#39;operando&lt;/h4&gt;
&lt;p&gt;Quando si scrive un valore immediato direttamente in un indirizzo di memoria, l&#39;assemblatore non può dedurre da solo quanti byte occupare, perché né la sorgente (un numero puro) né la destinazione (un indirizzo, non un registro) portano un&#39;informazione di dimensione. È necessario specificarla esplicitamente con un modificatore.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Quando la destinazione di una &lt;code&gt;mov&lt;/code&gt; è un indirizzo di memoria e la sorgente è un valore immediato, va sempre indicata la dimensione dell&#39;operando, altrimenti l&#39;assemblatore restituisce un errore di ambiguità. È possibile farlo attraverso quattro modificatori. &lt;/div&gt; &lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Modificatore&lt;/th&gt; &lt;th&gt;Dimensione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;byte ptr&lt;/code&gt;&lt;/td&gt; &lt;td&gt;1 byte, 8 bit&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;word ptr&lt;/code&gt;&lt;/td&gt; &lt;td&gt;2 byte, 16 bit&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;dword ptr&lt;/code&gt;&lt;/td&gt; &lt;td&gt;4 byte, 32 bit&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;&lt;code&gt;qword ptr&lt;/code&gt;&lt;/td&gt; &lt;td&gt;8 byte, 64 bit&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Applicando il modificatore corretto per lavorare con registri a 64 bit come &lt;code&gt;rax&lt;/code&gt;, la sequenza completa di scrittura e autodereferenziamento diventa:&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;mov qword ptr [133700], 42
mov rax, 133700
mov rax, [rax]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La prima riga scrive il valore &lt;code&gt;42&lt;/code&gt; all&#39;indirizzo di memoria &lt;code&gt;133700&lt;/code&gt;, specificando esplicitamente che si tratta di un blocco a 8 byte attraverso il modificatore &lt;code&gt;qword&lt;/code&gt;. La seconda riga carica in &lt;code&gt;rax&lt;/code&gt; l&#39;indirizzo &lt;code&gt;133700&lt;/code&gt;. La terza riga fa leggere a &lt;code&gt;rax&lt;/code&gt; il contenuto presente a quell&#39;indirizzo, sovrascrivendo se stesso: da indirizzo, &lt;code&gt;rax&lt;/code&gt; diventa il valore &lt;code&gt;42&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;offset-di-memoria-e-indirizzamento-relativo&quot;&gt;Offset di memoria e indirizzamento relativo&lt;/h3&gt;
&lt;p&gt;Un puntatore non sempre indica esattamente il dato di interesse: spesso punta all&#39;inizio di una struttura più ampia, come una stringa, un array o un record. Poiché ogni indirizzo identifica un singolo byte, per raggiungere un dato specifico all&#39;interno della struttura si somma un offset, cioè una distanza in byte, all&#39;indirizzo base.&lt;/p&gt;
&lt;p&gt;assembly&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-assembly&quot;&gt;.intel_syntax noprefix
.global _start
_start:
    mov rax, [rdi + 1]
    mov rax, 60
    syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo esempio, &lt;code&gt;rdi&lt;/code&gt; contiene l&#39;indirizzo base, mentre &lt;code&gt;[rdi + 1]&lt;/code&gt; accede al byte immediatamente successivo, saltando il primo byte della struttura. La sintassi con l&#39;offset all&#39;interno delle parentesi quadre è lo stesso pattern che si userà per scorrere array o stringhe carattere per carattere.&lt;/p&gt;
&lt;h4 id=&quot;un-cenno-allordine-dei-byte-little-endian&quot;&gt;Un cenno all&#39;ordine dei byte (little-endian)&lt;/h4&gt;
&lt;p&gt;Quando un valore occupa più di un byte, la CPU deve stabilire in quale ordine salvare i singoli byte in memoria. L&#39;architettura x86-64 usa l&#39;ordine little-endian: il byte meno significativo viene memorizzato per primo, cioè all&#39;indirizzo di memoria più basso.&lt;/p&gt;
&lt;p&gt;Per esempio, il valore:&lt;/p&gt;
&lt;p&gt;text&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;0x12345678
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;viene memorizzato in memoria come:&lt;/p&gt;
&lt;p&gt;text&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;78 56 34 12
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Quindi, il valore che leggiamo normalmente da sinistra a destra non corrisponde all&#39;ordine con cui i singoli byte sono disposti in memoria. È un dettaglio che diventa importante soprattutto quando si analizza la memoria con strumenti come GDB.&lt;/p&gt;
&lt;h3 id=&quot;puntatori-a-puntatori&quot;&gt;Puntatori a puntatori&lt;/h3&gt;
&lt;p&gt;Gli indirizzi di memoria sono a tutti gli effetti valori numerici, quindi la memoria stessa può contenere un indirizzo che punta a un altro indirizzo.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; È l&#39;equivalente di conservare in casa propria un foglietto con scritto l&#39;indirizzo di un amico: per raggiungerlo bisogna prima leggere il foglietto (primo accesso alla memoria) e poi usare l&#39;indirizzo letto per arrivare a destinazione (secondo accesso alla memoria). &lt;/div&gt; &lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Passaggio&lt;/th&gt; &lt;th&gt;Istruzione assembly&lt;/th&gt; &lt;th&gt;Valore nel registro&lt;/th&gt; &lt;th&gt;Spiegazione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;1. Caricamento indirizzo base&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, 123400&lt;/code&gt;&lt;/td&gt; &lt;td&gt;rdi = 123400&lt;/td&gt; &lt;td&gt;Inserisce l&#39;indirizzo iniziale nel registro&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;2. Prima dereferenziazione&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, [rdi]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;rdi = 133700&lt;/td&gt; &lt;td&gt;Legge il valore contenuto in 123400&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;3. Seconda dereferenziazione&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rax, [rdi]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;rax = 42&lt;/td&gt; &lt;td&gt;Legge il dato finale all&#39;indirizzo 133700&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;catena-di-puntatori-a-partire-da-un-registro&quot;&gt;Catena di puntatori a partire da un registro&lt;/h3&gt;
&lt;p&gt;L&#39;ultimo scenario combina i due concetti visti finora: l&#39;uso di un &lt;strong&gt;registro come puntatore&lt;/strong&gt; e il &lt;strong&gt;doppio dereferenziamento&lt;/strong&gt;, in un caso in cui non si conosce a priori alcun indirizzo numerico fisso, ma tutto parte dal contenuto di un registro fornito in ingresso.&lt;/p&gt;
&lt;h3 id=&quot;tabella-riepilogativa-delle-modalita-di-indirizzamento&quot;&gt;Tabella riepilogativa delle modalità di indirizzamento&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Pattern&lt;/th&gt; &lt;th&gt;Sintassi tipo&lt;/th&gt; &lt;th&gt;Cosa restituisce&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Valore diretto&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, 133700&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Il numero grezzo, nessun accesso alla memoria&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Dereferenziamento singolo&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, [rax]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Il contenuto memorizzato all&#39;indirizzo in rax&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Autodereferenziamento&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rax, [rax]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Il registro sovrascrive se stesso col dato letto&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Offset&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov al, [rdi + 1]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Il byte a un byte di distanza dall&#39;indirizzo base&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Doppia dereferenziazione&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, [rdi]&lt;/code&gt; seguito da &lt;code&gt;mov rax, [rdi]&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Il dato finale, raggiunto attraverso due accessi consecutivi alla memoria&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Catena da registro&lt;/td&gt; &lt;td&gt;&lt;code&gt;mov rdi, [rdi]&lt;/code&gt; ripetuto&lt;/td&gt; &lt;td&gt;Il valore in fondo a una catena di puntatori partita da un registro&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="deferenziamento"/>
      
    
      
      
    <category term="puntatori"/>
      
    
      
      
    <category term="indirizzi"/>
      
    
      
      
    <category term="offset"/>
      
    
  </entry>
  
  <entry>
    <title>Introduzione ad Assembly</title>
    <link href="https://dino-996.github.io/blog/introduzione-a-assembly/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/introduzione-a-assembly/</id>
    <published>2026-08-31T00:00:00.000Z</published>
    <updated>2026-08-31T00:00:00.000Z</updated>
    
    <summary>Un&#39;introduzione pratica all&#39;Assembly x86-64 su Linux: dall&#39;ISA e dai registri della CPU alle system call, fino alla creazione e all&#39;esecuzione di un programma ELF. Un percorso pensato per comprendere cosa succede realmente dietro le quinte di un semplice programma.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;cose-lisa&quot;&gt;Cos&#39;è l&#39;ISA&lt;/h2&gt;
&lt;p&gt;L&#39;&lt;strong&gt;ISA (Instruction Set Architecture)&lt;/strong&gt; è l&#39;insieme di istruzioni che un microprocessore è in grado di capire ed eseguire. Le operazioni matematiche, i confronti logici nonché la lettura e la scrittura di dati. Possiamo immaginarla come una specie di &lt;strong&gt;lingua comune&lt;/strong&gt; tra hardware e software. E&#39; grazie all&#39;ISA che un programma sa come interagire con la CPU e con la memoria del computer.&lt;/p&gt;
&lt;h2 id=&quot;la-famiglia-x86&quot;&gt;La famiglia x86&lt;/h2&gt;
&lt;p&gt;Un esempio famosissimo di ISA è la &lt;strong&gt;famiglia x86&lt;/strong&gt;, usata ancora oggi nella stragrande maggioranza di PC e server. Tutto nasce nel &lt;strong&gt;1978&lt;/strong&gt;, con l&#39;uscita del microprocessore &lt;strong&gt;Intel 8086&lt;/strong&gt;, un chip a &lt;strong&gt;16 bit&lt;/strong&gt;, che per l&#39;epoca rappresentava un salto in avanti notevole rispetto ai processori precedenti.&lt;/p&gt;
&lt;p&gt;Da lì in poi Intel ha continuato a evolvere questa architettura, passando per modelli come l&#39;&lt;strong&gt;80286&lt;/strong&gt;, l&#39;&lt;strong&gt;80386&lt;/strong&gt;, l&#39;&lt;strong&gt;80486&lt;/strong&gt;, fino ad arrivare ai &lt;strong&gt;Pentium&lt;/strong&gt; e, più recentemente, ai processori &lt;strong&gt;Intel Core&lt;/strong&gt;. Anche altri produttori, come &lt;strong&gt;AMD&lt;/strong&gt;, realizzano oggi chip basati sulla stessa architettura x86.&lt;/p&gt;
&lt;h3 id=&quot;x86-vs-x64-qual-e-la-differenza&quot;&gt;x86 vs x64: qual è la differenza&lt;/h3&gt;
&lt;p&gt;La differenza principale riguarda &lt;strong&gt;quanti bit&lt;/strong&gt; la CPU riesce a gestire in una volta sola:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;x86&lt;/strong&gt; (spesso chiamata anche &lt;em&gt;x86-32&lt;/em&gt;) indica l&#39;architettura a &lt;strong&gt;32 bit&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;x64&lt;/strong&gt; (o &lt;em&gt;x86-64&lt;/em&gt;, o &lt;em&gt;AMD64&lt;/em&gt;) indica l&#39;architettura a &lt;strong&gt;64 bit&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Va detto che, storicamente, x86 indicava &lt;strong&gt;tutta la famiglia di processori derivati dall&#39;8086&lt;/strong&gt; (compresi quindi anche i modelli a 16 e a 32 bit), non solo la versione a 32 bit. Nel linguaggio comune, però, x86 viene usato quasi sempre come sinonimo di 32 bit, proprio per contrapporlo a x64.&lt;/p&gt;
&lt;h3 id=&quot;perche-conta-il-numero-di-bit&quot;&gt;Perché conta il numero di bit&lt;/h3&gt;
&lt;p&gt;Il numero di bit determina anche &lt;strong&gt;quanta memoria RAM la CPU riesce a indirizzare&lt;/strong&gt; (cioè ad usare):&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Con &lt;strong&gt;32 bit&lt;/strong&gt; lo spazio di indirizzamento teorico è di &lt;strong&gt;4 GiB&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;Con &lt;strong&gt;64 bit&lt;/strong&gt; lo spazio di indirizzamento teorico arriva teoricamente a &lt;strong&gt;16 EiB&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Questi 16 exabyte, però, sono un limite puramente &lt;em&gt;teorico&lt;/em&gt;, che deriva dal calcolo matematico 2⁶⁴. Nella pratica, i processori e i sistemi operativi attuali supportano molta meno memoria (di solito qualche centinaio di terabyte), per via di limiti fisici e progettuali reali. Resta comunque un numero enorme rispetto ai sistemi a 32 bit.&lt;/p&gt;
&lt;p&gt;Questo salto di capacità permette ai sistemi a 64 bit di lavorare con dataset più grandi ed eseguire calcoli più complessi, rendendoli più adatti a compiti impegnativi. In più, l&#39;architettura a 64 bit introduce funzionalità di &lt;strong&gt;sicurezza&lt;/strong&gt; più avanzate (come la protezione contro l&#39;esecuzione di codice malevolo in memoria) e prestazioni migliori, grazie anche a un numero maggiore di &lt;strong&gt;registri general-purpose&lt;/strong&gt; e a set di istruzioni più moderni.&lt;/p&gt;
&lt;h3 id=&quot;come-funziona-una-cpu-in-breve&quot;&gt;Come funziona una CPU, in breve&lt;/h3&gt;
&lt;p&gt;Una CPU x86_64 esegue in fondo un numero limitato di operazioni elementari, tutte combinabili tra loro per costruire programmi complessi. A livello concettuale, possiamo ricondurre molte operazioni a tre categorie: &lt;strong&gt;sposta&lt;/strong&gt; dati, &lt;strong&gt;modifica&lt;/strong&gt; dati e &lt;strong&gt;prende decisioni&lt;/strong&gt; in base ai dati.&lt;/p&gt;
&lt;p&gt;I dati su cui la CPU lavora vengono conservati principalmente nei registri, piccole memorie interne alla CPU, estremamente veloci ma anche costose e limitate. L&#39;ISA x86-64 mette a disposizione &lt;strong&gt;16 registri general-purpose a 64 bit&lt;/strong&gt;, ciascuno pensato per contenere valori numerici che il processore può leggere e scrivere in tempi minimi.&lt;/p&gt;
&lt;h2 id=&quot;panoramica-dei-registri&quot;&gt;Panoramica dei registri&lt;/h2&gt;
&lt;p&gt;I primi otto sono le &lt;strong&gt;estensioni&lt;/strong&gt; a 64 bit dei classici registri x86 a 32 bit, mentre gli ultimi otto sono stati aggiunti specificamente per la modalità a 64 bit.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;RAX&lt;/strong&gt; (Accumulatore)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RBX&lt;/strong&gt; (Registro base)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RCX&lt;/strong&gt; (Contatore)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RDX&lt;/strong&gt; (Registro dati)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RSI&lt;/strong&gt; (Source Index)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RDI&lt;/strong&gt; (Destination Index)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RBP&lt;/strong&gt; (Base Pointer dello stack)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RSP&lt;/strong&gt; (Stack Pointer)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R8&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R9&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R10&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R11&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R12&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R13&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R14&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;R15&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h3 id=&quot;il-registro-rax-e-listruzione-mov&quot;&gt;Il registro rax e l&#39;istruzione mov&lt;/h3&gt;
&lt;p&gt;Tra i registri di uso generale, &lt;code&gt;rax&lt;/code&gt; è uno dei più usati. Può contenere un valore numerico, e i dati vengono inseriti nei registri tramite l’istruzione &lt;strong&gt;mov&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;La sintassi di mov è semplice, il primo operando è la destinazione, il secondo è la sorgente e i due sono sempre separati da una virgola.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rax, 1337
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo esempio il valore 1337 viene copiato dentro il registro rax.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Precisiamo che gli articoli su Assembly faranno riferimento alla sintassi Intel, originale di x86, e non la sintassi AT&amp;amp;T, un dettagio da tenere presente perché l&#39;ordine degli operandi cambia se si sceglie una sintassi diversa.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;le-system-call-chiamate-di-sistema&quot;&gt;Le System Call (chiamate di sistema)&lt;/h3&gt;
&lt;p&gt;Una &lt;strong&gt;System Call&lt;/strong&gt; è il meccanismo con cui un programma comunica con il sistema operativo. È il sistema operativo, infatti, a gestire l’avvio e la terminazione dei programmi, l’accesso all’hardware, la rete e le altre risorse di sistema.&lt;/p&gt;
&lt;p&gt;Come abbiamo detto, le istruzioni Assembly interagiscono direttamente con la CPU, ma per richiedere determinati servizi al kernel, il programma utilizza il meccanismo delle system call. Su Linux x86-64, l&#39;istruzione &lt;code&gt;syscall&lt;/code&gt; è il meccanismo utilizzato per effettuare una system call.&lt;/p&gt;
&lt;h4 id=&quot;la-syscall-exit&quot;&gt;La syscall exit&lt;/h4&gt;
&lt;p&gt;La prima chiamata di sistema che vale la pena imparare è &lt;code&gt;exit&lt;/code&gt;, che serve a terminare esplicitamente il processo e a restituire al sistema operativo un codice di uscita. Il numero associato a &lt;code&gt;exit&lt;/code&gt; è 60.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rax, 60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ogni &lt;code&gt;syscall&lt;/code&gt; è identificata da un numero, il valore viene caricato in &lt;code&gt;rax&lt;/code&gt;, l&#39;istruzione &lt;code&gt;syscall&lt;/code&gt; esegue la richiesta, e &lt;code&gt;exit&lt;/code&gt; (60) termina correttamente il programma.&lt;/p&gt;
&lt;p&gt;Quando un programma termina restituisce un &lt;strong&gt;codice di uscita&lt;/strong&gt; attraverso la &lt;code&gt;syscall exit&lt;/code&gt;, la domanda è: come si comunica questo codice di uscita?&lt;/p&gt;
&lt;p&gt;Banalmente, la risposta è tramite i registri. Il primo parametro di una &lt;code&gt;syscall&lt;/code&gt; viene inserito nel registro &lt;code&gt;rdi&lt;/code&gt;. Nel caso di &lt;code&gt;exit&lt;/code&gt; questo parametro è esattamente il codice di uscita.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Per fare un&#39;analogia con Python, la syscall &lt;code&gt;exit&lt;/code&gt; è simile alla chiamata a una funzione &lt;code&gt;exit()&lt;/code&gt; a cui passiamo un parametro, ad esempio &lt;code&gt;exit(42)&lt;/code&gt;. Il valore &lt;code&gt;42&lt;/code&gt; rappresenta il codice di uscita con cui il programma termina.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;mov rdi, 42
mov rax, 60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;A differenza di altri linguaggi di programmazione, in Assembly si lavora direttamente con i registri della CPU. Alla CPU non interessa quindi se impostiamo prima il codice di uscita o il numero della syscall &lt;code&gt;exit&lt;/code&gt;: ciò che conta è che, al momento dell&#39;esecuzione di &lt;code&gt;syscall&lt;/code&gt;, i valori siano nei registri corretti. I valori, invece, non vanno assegnati ai registri sbagliati: &lt;code&gt;RAX&lt;/code&gt; deve contenere il numero della syscall e &lt;code&gt;RDI&lt;/code&gt; il codice di uscita. Inoltre Con &lt;strong&gt;GNU assembler&lt;/strong&gt;, le istruzioni e i nomi dei registri x86 non sono case sensitive.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Registro&lt;/th&gt; &lt;th&gt;Scopo&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rax&lt;/td&gt; &lt;td&gt;Numero della chiamata di sistema&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rdi&lt;/td&gt; &lt;td&gt;Primo parametro della chiamata (codice di uscita per exit)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;rsi&lt;/td&gt; &lt;td&gt;Registro di uso generale, utile per memorizzare valori intermedi o copiarli in altri registri&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;dal-codice-sorgente-alleseguibile&quot;&gt;Dal codice sorgente all&#39;eseguibile&lt;/h2&gt;
&lt;p&gt;Scrivere codice Assembly non basta per eseguirlo, serve trasformarlo in un file eseguibile&lt;br&gt;
attraverso tre passaggi principali:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Scrivere il codice in un file .s.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Assemblarlo&lt;/strong&gt; in un file oggetto .o con as.&lt;/li&gt;
&lt;li&gt;Collegarlo (linkarlo) in un &lt;strong&gt;eseguibile&lt;/strong&gt; con ld.&lt;/li&gt;
&lt;/ol&gt;
&lt;h4 id=&quot;scivere-il-file-assembly&quot;&gt;Scivere il file Assembly&lt;/h4&gt;
&lt;p&gt;Sentiti libero di usare qualsiasi editor di testo per salvare il codice in un file, ad esempio creiamo un file nominato &lt;code&gt;program.s&lt;/code&gt;. All&#39;inizio del file è molto importante aggiungere la direttiva &lt;code&gt;.intel_syntax noprefix&lt;/code&gt;, che indica all&#39;assembler di utilizzare la sintassi Intel senza prefissi.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
mov rdi, 42
mov rax, 60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questa direttiva riguarda esclusivamente l&#39;assembler e non riguarda in nessun modo la CPU.&lt;/p&gt;
&lt;h3 id=&quot;assemblare-il-codice&quot;&gt;Assemblare il codice&lt;/h3&gt;
&lt;p&gt;Il file Assembly (&lt;strong&gt;codice sorgente&lt;/strong&gt;) viene convertito in un file oggetto (&lt;strong&gt;codice macchina e informazioni necessarie al linking&lt;/strong&gt;) con il comando seguente:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;as -o program.o program.s
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;collegare-il-file-oggetto&quot;&gt;Collegare il file oggetto&lt;/h3&gt;
&lt;p&gt;L&#39;eseguibile si crea con il comando:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ld -o program.elf program.o
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il programma può essere eseguito con &lt;code&gt;./program.elf&lt;/code&gt;, e il codice di uscita si controlla con &lt;code&gt;echo $?&lt;/code&gt; (&lt;code&gt;$?&lt;/code&gt; è una variabile speciale della shell che contiene lo status di uscita dell&#39;ultimo comando eseguito), che nel nostro esempio restituisce 42.&lt;/p&gt;
&lt;h3 id=&quot;il-formato-elf&quot;&gt;Il formato ELF&lt;/h3&gt;
&lt;p&gt;Su Linux, un programma eseguibile è generalmente un file in formato &lt;strong&gt;ELF&lt;/strong&gt; (&lt;em&gt;Executable and Linkable Format&lt;/em&gt;). Possiamo quindi trovare un eseguibile chiamato &lt;code&gt;program.elf&lt;/code&gt;, ma &lt;strong&gt;&lt;code&gt;.elf&lt;/code&gt; è solo un&#39;estensione convenzionale&lt;/strong&gt;: potremmo chiamarlo semplicemente &lt;code&gt;program&lt;/code&gt; e funzionerebbe comunque.&lt;/p&gt;
&lt;p&gt;Questo perché, a differenza di Windows, &lt;strong&gt;Linux non usa l&#39;estensione del file per determinarne il tipo o la sua eseguibilità&lt;/strong&gt;. Ciò che conta è il &lt;strong&gt;contenuto del file&lt;/strong&gt; e, per l&#39;esecuzione, anche la presenza dei &lt;strong&gt;permessi di esecuzione&lt;/strong&gt;.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;In Linux l&#39;&lt;strong&gt;estensione&lt;/strong&gt; è principalmente una convenzione per l&#39;utente; il formato ELF è determinato dal contenuto del file, non dal suo nome.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id=&quot;compilato-vs-assemblato&quot;&gt;Compilato vs Assemblato&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Assembly è un linguaggio di programmazione a basso livello, assemblato,&lt;/strong&gt; attenzione per chi viene da linguaggi come Java, C#, C o C++, è comune associare il termine &lt;em&gt;compilato&lt;/em&gt; al processo di traduzione del codice sorgente. In senso generale, anche l&#39;Assembly viene tradotto prima dell&#39;esecuzione; tuttavia, tecnicamente è più corretto parlare di &lt;strong&gt;assemblaggio&lt;/strong&gt;, perché il programma che effettua la traduzione è un &lt;strong&gt;assembler&lt;/strong&gt;, non un compiler (&lt;em&gt;compilato&lt;/em&gt; e &lt;em&gt;assemblato&lt;/em&gt; non sono necessariamente categorie opposte).&lt;/p&gt;
&lt;p&gt;La differenza fondamentale è che il &lt;strong&gt;compiler&lt;/strong&gt; traduce un linguaggio di programmazione in un&#39;altra rappresentazione, effettuando anche analisi e trasformazioni del programma, mentre l&#39;&lt;strong&gt;assembler&lt;/strong&gt; traduce le istruzioni Assembly nella corrispondente rappresentazione in &lt;strong&gt;linguaggio macchina dell&#39;architettura target&lt;/strong&gt;, in questo caso &lt;strong&gt;x86-64&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;Durante il collegamento può comparire l’avviso &lt;code&gt;cannot find entry symbol _start&lt;/code&gt;. Significa che il linker non trova il simbolo &lt;code&gt;_start&lt;/code&gt; indicato come &lt;strong&gt;punto di ingresso&lt;/strong&gt;.&lt;/p&gt;
&lt;h2 id=&quot;il-simbolo-start&quot;&gt;Il simbolo _start&lt;/h2&gt;
&lt;p&gt;Per eliminare l&#39;avviso si può definire esplicitamente un punto di ingresso (&lt;code&gt;_start&lt;/code&gt;) in questo modo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global _start
_start:
mov rdi, 42
mov rax, 60
syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ricapitolando, &lt;code&gt;.global _start&lt;/code&gt; rende il simbolo &lt;code&gt;_start&lt;/code&gt; visibile al &lt;strong&gt;linker&lt;/strong&gt;, mentre &lt;code&gt;_start:&lt;/code&gt; definisce l&#39;etichetta utilizzata come punto di ingresso del programma.&lt;/p&gt;
&lt;p&gt;È importante ricordare che &lt;code&gt;_start&lt;/code&gt;  &lt;strong&gt;NON è una parola riservata dell&#39;Assembly&lt;/strong&gt;: è semplicemente il nome di un&#39;etichetta, scelto per convenzione. Potremmo quindi chiamarla, ad esempio, &lt;code&gt;inizio&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-asm&quot;&gt;.intel_syntax noprefix
.global inizio
inizio:
    mov rdi, 42
    mov rax, 60
    syscall
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo caso, però, dovremmo indicare &lt;code&gt;inizio&lt;/code&gt; come punto di ingresso al linker, ad esempio con &lt;code&gt;ld -e inizio -o program program.o&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Funzione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;as -o program.o program.s&lt;/td&gt; &lt;td&gt;Assembla il codice Assembly in un file oggetto&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;ld -o program.elf program.o&lt;/td&gt; &lt;td&gt;Crea l&#39;eseguibile&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;./program.elf&lt;/td&gt; &lt;td&gt;Esegue il programma&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;echo $?&lt;/td&gt; &lt;td&gt;Mostra il codice di uscita del programma&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;il-registro-rsi-e-la-copia-tra-registri&quot;&gt;Il registro RSI e la copia tra registri&lt;/h3&gt;
&lt;p&gt;Un altro registro di uso generale è &lt;code&gt;rsi&lt;/code&gt;, che come gli alti, può essere usato per memorizzare dati, per esempio:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;mov rsi, 42
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Abbiamo registrato il valore 42 nel registro &lt;code&gt;rsi&lt;/code&gt;, ma possiamo fare di più, per esempio copiare il contenuto di un registro in un altro registro:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;mov rsi, 42
mov rdi, rsi
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La seconda istruzione non fa altro che &lt;strong&gt;copiare&lt;/strong&gt; il valore del registro &lt;code&gt;rsi&lt;/code&gt; in &lt;code&gt;rdi&lt;/code&gt;, dopo l&#39;esecuzione del codice ambo i registri conterranno il valore 42. Un dettaglio da tenere presente è che, nonostante il nome dell’istruzione sia &lt;strong&gt;mov&lt;/strong&gt; (abbreviazione di &lt;em&gt;move&lt;/em&gt;), essa non sposta realmente il valore da un registro all’altro, ma lo &lt;strong&gt;copia&lt;/strong&gt;. Il motivo storico per cui l’istruzione si chiami mov invece di qualcosa come set o copy non è del tutto chiaro, si tratta semplicemente di una convenzione consolidata dell’assembly.&lt;/p&gt;
&lt;h2 id=&quot;buone-pratiche-che-avrei-voluto-conosce-prima-per-chi-cominca-con-assembly&quot;&gt;Buone pratiche (che avrei voluto conosce prima) per chi cominca con Assembly&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Non dare &lt;strong&gt;mai per scontato&lt;/strong&gt; un valore di registro nel non specificatamente inizializzato proprio codice, anche se in un ambiente controllato di apprendimento un registro può sembrare &lt;em&gt;vuoto&lt;/em&gt; o a zero.&lt;/li&gt;
&lt;li&gt;Tenere presente che secondo la convenzione di chiamata System V AMD64 ABI comunemente usata su Linux, alcuni registri devono essere preservati dalla funzione chiamata (i cosiddetti registri callee-saved), un aspetto rilevante non appena si inizia a scrivere funzioni piu complesse delle semplici sequenze lineari viste fin ora.&lt;/li&gt;
&lt;li&gt;Verificare sempre il codice di uscita del proprio programma con echo $? subito dopo l’esecuzione, è il modo più rapido per capire se la logica implementata è corretta.&lt;/li&gt;
&lt;li&gt;Per ispezionare il contenuto di un file oggetto o di un eseguibile durante il debug, strumenti come &lt;strong&gt;objdump&lt;/strong&gt; o &lt;strong&gt;gdb&lt;/strong&gt; (presente nei prossimi articoli) permettono di vedere le istruzioni effettivamente generate, un passaggio utile quando il comportamento del programma non corrisponde alle aspettative.&lt;/li&gt;
&lt;/ol&gt;


]]></content>
    
    
    
      
      
    <category term="assembly"/>
      
    
      
      
    <category term="ISA"/>
      
    
      
      
    <category term="x86_64"/>
      
    
      
      
    <category term="system call"/>
      
    
      
      
    <category term="registri"/>
      
    
  </entry>
  
  <entry>
    <title>Guida completa a Docker</title>
    <link href="https://dino-996.github.io/blog/post/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/post/</id>
    <published>2026-08-10T00:00:00.000Z</published>
    <updated>2026-08-10T00:00:00.000Z</updated>
    
    <summary>Panoramica completa sull&#39;architettura e sul funzionamento dei container Docker. La guida copre i comandi essenziali per la gestione di immagini e volumi, la scrittura di Dockerfile multi-stage, l&#39;uso di Docker Compose e la configurazione dell&#39;ambiente su Windows tramite WSL2.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;cose-docker&quot;&gt;Cos&#39;è docker&lt;/h2&gt;
&lt;p&gt;Docker è una piattaforma di containerizzazione, permette di eseguire applicazioni in ambienti isolati (container) che includono tutto il necessario per funzionare: codice, runtime, librerie, dipendenze di sistema, senza dover installare nulla direttamente sulla macchina host.&lt;/p&gt;
&lt;p&gt;La differenza principale rispetto a una macchina virtuale è che i container condividono il kernel del sistema operativo host, risultando molto più leggeri e veloci da avviare.&lt;/p&gt;
&lt;h2 id=&quot;concetti-fondamentali&quot;&gt;Concetti fondamentali&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Immagine (image)&lt;/strong&gt; Un pacchetto immutabile che contiene filesystem, dipendenze e istruzioni per eseguire un&#39;applicazione. Le immagini sono composte da layer sovrapposti, ognuno corrispondente a un&#39;istruzione del Dockerfile che l&#39;ha generata.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Container&lt;/strong&gt; Un&#39;istanza in esecuzione di un&#39;immagine. È possibile avviare più container dalla stessa immagine, ognuno isolato dagli altri.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Registry&lt;/strong&gt; Un archivio di immagini scaricabili. Docker Hub è il registry pubblico predefinito (es. &lt;code&gt;node:22&lt;/code&gt;, &lt;code&gt;postgres:16&lt;/code&gt;, &lt;code&gt;yanwk/comfyui-boot&lt;/code&gt;).&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Dockerfile&lt;/strong&gt; Un file di testo con le istruzioni per costruire un&#39;immagine personalizzata.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Volume&lt;/strong&gt; Uno spazio di archiviazione gestito da Docker, indipendente dal ciclo di vita del container, usato per persistere dati.&lt;/p&gt;
&lt;h2 id=&quot;comandi-essenziali&quot;&gt;Comandi essenziali&lt;/h2&gt;
&lt;h3 id=&quot;gestione-container&quot;&gt;Gestione container&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker run &amp;lt;opzioni&amp;gt; &amp;lt;immagine&amp;gt;
docker ps
docker ps -a
docker stop &amp;lt;nome-o-id&amp;gt;
docker rm &amp;lt;nome-o-id&amp;gt;
docker exec -it &amp;lt;nome-o-id&amp;gt; bash
docker logs &amp;lt;nome-o-id&amp;gt;
docker logs -f &amp;lt;nome-o-id&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In ordine, i comandi sopra: avviano un container da un&#39;immagine (scaricandola se non presente), elencano i container in esecuzione, elencano tutti i container anche quelli fermi, fermano un container, rimuovono un container fermo, eseguono un comando dentro un container già attivo, mostrano i log e infine li mostrano in tempo reale.&lt;/p&gt;
&lt;h3 id=&quot;opzioni-piu-usate-con-docker-run&quot;&gt;Opzioni più usate con docker run&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Opzione&lt;/th&gt; &lt;th&gt;Effetto&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;--rm&lt;/td&gt; &lt;td&gt;Rimuove il container automaticamente allo stop&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-it&lt;/td&gt; &lt;td&gt;Modalità interattiva con terminale allegato&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-d&lt;/td&gt; &lt;td&gt;Modalità detached, esegue in background&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;--name&lt;/td&gt; &lt;td&gt;Assegna un nome al container&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-p host:container&lt;/td&gt; &lt;td&gt;Mappa una porta dell&#39;host su una del container&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-v&lt;/td&gt; &lt;td&gt;Monta un volume o bind mount&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-e&lt;/td&gt; &lt;td&gt;Imposta una variabile d&#39;ambiente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;--gpus all&lt;/td&gt; &lt;td&gt;Espone le GPU al container (richiede NVIDIA Container Toolkit)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;-w&lt;/td&gt; &lt;td&gt;Imposta la working directory dentro il container&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h3 id=&quot;gestione-immagini&quot;&gt;Gestione immagini&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker pull &amp;lt;immagine&amp;gt;
docker images
docker rmi &amp;lt;immagine&amp;gt;
docker build -t &amp;lt;nome-immagine&amp;gt; .
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questi comandi scaricano un&#39;immagine senza avviarla, elencano le immagini locali, rimuovono un&#39;immagine e costruiscono un&#39;immagine da un Dockerfile nella directory corrente.&lt;/p&gt;
&lt;h2 id=&quot;persistenza-dei-dati-bind-mount-vs-volume&quot;&gt;Persistenza dei dati, bind mount vs volume&lt;/h2&gt;
&lt;p&gt;Per default un container è effimero: tutto ciò che si scrive al suo interno sparisce quando viene rimosso. Esistono due modi per persistere i dati.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Bind mount&lt;/strong&gt;, collega una cartella reale dell&#39;host al container:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker run -v &amp;quot;C:&#92;percorso&#92;cartella:/app&amp;quot; &amp;lt;immagine&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;I file sono visibili e gestibili direttamente da Windows. Utile quando si vuole editare il codice con il proprio editor mentre gira nel container.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Volume Docker&lt;/strong&gt;, spazio gestito internamente da Docker, non visibile come cartella Windows:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker volume create nome-volume
docker run -v nome-volume:/app &amp;lt;immagine&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il volume sopravvive alla rimozione del container ma resta isolato, facile da eliminare del tutto:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker volume rm nome-volume
docker volume ls
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Combinazione tipica&lt;/strong&gt; (codice sincronizzato, dipendenze isolate):&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker run -v &amp;quot;${PWD}:/app&amp;quot; -v node-modules-cache:/app/node_modules -w /app node:22 npm install
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questo pattern evita che &lt;code&gt;node_modules&lt;/code&gt; venga scritto sul disco Windows, dove su WSL2 l&#39;I/O tra molti file piccoli è più lento rispetto al filesystem interno di Docker.&lt;/p&gt;
&lt;h2 id=&quot;rete-e-porte&quot;&gt;Rete e porte&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;-p 4200:4200
-p 8080:4200
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La prima riga mappa la porta 4200 del container sulla 4200 dell&#39;host, la seconda mappa su una porta host diversa (8080 verso 4200).&lt;/p&gt;
&lt;p&gt;Molti dev server (Angular, Vite, ecc.) per default ascoltano solo su &lt;code&gt;localhost&lt;/code&gt; dentro il container. Serve avviarli con un&#39;opzione che li faccia ascoltare su tutte le interfacce, altrimenti non sono raggiungibili dall&#39;host anche con la porta mappata correttamente (es. &lt;code&gt;ng serve --host 0.0.0.0&lt;/code&gt;).&lt;/p&gt;
&lt;h3 id=&quot;reti-docker-personalizzate&quot;&gt;Reti Docker personalizzate&lt;/h3&gt;
&lt;p&gt;Oltre al mapping di porte verso l&#39;host, Docker permette di creare reti bridge dedicate per far comunicare più container tra loro tramite hostname, senza esporre porte all&#39;esterno:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker network create --driver bridge rete-progetto
docker run -d --network rete-progetto --name db postgres:16
docker run -d --network rete-progetto --name backend mio-progetto
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Su una rete personalizzata, il container &lt;code&gt;backend&lt;/code&gt; può raggiungere il database semplicemente usando &lt;code&gt;db&lt;/code&gt; come hostname, invece di dover conoscere il suo indirizzo IP. Docker Compose crea automaticamente una rete di questo tipo per ogni progetto, motivo per cui i servizi definiti in un &lt;code&gt;docker-compose.yml&lt;/code&gt; si vedono tra loro per nome senza configurazione aggiuntiva.&lt;/p&gt;
&lt;h2 id=&quot;dockerfile&quot;&gt;Dockerfile&lt;/h2&gt;
&lt;p&gt;Un Dockerfile descrive come costruire un&#39;immagine, istruzione per istruzione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;FROM node:22
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 4200
CMD [&amp;quot;npm&amp;quot;, &amp;quot;start&amp;quot;, &amp;quot;--&amp;quot;, &amp;quot;--host&amp;quot;, &amp;quot;0.0.0.0&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Istruzioni principali:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;FROM&lt;/code&gt;, immagine base di partenza&lt;/li&gt;
&lt;li&gt;&lt;code&gt;WORKDIR&lt;/code&gt;, directory di lavoro dentro il container&lt;/li&gt;
&lt;li&gt;&lt;code&gt;COPY&lt;/code&gt; / &lt;code&gt;ADD&lt;/code&gt;, copia file dall&#39;host all&#39;immagine&lt;/li&gt;
&lt;li&gt;&lt;code&gt;RUN&lt;/code&gt;, esegue comandi in fase di build (installazioni, download)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ENV&lt;/code&gt;, imposta variabili d&#39;ambiente&lt;/li&gt;
&lt;li&gt;&lt;code&gt;EXPOSE&lt;/code&gt;, documenta quali porte usa il container (non le pubblica da sola, serve comunque &lt;code&gt;-p&lt;/code&gt; al run)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CMD&lt;/code&gt;, comando eseguito all&#39;avvio del container (uno solo, sovrascrivibile)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ENTRYPOINT&lt;/code&gt;, simile a &lt;code&gt;CMD&lt;/code&gt; ma meno facilmente sovrascrivibile, spesso usato insieme&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Build e run:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker build -t mio-progetto .
docker run --rm -it -p 4200:4200 mio-progetto
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ogni &lt;code&gt;RUN&lt;/code&gt; che scarica file pesanti (es. un checkpoint AI) li incorpora permanentemente nei layer dell&#39;immagine, aumentandone le dimensioni su disco in modo fisso, diverso dallo scaricarli a runtime in un container effimero, dove occupano spazio solo mentre il container esiste.&lt;/p&gt;
&lt;h3 id=&quot;multi-stage-build&quot;&gt;Multi-stage build&lt;/h3&gt;
&lt;p&gt;Per immagini di produzione, costruire e servire l&#39;applicazione nello stesso stage porta a immagini gonfiate da strumenti di build non necessari a runtime (compilatori, dipendenze di sviluppo). Il pattern multi-stage separa la fase di build da quella di esecuzione, copiando nell&#39;immagine finale solo gli artefatti compilati:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;FROM node:22 AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:22 AS runtime
WORKDIR /app
COPY --from=build /app/dist ./dist
COPY package*.json ./
RUN npm ci --omit=dev
EXPOSE 4200
CMD [&amp;quot;node&amp;quot;, &amp;quot;dist/server.js&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Lo stage &lt;code&gt;build&lt;/code&gt; contiene tutte le dipendenze e i tool necessari alla compilazione, lo stage &lt;code&gt;runtime&lt;/code&gt; eredita solo ciò che serve per far girare l&#39;app, con un&#39;immagine finale sensibilmente più piccola e con una superficie di attacco ridotta.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; Il comando &lt;code&gt;npm ci&lt;/code&gt; installa le dipendenze esattamente come bloccate nel file di lock, a differenza di &lt;code&gt;npm install&lt;/code&gt; che può aggiornarle. È la scelta preferibile in fase di build per garantire riproducibilità. &lt;/div&gt;
&lt;h3 id=&quot;dockerignore&quot;&gt;.dockerignore&lt;/h3&gt;
&lt;p&gt;Così come &lt;code&gt;.gitignore&lt;/code&gt; esclude file dal controllo versione, un file &lt;code&gt;.dockerignore&lt;/code&gt; nella directory del progetto esclude file dal contesto di build inviato al demone Docker:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-text&quot;&gt;node_modules
.git
dist
*.log
.env
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Senza questo file, l&#39;intera cartella &lt;code&gt;node_modules&lt;/code&gt; locale (spesso enorme) viene inviata al demone a ogni build, rallentando il processo e rischiando di sovrascrivere dipendenze già installate correttamente dentro il container.&lt;/p&gt;
&lt;h3 id=&quot;esecuzione-non-privilegiata&quot;&gt;Esecuzione non privilegiata&lt;/h3&gt;
&lt;p&gt;Per default, i processi in un container Docker girano come utente &lt;code&gt;root&lt;/code&gt;. In caso di vulnerabilità nell&#39;applicazione, questo aumenta il potenziale impatto di un attacco. Le immagini ufficiali Node, tra cui &lt;code&gt;node:22&lt;/code&gt;, includono già un utente non privilegiato predefinito che può essere attivato nel Dockerfile:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;FROM node:22
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
USER node
EXPOSE 4200
CMD [&amp;quot;npm&amp;quot;, &amp;quot;start&amp;quot;, &amp;quot;--&amp;quot;, &amp;quot;--host&amp;quot;, &amp;quot;0.0.0.0&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;healthcheck&quot;&gt;Healthcheck&lt;/h3&gt;
&lt;p&gt;L&#39;istruzione &lt;code&gt;HEALTHCHECK&lt;/code&gt; permette a Docker di monitorare autonomamente lo stato di salute di un container, informazione visibile in &lt;code&gt;docker ps&lt;/code&gt; e utilizzata da Docker Compose per gestire l&#39;ordine di avvio dei servizi dipendenti:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;HEALTHCHECK --interval=30s --timeout=5s --retries=3 CMD curl -f http://localhost:4200 || exit 1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Se il comando restituisce un codice diverso da zero per il numero di tentativi configurato, il container viene marcato come &lt;code&gt;unhealthy&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;docker-compose&quot;&gt;Docker compose&lt;/h2&gt;
&lt;p&gt;Per progetti con più servizi collegati (es. backend Spring Boot + database Postgres), Docker Compose evita di scrivere comandi &lt;code&gt;docker run&lt;/code&gt; lunghi e ripetuti.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;docker-compose.yml&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: test
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - &amp;quot;5432:5432&amp;quot;
  backend:
    build: .
    ports:
      - &amp;quot;8080:8080&amp;quot;
    depends_on:
      - db
volumes:
  pgdata:
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Comandi:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker compose up
docker compose up -d
docker compose down
docker compose down -v
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In ordine: avviano tutti i servizi definiti, li avviano in background, fermano tutto, fermano e rimuovono anche i volumi nominati.&lt;/p&gt;
&lt;h2 id=&quot;gpu-su-windows-wsl2&quot;&gt;Gpu su windows (wsl2)&lt;/h2&gt;
&lt;p&gt;Per usare &lt;code&gt;--gpus all&lt;/code&gt; servono tre prerequisiti:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Docker Desktop configurato con backend WSL2 (non Hyper-V puro)&lt;/li&gt;
&lt;li&gt;Driver NVIDIA aggiornati lato Windows (non dentro la distro WSL)&lt;/li&gt;
&lt;li&gt;Integrazione WSL2 abilitata per la distro in uso nelle impostazioni di Docker Desktop&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Se &lt;code&gt;--gpus all&lt;/code&gt; fallisce con un errore relativo al driver, di solito la causa è WSL2 non aggiornato (&lt;code&gt;wsl --update&lt;/code&gt;) o l&#39;integrazione non abilitata per la distro corretta. La sezione 13 approfondisce i comandi WSL utili in questi casi.&lt;/p&gt;
&lt;h2 id=&quot;pulizia-e-gestione-dello-spazio&quot;&gt;Pulizia e gestione dello spazio&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker system df
docker system prune
docker system prune --volumes
docker image prune -a
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In ordine: mostrano quanto spazio occupano immagini, container, volumi e cache di build, rimuovono container fermi/immagini non taggate/cache inutilizzata, fanno lo stesso rimuovendo anche i volumi non referenziati, e rimuovono tutte le immagini non usate da nessun container anche se taggate.&lt;/p&gt;
&lt;p&gt;Su Windows, lo storage effettivo di Docker vive nel disco virtuale di WSL2 (file &lt;code&gt;.vhdx&lt;/code&gt;). Anche dopo un &lt;code&gt;prune&lt;/code&gt;, il file può restare fisicamente grande sul disco Windows finché non viene compattato:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;wsl --shutdown
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;seguito da un&#39;ottimizzazione del vhdx tramite &lt;code&gt;diskpart&lt;/code&gt; o strumenti dedicati, se lo spazio recuperato non basta. La sezione 13 include anche un comando WSL nativo per ridurre dinamicamente questo spazio.&lt;/p&gt;
&lt;h2 id=&quot;sicurezza-dellimmagine&quot;&gt;Sicurezza dell&#39;immagine&lt;/h2&gt;
&lt;p&gt;Oltre alle buone pratiche di costruzione (multi-stage, utente non privilegiato, &lt;code&gt;.dockerignore&lt;/code&gt;), è buona norma scansionare periodicamente le immagini alla ricerca di vulnerabilità note nelle dipendenze e nei pacchetti di sistema inclusi, prima di promuoverle in produzione:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;trivy image &amp;lt;nome-immagine&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; La scansione delle immagini dovrebbe far parte della pipeline CI/CD, non essere un controllo manuale occasionale: bloccare automaticamente il deploy di immagini con vulnerabilità critiche note riduce sensibilmente la superficie di rischio. &lt;/div&gt;
&lt;h2 id=&quot;casi-pratici-di-riferimento&quot;&gt;Casi pratici di riferimento&lt;/h2&gt;
&lt;p&gt;Ambiente ComfyUI completamente effimero (nessuna persistenza, download ogni volta):&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker run --rm -it --gpus all -p 8188:8188 --name comfyui-ephemeral yanwk/comfyui-boot:cu124-slim
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ambiente ComfyUI con modelli persistenti tra un test e l&#39;altro nella stessa giornata:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker volume create test-vol
docker run --rm -it --gpus all -p 8188:8188 -v test-vol:/root --name comfyui-ephemeral yanwk/comfyui-boot:cu124-slim
docker volume rm test-vol
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Sviluppo Angular senza Node installato su Windows:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker run --rm -it -v &amp;quot;${PWD}:/app&amp;quot; -v node-cache:/app/node_modules -w /app -p 4200:4200 node:22 npm start -- --host 0.0.0.0
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Database Postgres per test locali di un backend Spring Boot:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-powershell&quot;&gt;docker volume create pgdata-progetto
docker run --rm -it --name pg-test -e POSTGRES_PASSWORD=test -p 5432:5432 -v pgdata-progetto:/var/lib/postgresql/data postgres:16
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;buone-pratiche&quot;&gt;Buone pratiche&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Usa &lt;code&gt;--rm&lt;/code&gt; di default per container di test, evita accumulo di container fermi dimenticati&lt;/li&gt;
&lt;li&gt;Preferisci volumi nominati ai bind mount quando i dati non devono essere navigabili da Windows (più veloci su WSL2, più facili da eliminare in blocco)&lt;/li&gt;
&lt;li&gt;Assegna nomi univoci a volumi e container per progetto, per evitare collisioni tra progetti diversi&lt;/li&gt;
&lt;li&gt;Non installare dipendenze pesanti in &lt;code&gt;RUN&lt;/code&gt; se servono solo per test occasionali: preferisci scaricarle a runtime in un container effimero&lt;/li&gt;
&lt;li&gt;Controlla periodicamente &lt;code&gt;docker system df&lt;/code&gt; per capire dove va lo spazio, prima che il vhdx di WSL2 diventi ingombrante&lt;/li&gt;
&lt;li&gt;Per progetti multi-servizio, passa a Docker Compose appena i comandi &lt;code&gt;docker run&lt;/code&gt; diventano difficili da ricordare o coordinare a mano&lt;/li&gt;
&lt;li&gt;Usa build multi-stage e un utente non privilegiato nelle immagini destinate alla produzione, non solo negli ambienti di test locali&lt;/li&gt;
&lt;li&gt;Escludi sempre &lt;code&gt;node_modules&lt;/code&gt;, &lt;code&gt;.git&lt;/code&gt; e file sensibili tramite &lt;code&gt;.dockerignore&lt;/code&gt; per build più rapide e immagini più pulite&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;comandi-wsl-utili-per-docker-desktop-su-windows&quot;&gt;Comandi WSL utili per Docker Desktop su Windows&lt;/h2&gt;
&lt;p&gt;Docker Desktop su Windows si appoggia a WSL2 per eseguire i container. Conoscere i comandi WSL principali aiuta a diagnosticare e risolvere i problemi più comuni, oltre a tenere sotto controllo le risorse assegnate.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Comando&lt;/th&gt; &lt;th&gt;Effetto&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot; colspan=&quot;2&quot;&gt;Gestione distribuzioni&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl -l -v&lt;/td&gt; &lt;td&gt;Lista delle distro installate, con versione WSL e stato&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl -l --online&lt;/td&gt; &lt;td&gt;Lista delle distro disponibili da installare&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --install &lt;Distro&gt;&lt;/td&gt; &lt;td&gt;Installa una distro (es. Ubuntu)&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --install --no-distribution&lt;/td&gt; &lt;td&gt;Installa solo la piattaforma WSL, senza distro&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --unregister &lt;Distro&gt;&lt;/td&gt; &lt;td&gt;Disinstalla completamente una distro, cancellandone anche i dati&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --set-default &lt;Distro&gt;&lt;/td&gt; &lt;td&gt;Imposta la distro di default&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --set-version &lt;Distro&gt; 2&lt;/td&gt; &lt;td&gt;Converte una distro da WSL1 a WSL2&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot; colspan=&quot;2&quot;&gt;Avvio e controllo&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl&lt;/td&gt; &lt;td&gt;Avvia la distro di default&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl -d &lt;Distro&gt;&lt;/td&gt; &lt;td&gt;Avvia una distro specifica&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --shutdown&lt;/td&gt; &lt;td&gt;Spegne tutte le istanze WSL, utile dopo problemi con Docker&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --terminate &lt;Distro&gt;&lt;/td&gt; &lt;td&gt;Spegne una singola distro senza toccare le altre&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --status&lt;/td&gt; &lt;td&gt;Mostra la versione di default e lo stato generale&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot; colspan=&quot;2&quot;&gt;Aggiornamenti&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --update&lt;/td&gt; &lt;td&gt;Aggiorna il kernel Linux di WSL&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --update --pre-release&lt;/td&gt; &lt;td&gt;Aggiorna alla versione pre-release, utile per bug fix recenti&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --version&lt;/td&gt; &lt;td&gt;Mostra le versioni di WSL, kernel e componenti correlati&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot; colspan=&quot;2&quot;&gt;Risorse e configurazione&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --manage &lt;Distro&gt; --set-sparse true&lt;/td&gt; &lt;td&gt;Riduce dinamicamente lo spazio occupato dal vhdx&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot; colspan=&quot;2&quot;&gt;Diagnostica rapida per problemi Docker&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl -l -v&lt;/td&gt; &lt;td&gt;Verifica che docker-desktop e docker-desktop-data siano presenti e in stato Running&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;wsl --status&lt;/td&gt; &lt;td&gt;Controlla che la versione di default sia 2, non 1&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;p&gt;Per limitare RAM e CPU assegnate a WSL2, utile se Docker consuma troppa memoria, si crea o modifica un file &lt;code&gt;.wslconfig&lt;/code&gt; in &lt;code&gt;C:&#92;Users&#92;&amp;lt;utente&amp;gt;&#92;&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ini&quot;&gt;[wsl2]
memory=8GB
processors=4
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Dopo aver modificato il file, è sempre necessario eseguire &lt;code&gt;wsl --shutdown&lt;/code&gt; seguito da un riavvio della distro perché le modifiche abbiano effetto.&lt;/p&gt;


]]></content>
    
    
    
      
      
    <category term="docker"/>
      
    
      
      
    <category term="container"/>
      
    
      
      
    <category term="devops"/>
      
    
  </entry>
  
  <entry>
    <title>Unpacking e assegnazione</title>
    <link href="https://dino-996.github.io/blog/unpacking-e-assegnazione-in-python/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/unpacking-e-assegnazione-in-python/</id>
    <published>2026-07-31T00:00:00.000Z</published>
    <updated>2026-07-31T00:00:00.000Z</updated>
    
    <summary>Esplora le potenzialità dell&#39;unpacking e dell&#39;assegnazione multipla in Python: dallo scambio immediato di variabili all&#39;uso avanzato degli operatori * e ** in cicli, collezioni e funzioni (*args e **kwargs).</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;In Python, l&#39;&lt;strong&gt;unpacking&lt;/strong&gt; (spacchettamento) è un meccanismo potente che consente di estrarre gli elementi contenuti in una struttura dati, come una lista, una tupla o un dizionario, e di assegnarli direttamente a una o più variabili in modo contestuale e leggibile.&lt;/p&gt;
&lt;p&gt;Questa funzionalità si basa sull&#39;iterazione interna degli oggetti. Quando si esegue un&#39;assegnazione multipla, Python itera sulla collezione a destra dell&#39;uguale e distribuisce i valori alle variabili specificate a sinistra, seguendo l&#39;ordine di iterazione.&lt;/p&gt;
&lt;h2 id=&quot;assegnazione-multipla-le-basi&quot;&gt;Assegnazione multipla, le basi&lt;/h2&gt;
&lt;p&gt;L&#39;assegnazione multipla (multiple assignment) permette di assegnare più variabili in una sola istruzione, separando i nomi delle variabili con una virgola. Python esegue automaticamente l&#39;unpacking della struttura dati a destra dell&#39;operatore di assegnazione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;primo, secondo, terzo = (1, 2, 3)
print(primo)
print(secondo)
print(terzo)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Lo stesso comportamento funziona con qualsiasi iterabile, incluse le liste.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;a, b, c = [&amp;quot;rosso&amp;quot;, &amp;quot;verde&amp;quot;, &amp;quot;blu&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Vantaggio chiave:&lt;/strong&gt; questo approccio riduce la verbosità del codice e migliora la leggibilità, specialmente quando si lavora con funzioni che restituiscono tuple o dati strutturati.&lt;/p&gt;
&lt;h3 id=&quot;cosa-succede-quando-il-numero-di-elementi-non-corrisponde&quot;&gt;Cosa succede quando il numero di elementi non corrisponde&lt;/h3&gt;
&lt;p&gt;Un errore comune per chi inizia a lavorare con l&#39;unpacking è dimenticare che il numero di variabili a sinistra deve corrispondere esattamente al numero di elementi dell&#39;iterabile a destra, a meno di non usare l&#39;operatore &lt;code&gt;*&lt;/code&gt; descritto più avanti. Se i conteggi non coincidono, Python solleva un&#39;eccezione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;a, b = [1, 2, 3]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questo codice genera un errore di tipo ValueError, poiché la lista contiene tre elementi ma sono state fornite solo due variabili di destinazione. Conoscere questo comportamento aiuta a scrivere codice più robusto, evitando assunzioni implicite sulla forma dei dati in ingresso.&lt;/p&gt;
&lt;h2 id=&quot;scambio-di-valori-senza-variabile-temporanea&quot;&gt;Scambio di valori senza variabile temporanea&lt;/h2&gt;
&lt;p&gt;Uno degli utilizzi più eleganti dell&#39;assegnazione multipla è lo scambio di valori tra due variabili senza la necessità di una variabile temporanea.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;a = 10
b = 20
a, b = b, a
print(a)
print(b)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Python valuta prima l&#39;intero lato destro dell&#39;espressione, dove &lt;code&gt;b, a&lt;/code&gt; crea una tupla temporanea &lt;code&gt;(20, 10)&lt;/code&gt;, e poi esegue l&#39;unpacking a sinistra. Questo elimina il rischio di errori legati all&#39;uso di variabili temporanee e rende il codice più conciso.&lt;/p&gt;
&lt;h2 id=&quot;unpacking-nei-cicli-for&quot;&gt;Unpacking nei cicli for&lt;/h2&gt;
&lt;p&gt;L&#39;unpacking è estremamente utile quando si iterano su strutture dati composte, come liste di tuple o dizionari.&lt;/p&gt;
&lt;h3 id=&quot;uso-con-dizionari&quot;&gt;Uso con dizionari&lt;/h3&gt;
&lt;p&gt;Il metodo &lt;code&gt;items()&lt;/code&gt; dei dizionari restituisce un oggetto vista contenente coppie chiave-valore sotto forma di tuple. L&#39;unpacking rende l&#39;iterazione molto chiara.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;persona = {&amp;quot;nome&amp;quot;: &amp;quot;Anna&amp;quot;, &amp;quot;eta&amp;quot;: 25}
for chiave, valore in persona.items():
    print(f&amp;quot;{chiave}: {valore}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;uso-con-enumerate&quot;&gt;Uso con enumerate()&lt;/h3&gt;
&lt;p&gt;La funzione &lt;code&gt;enumerate()&lt;/code&gt; è un altro caso classico: restituisce una sequenza di tuple contenenti l&#39;indice e l&#39;elemento corrispondente.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;nomi = [&amp;quot;Anna&amp;quot;, &amp;quot;Luca&amp;quot;, &amp;quot;Marco&amp;quot;]
for indice, nome in enumerate(nomi):
    print(f&amp;quot;{indice}: {nome}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;uso-con-zip&quot;&gt;Uso con zip()&lt;/h3&gt;
&lt;p&gt;Un pattern altrettanto comune consiste nel combinare &lt;code&gt;zip()&lt;/code&gt; con l&#39;unpacking per iterare simultaneamente su più sequenze parallele, ad esempio due liste che rappresentano rispettivamente chiavi e valori raccolti da fonti diverse.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;nomi = [&amp;quot;Anna&amp;quot;, &amp;quot;Luca&amp;quot;, &amp;quot;Marco&amp;quot;]
ruoli = [&amp;quot;Developer&amp;quot;, &amp;quot;Tester&amp;quot;, &amp;quot;Architect&amp;quot;]
for nome, ruolo in zip(nomi, ruoli):
    print(f&amp;quot;{nome} lavora come {ruolo}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;loperatore-nellunpacking-raccogliere-e-ignorare&quot;&gt;L&#39;operatore * nell&#39;unpacking, raccogliere e ignorare&lt;/h2&gt;
&lt;p&gt;L&#39;operatore &lt;code&gt;*&lt;/code&gt; (star) ha un ruolo speciale durante l&#39;unpacking, detto anche unpacking esteso: può essere utilizzato per raccogliere più elementi rimanenti in un&#39;unica variabile sotto forma di lista.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [1, 2, 3, 4, 5]
primo, secondo, *resto = numeri
print(primo)
print(secondo)
print(resto)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;È possibile utilizzare questa tecnica anche per ignorare elementi che non interessano, usando convenzionalmente il nome &lt;code&gt;_&lt;/code&gt; per la variabile di scarto.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;primo, *_, ultimo = numeri
print(primo)
print(ultimo)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;loperatore-per-lespansione-di-collezioni&quot;&gt;L&#39;operatore * per l&#39;espansione di collezioni&lt;/h2&gt;
&lt;p&gt;L&#39;operatore &lt;code&gt;*&lt;/code&gt; può essere utilizzato anche per spacchettare il contenuto di liste o tuple all&#39;interno di una nuova collezione. Questa operazione è nota come unpacking in fase di costruzione.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;a = [1, 2, 3]
b = [4, 5, 6]
unione = [*a, *b]
print(unione)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Lo stesso principio vale per le tuple.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;t1 = (1, 2)
t2 = (3, 4)
t3 = (*t1, *t2)
print(t3)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;loperatore-per-i-dizionari&quot;&gt;L&#39;operatore ** per i dizionari&lt;/h2&gt;
&lt;p&gt;L&#39;operatore &lt;code&gt;**&lt;/code&gt; (doppio star) svolge un ruolo analogo per i dizionari: permette di espandere le coppie chiave-valore all&#39;interno di un nuovo dizionario. È particolarmente utile per unire dizionari o per creare copie con valori sovrascritti.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;dati_personali = {&amp;quot;nome&amp;quot;: &amp;quot;Anna&amp;quot;}
dati_lavoro = {&amp;quot;ruolo&amp;quot;: &amp;quot;Developer&amp;quot;, &amp;quot;azienda&amp;quot;: &amp;quot;TechCorp&amp;quot;}
profilo = {**dati_personali, **dati_lavoro}
print(profilo)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;packing-il-ruolo-opposto-di&quot;&gt;Packing, il ruolo opposto di *&lt;/h2&gt;
&lt;p&gt;Quando l&#39;operatore &lt;code&gt;*&lt;/code&gt; viene utilizzato in un contesto di assegnazione a sinistra, esegue il packing (impacchettamento). Il packing raccoglie più valori in un&#39;unica lista.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;primo, *altri = [10, 20, 30, 40]
print(altri)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;unpacking-e-packing-nelle-funzioni-args-e-kwargs&quot;&gt;Unpacking e packing nelle funzioni, args e kwargs&lt;/h2&gt;
&lt;p&gt;L&#39;unpacking e il packing sono pilastri fondamentali nella definizione e chiamata delle funzioni in Python, consentendo una gestione flessibile degli argomenti.&lt;/p&gt;
&lt;h3 id=&quot;raccogliere-argomenti-posizionali-con-args&quot;&gt;Raccogliere argomenti posizionali con args&lt;/h3&gt;
&lt;p&gt;Quando una funzione deve accettare un numero arbitrario di argomenti posizionali, si utilizza &lt;code&gt;*args&lt;/code&gt; nella firma. Python raccoglie tutti gli argomenti posizionali extra in una tupla.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def somma(*numeri):
    return sum(numeri)
print(somma(1, 2, 3, 4))
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;raccogliere-argomenti-nominati-con-kwargs&quot;&gt;Raccogliere argomenti nominati con kwargs&lt;/h3&gt;
&lt;p&gt;Analogamente, &lt;code&gt;**kwargs&lt;/code&gt; raccoglie tutti gli argomenti passati con nome (keyword arguments) in un dizionario.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def stampa_info(**dati):
    for chiave, valore in dati.items():
        print(f&amp;quot;{chiave}: {valore}&amp;quot;)
stampa_info(nome=&amp;quot;Luca&amp;quot;, eta=30)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;passare-collezioni-come-argomenti-unpacking-in-chiamata&quot;&gt;Passare collezioni come argomenti, unpacking in chiamata&lt;/h3&gt;
&lt;p&gt;Durante la chiamata di una funzione, è possibile fare l&#39;operazione inversa: spacchettare una lista o una tupla in argomenti posizionali con &lt;code&gt;*&lt;/code&gt;, o un dizionario in argomenti nominati con &lt;code&gt;**&lt;/code&gt;. Questo rende il codice molto flessibile e riduce la necessità di wrapper.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;valori = (1, 2, 3)
somma(*valori)
dati = {&amp;quot;nome&amp;quot;: &amp;quot;Anna&amp;quot;, &amp;quot;eta&amp;quot;: 25}
stampa_info(**dati)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;forzare-argomenti-keyword-only-con-lasterisco-isolato&quot;&gt;Forzare argomenti keyword-only con l&#39;asterisco isolato&lt;/h3&gt;
&lt;p&gt;Un pattern legato all&#39;unpacking, ma spesso sottovalutato, consiste nell&#39;uso di un asterisco isolato nella firma di una funzione per forzare gli argomenti successivi a essere passati esclusivamente per nome. Questo migliora la leggibilità delle chiamate e riduce gli errori legati all&#39;ordine dei parametri, un aspetto rilevante per la manutenibilità del codice in team.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def crea_utente(nome, eta, *, ruolo=&amp;quot;guest&amp;quot;):
    return {&amp;quot;nome&amp;quot;: nome, &amp;quot;eta&amp;quot;: eta, &amp;quot;ruolo&amp;quot;: ruolo}
crea_utente(&amp;quot;Anna&amp;quot;, 25, ruolo=&amp;quot;admin&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo esempio, &lt;code&gt;ruolo&lt;/code&gt; può essere fornito soltanto specificandone esplicitamente il nome, non come terzo argomento posizionale.&lt;/p&gt;
&lt;h2 id=&quot;best-practice-e-pattern-avanzati&quot;&gt;Best practice e pattern avanzati&lt;/h2&gt;
&lt;h3 id=&quot;pattern-matching-strutturale&quot;&gt;Pattern matching strutturale&lt;/h3&gt;
&lt;p&gt;Le versioni moderne di Python integrano l&#39;unpacking nei costrutti di pattern matching, consentendo estrazioni complesse in modo dichiarativo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;dati = [&amp;quot;utente&amp;quot;, {&amp;quot;nome&amp;quot;: &amp;quot;Marco&amp;quot;, &amp;quot;ruolo&amp;quot;: &amp;quot;admin&amp;quot;}]
match dati:
    case [&amp;quot;utente&amp;quot;, {&amp;quot;nome&amp;quot;: nome, &amp;quot;ruolo&amp;quot;: ruolo}]:
        print(f&amp;quot;Utente {nome} con ruolo {ruolo}&amp;quot;)
    case _:
        print(&amp;quot;Formato dati non riconosciuto&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;unpacking-con-namedtuple&quot;&gt;Unpacking con NamedTuple&lt;/h3&gt;
&lt;p&gt;L&#39;unpacking si applica in modo naturale anche alle &lt;code&gt;namedtuple&lt;/code&gt;, una struttura dati molto usata per modellare record leggibili e immutabili, tema rilevante per chi punta a una chiarezza architetturale superiore rispetto a semplici tuple anonime.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from collections import namedtuple
Punto = namedtuple(&amp;quot;Punto&amp;quot;, [&amp;quot;x&amp;quot;, &amp;quot;y&amp;quot;])
p = Punto(3, 4)
x, y = p
print(x)
print(y)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;design-pattern-e-python&quot;&gt;Design pattern e Python&lt;/h3&gt;
&lt;p&gt;L&#39;unpacking e l&#39;espansione di collezioni sono alla base di numerosi design pattern:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Factory Pattern:&lt;/strong&gt; utilizzare l&#39;unpacking per disaccoppiare la creazione di oggetti.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Decorator Pattern:&lt;/strong&gt; &lt;code&gt;*args&lt;/code&gt; e &lt;code&gt;**kwargs&lt;/code&gt; permettono di scrivere decoratori generici che preservano la firma delle funzioni originali.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Adapter Pattern:&lt;/strong&gt; l&#39;uso di &lt;code&gt;**&lt;/code&gt; per adattare dizionari di configurazione a firme di funzioni è una pratica comune per rendere le interfacce più flessibili.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;una-nota-sulle-prestazioni&quot;&gt;Una nota sulle prestazioni&lt;/h3&gt;
&lt;p&gt;Dal punto di vista dell&#39;efficienza del codice, l&#39;unpacking non introduce un overhead significativo rispetto all&#39;accesso per indice: il costo computazionale resta paragonabile, mentre il guadagno in leggibilità e in riduzione della probabilità di errori umani è concreto. Per questo motivo l&#39;unpacking è generalmente preferibile quando la struttura dei dati è nota e stabile.&lt;/p&gt;
&lt;h2 id=&quot;attenzione-usi-distinti-di-e&quot;&gt;Attenzione, usi distinti di * e **&lt;/h2&gt;
&lt;p&gt;È fondamentale non confondere i diversi usi di &lt;code&gt;*&lt;/code&gt; e &lt;code&gt;**&lt;/code&gt;, che ricorrono in due contesti completamente separati del linguaggio:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Operatori matematici:&lt;/strong&gt; &lt;code&gt;2 * 3&lt;/code&gt; per la moltiplicazione, &lt;code&gt;2 ** 3&lt;/code&gt; per l&#39;elevamento a potenza.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Operatori di unpacking e packing:&lt;/strong&gt; &lt;code&gt;*valori&lt;/code&gt; e &lt;code&gt;**dati&lt;/code&gt; in contesti di assegnazione, definizione o chiamata di funzione.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Comprendere questa differenza è essenziale per sfruttare appieno una delle caratteristiche più potenti e caratteristiche dello stile Python, evitando al contempo ambiguità di lettura nel codice.&lt;/p&gt;
&lt;h2 id=&quot;riepilogo-delle-sintassi-principali&quot;&gt;Riepilogo delle sintassi principali&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Contesto&lt;/th&gt;
        &lt;th&gt;Sintassi&lt;/th&gt;
        &lt;th&gt;Comportamento&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Assegnazione multipla&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;a, b = x, y&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Unpacking di iterabili&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Unpacking esteso&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;a, *b = [1,2,3]&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Packing degli elementi rimanenti in b&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Costruzione di collezioni&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;[*a, *b]&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Espansione di iterabili in nuove liste&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Definizione funzione&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;def f(*args, **kwargs)&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Packing di argomenti posizionali e nominati&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Chiamata funzione&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;f(*lista, **dict)&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Unpacking di iterabili e dizionari in argomenti&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Argomenti keyword-only&lt;/td&gt;
        &lt;td&gt;&lt;code&gt;def f(a, b, *, c)&lt;/code&gt;&lt;/td&gt;
        &lt;td&gt;Forza c a essere passato solo per nome&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;

]]></content>
    
    
    
      
      
    <category term="python"/>
      
    
      
      
    <category term="unpacking"/>
      
    
      
      
    <category term="argskwargs"/>
      
    
      
      
    <category term="sintassi"/>
      
    
      
      
    <category term="assegnazione"/>
      
    
  </entry>
  
  <entry>
    <title>Sottoprogrammi</title>
    <link href="https://dino-996.github.io/blog/sottoprogrammi-in-python/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/sottoprogrammi-in-python/</id>
    <published>2026-07-31T00:00:00.000Z</published>
    <updated>2026-07-31T00:00:00.000Z</updated>
    
    <summary>Scopri i pilastri della programmazione procedurale in Python: dalle funzioni e l&#39;organizzazione modulare del codice tramite package, fino all&#39;uso ottimale di strutture dati, comprehension e slicing.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;La programmazione procedurale costituisce uno dei pilastri fondamentali nello sviluppo software in Python. Basata sulla scomposizione di un problema complesso in blocchi di codice riutilizzabili e modulari, questo paradigma permette di scrivere applicazioni manutenibili, efficienti e facilmente testabili.&lt;/p&gt;
&lt;p&gt;In questa guida analizzeremo i concetti chiave della programmazione procedurale in Python, la creazione di sottoprogrammi, l&#39;organizzazione del codice tramite moduli e package, fino all&#39;utilizzo ottimale dei tipi di dati sequenziali e strutturati.&lt;/p&gt;
&lt;h2 id=&quot;sottoprogrammi-e-architettura-delle-funzioni-in-python&quot;&gt;Sottoprogrammi e architettura delle funzioni in Python&lt;/h2&gt;
&lt;p&gt;In Python tutti i sottoprogrammi sono formalmente definiti come funzioni. Non esiste una distinzione sintattica rigida tra &amp;quot;procedure&amp;quot; e &amp;quot;funzioni&amp;quot;, qualsiasi blocco di codice definito tramite la parola chiave &lt;code&gt;def&lt;/code&gt; restituisce sempre un valore. Quando una funzione non contiene un&#39;istruzione &lt;code&gt;return&lt;/code&gt; esplicita, il linguaggio restituisce automaticamente l&#39;oggetto &lt;code&gt;None&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Dal punto di vista architetturale, le funzioni in Python sono oggetti di prima classe (&lt;em&gt;first-class objects&lt;/em&gt;). Questo significa che possono essere assegnate a variabili, passate come argomenti ad altre funzioni o restituite come valori di ritorno, consentendo l&#39;implementazione di pattern avanzati come le &lt;em&gt;closure&lt;/em&gt;.&lt;/p&gt;
&lt;h3 id=&quot;sintassi-e-documentazione-delle-funzioni&quot;&gt;Sintassi e documentazione delle funzioni&lt;/h3&gt;
&lt;p&gt;La definizione di una funzione richiede una sintassi chiara. È buona norma integrare immediatamente sotto la firma della funzione un literal stringa denominato docstring, utilizzato per documentare lo scopo dell&#39;algoritmo secondo la convenzione PEP 257.&lt;/p&gt;
&lt;p&gt;Sintassi base di definizione:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def nome_funzione(parametro1: float, parametro2: float) -&amp;gt; float:
    &amp;quot;&amp;quot;&amp;quot;Descrizione sintetica del comportamento della funzione.&amp;quot;&amp;quot;&amp;quot;
    risultato = parametro1 + parametro2
    return risultato
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Esempio pratico per il calcolo del discriminante di un&#39;equazione di secondo grado:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def discriminante(a: float, b: float, c: float) -&amp;gt; float:
    &amp;quot;&amp;quot;&amp;quot;Calcola il discriminante (delta) di un&#39;equazione di secondo grado.&amp;quot;&amp;quot;&amp;quot;
    return (b ** 2 - 4 * a * c)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;valutazione-e-passaggio-dei-parametri&quot;&gt;Valutazione e passaggio dei parametri&lt;/h3&gt;
&lt;p&gt;Il modello di passaggio dei parametri in Python segue la semantica del pass-by-assignment (noto anche come &lt;em&gt;pass-by-object-reference&lt;/em&gt;). Gli argomenti passati a una funzione sono riferimenti a oggetti in memoria. Se l&#39;oggetto passato è immutabile, ad esempio interi, stringhe o tuple, le modifiche all&#39;interno della funzione non altereranno la variabile originale. Se l&#39;oggetto è mutabile, ad esempio liste o dizionari, le modifiche apportate nello scope della funzione si rifletteranno sull&#39;oggetto esterno.&lt;/p&gt;
&lt;p&gt;Python offre grande flessibilità nella gestione degli argomenti, consentendo di impostare valori di default e di utilizzare sia la notazione posizionale che quella nominale (&lt;em&gt;keyword arguments&lt;/em&gt;).&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def welcome(nome: str, cdl: str = &#39;Informatica&#39;, anno: str = &#39;primo&#39;) -&amp;gt; str:
    &amp;quot;&amp;quot;&amp;quot;Genera una stringa formattata di benvenuto per lo studente.&amp;quot;&amp;quot;&amp;quot;
    return f&amp;quot;{nome} è uno studente del {anno} anno del cdl in {cdl}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Invocazioni valide della funzione con differenti combinazioni di argomenti:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;messaggio1 = welcome(&#39;Mario&#39;, &#39;Ing. Informatica&#39;, &#39;secondo&#39;)
messaggio2 = welcome(&#39;Mario&#39;)
messaggio3 = welcome(nome=&#39;Mario&#39;, cdl=&#39;Ing. Informatica&#39;, anno=&#39;secondo&#39;)
messaggio4 = welcome(&#39;Mario&#39;, anno=&#39;secondo&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;separatori-dei-parametri-per-il-controllo-delle-api&quot;&gt;Separatori dei parametri per il controllo delle API&lt;/h3&gt;
&lt;p&gt;Per garantire una chiara separazione delle responsabilità e proteggere l&#39;interfaccia delle librerie, Python permette di limitare l&#39;uso dei parametri attraverso i separatori &lt;code&gt;/&lt;/code&gt; e &lt;code&gt;*&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Posizione del parametro&lt;/th&gt;
        &lt;th&gt;Comportamento consentito&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Prima di /&lt;/td&gt;
        &lt;td&gt;Esclusivamente posizionale&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Tra / e *&lt;/td&gt;
        &lt;td&gt;Sia posizionale che nominale&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;Dopo *&lt;/td&gt;
        &lt;td&gt;Esclusivamente nominale&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def wlc(nome: str, /, cdl: str, *, anno: str = &#39;I&#39;) -&amp;gt; str:
    &amp;quot;&amp;quot;&amp;quot;Funzione con vincoli rigidi sulla tipologia di argomenti.&amp;quot;&amp;quot;&amp;quot;
    return f&amp;quot;{nome} - {cdl} - {anno}&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Esempi di chiamate valide ed errate:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;wlc(&#39;Mario&#39;, &#39;Informatica&#39;, anno=&#39;II&#39;)
wlc(&#39;Mario&#39;, cdl=&#39;Informatica&#39;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;funzioni-con-numero-variabile-di-argomenti&quot;&gt;Funzioni con numero variabile di argomenti&lt;/h3&gt;
&lt;p&gt;Oltre ai parametri posizionali e nominali con vincoli rigidi, Python consente di definire funzioni capaci di accettare un numero arbitrario di argomenti tramite le notazioni &lt;code&gt;*args&lt;/code&gt; e &lt;code&gt;**kwargs&lt;/code&gt;. Questo pattern è ampiamente utilizzato nelle librerie standard e di terze parti per costruire interfacce flessibili.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def somma_valori(*args: float) -&amp;gt; float:
    &amp;quot;&amp;quot;&amp;quot;Somma un numero arbitrario di valori numerici.&amp;quot;&amp;quot;&amp;quot;
    return sum(args)
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def stampa_dettagli(**kwargs: str) -&amp;gt; None:
    &amp;quot;&amp;quot;&amp;quot;Stampa una coppia chiave, valore per ogni argomento nominale ricevuto.&amp;quot;&amp;quot;&amp;quot;
    for chiave, valore in kwargs.items():
        print(f&amp;quot;{chiave}, {valore}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;modularita-del-codice-moduli-e-package&quot;&gt;Modularità del codice, moduli e package&lt;/h2&gt;
&lt;p&gt;Man mano che la dimensione del software cresce, l&#39;organizzazione del codice all&#39;interno di un unico file diventa inefficace. Python gestisce la modularità tramite moduli e package.&lt;/p&gt;
&lt;h3 id=&quot;moduli-in-python&quot;&gt;Moduli in Python&lt;/h3&gt;
&lt;p&gt;Un modulo è semplicemente un file di testo contenente codice Python con estensione &lt;code&gt;.py&lt;/code&gt;. Per utilizzare le funzioni e le variabili definite in un altro modulo, si utilizza l&#39;istruzione &lt;code&gt;import&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Supponendo che il file &lt;code&gt;equazioni.py&lt;/code&gt; contenga la definizione della funzione &lt;code&gt;discriminante&lt;/code&gt;, è possibile importare e utilizzare il codice con diverse sintassi:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;import equazioni
risultato = equazioni.discriminante(1, -5, 6)
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;import equazioni as eq
risultato = eq.discriminante(1, -5, 6)
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from equazioni import discriminante
risultato = discriminante(1, -5, 6)
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from equazioni import discriminante as discr
risultato = discr(1, -5, 6)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;L&#39;uso dell&#39;istruzione &lt;code&gt;from modulo import *&lt;/code&gt; è fortemente sconsigliato dalle linee guida ufficiali PEP 8, in quanto inquina il namespace locale e rende difficile tracciare l&#39;origine dei simboli utilizzati.&lt;/p&gt;
&lt;p&gt;Una buona pratica quando un modulo viene sia importato che eseguito direttamente è racchiudere il codice destinato all&#39;esecuzione diretta all&#39;interno del blocco condizionale seguente, così da evitarne l&#39;esecuzione involontaria in fase di import.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;if __name__ == &amp;quot;__main__&amp;quot;:
    risultato = discriminante(1, -5, 6)
    print(risultato)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;risoluzione-dei-moduli-e-organizzazione-in-package&quot;&gt;Risoluzione dei moduli e organizzazione in package&lt;/h3&gt;
&lt;p&gt;Quando viene eseguita un&#39;istruzione &lt;code&gt;import&lt;/code&gt;, l&#39;interprete Python ricerca il modulo richiesto seguendo un ordine preciso.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;La cartella corrente di esecuzione del file.&lt;/li&gt;
&lt;li&gt;I percorsi specificati nella variabile d&#39;ambiente &lt;code&gt;PYTHONPATH&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;I percorsi standard delle librerie di sistema installate nell&#39;ambiente Python, elencati in &lt;code&gt;sys.path&lt;/code&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Un package è una directory organizzata che raggruppa più moduli correlati. Per essere riconosciuta come package strutturato nelle versioni tradizionali di Python, la directory deve contenere un file di inizializzazione denominato &lt;code&gt;__init__.py&lt;/code&gt;, che può anche essere vuoto.&lt;/p&gt;
&lt;p&gt;Esempio di importazione da una struttura a directory nidificata:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;import sound.effects.echo
sound.effects.echo.echofilter(input_bytes, output_bytes)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;tipi-di-dati-sequenziali-e-strutture-di-collezioni&quot;&gt;Tipi di dati sequenziali e strutture di collezioni&lt;/h2&gt;
&lt;p&gt;Python mette a disposizione una serie di tipi di dati nativi pensati per memorizzare collezioni di elementi. La comprensione di queste strutture è fondamentale per ottimizzare le prestazioni e l&#39;allocazione della memoria.&lt;/p&gt;
&lt;h3 id=&quot;panoramica-dei-tipi-sequenziali&quot;&gt;Panoramica dei tipi sequenziali&lt;/h3&gt;
&lt;p&gt;Ogni tipo strutturato supporta sia una sintassi letterale rapida che l&#39;uso della propria funzione costruttrice, come &lt;code&gt;str()&lt;/code&gt;, &lt;code&gt;list()&lt;/code&gt;, &lt;code&gt;tuple()&lt;/code&gt;, &lt;code&gt;set()&lt;/code&gt; e &lt;code&gt;dict()&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;il-tipo-set&quot;&gt;Il tipo set&lt;/h3&gt;
&lt;p&gt;Un &lt;code&gt;set&lt;/code&gt; è una collezione non ordinata di elementi univoci, particolarmente utile quando è necessario garantire l&#39;assenza di duplicati o eseguire in modo efficiente controlli di appartenenza e operazioni insiemistiche.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;prodotti_attivi = {&#39;server&#39;, &#39;switch&#39;, &#39;router&#39;}
prodotti_dismessi = {&#39;hub&#39;, &#39;switch&#39;}
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Operazione&lt;/th&gt; &lt;th&gt;Descrizione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;s.add(x)&lt;/td&gt; &lt;td&gt;Aggiunge l&#39;elemento x all&#39;insieme, se non già presente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;s.remove(x)&lt;/td&gt; &lt;td&gt;Rimuove l&#39;elemento x, sollevando un&#39;eccezione se assente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;s.discard(x)&lt;/td&gt; &lt;td&gt;Rimuove l&#39;elemento x senza sollevare eccezioni se assente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;s1 union s2&lt;/td&gt; &lt;td&gt;Restituisce l&#39;unione tra due insiemi&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;s1 intersection s2&lt;/td&gt; &lt;td&gt;Restituisce l&#39;intersezione tra due insiemi&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;prodotti_attivi.add(&#39;firewall&#39;)
tutti_prodotti = prodotti_attivi.union(prodotti_dismessi)
comuni = prodotti_attivi.intersection(prodotti_dismessi)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Grazie alla struttura interna basata su tabelle hash, l&#39;operazione di verifica di appartenenza, ad esempio &lt;code&gt;&#39;switch&#39; in prodotti_attivi&lt;/code&gt;, risulta generalmente più efficiente rispetto alla stessa verifica eseguita su una lista di dimensioni comparabili.&lt;/p&gt;
&lt;h3 id=&quot;il-tipo-dict&quot;&gt;Il tipo dict&lt;/h3&gt;
&lt;p&gt;Un &lt;code&gt;dict&lt;/code&gt; è una collezione di coppie chiave, valore che consente di associare a ciascun elemento un identificatore univoco anziché una posizione numerica, risultando particolarmente adatto a rappresentare dati strutturati e configurazioni.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;studente = {&#39;nome&#39;: &#39;Mario&#39;, &#39;cdl&#39;: &#39;Informatica&#39;, &#39;anno&#39;: &#39;secondo&#39;}
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Operazione&lt;/th&gt; &lt;th&gt;Descrizione&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d[chiave]&lt;/td&gt; &lt;td&gt;Accede al valore associato alla chiave, sollevando un&#39;eccezione se assente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d.get(chiave, default)&lt;/td&gt; &lt;td&gt;Accede al valore associato alla chiave, restituendo un default se assente&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d.keys()&lt;/td&gt; &lt;td&gt;Restituisce le chiavi presenti nel dizionario&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d.values()&lt;/td&gt; &lt;td&gt;Restituisce i valori presenti nel dizionario&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d.items()&lt;/td&gt; &lt;td&gt;Restituisce le coppie chiave, valore presenti nel dizionario&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;d.update(altro_dict)&lt;/td&gt; &lt;td&gt;Aggiorna il dizionario con le coppie chiave, valore di un altro dizionario&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;corso = studente.get(&#39;cdl&#39;, &#39;Non specificato&#39;)
for chiave, valore in studente.items():
    print(f&amp;quot;{chiave}, {valore}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A partire da Python 3.7 l&#39;ordine di inserimento delle chiavi in un dizionario viene mantenuto in modo stabile, una caratteristica che ha reso il &lt;code&gt;dict&lt;/code&gt; una struttura sempre più utilizzata anche in contesti dove in passato si sarebbe preferita una lista di tuple.&lt;/p&gt;
&lt;h3 id=&quot;restituzione-di-valori-multipli-tramite-tuple&quot;&gt;Restituzione di valori multipli tramite tuple&lt;/h3&gt;
&lt;p&gt;Un pattern ampiamente utilizzato nella programmazione procedurale in Python è la restituzione di più valori da una singola funzione. Mentre in linguaggi come C o Java occorre definire strutture rigide, in Python la funzione può semplicemente restituire una tupla, sfruttando la tecnica dell&#39;unboxing, detta anche &lt;em&gt;tuple unpacking&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;Esempio di ricerca del valore massimo in una lista di misurazioni:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def massima(temperature: list) -&amp;gt; tuple:
    &amp;quot;&amp;quot;&amp;quot;Individua l&#39;indice e il valore massimo all&#39;interno di una sequenza.&amp;quot;&amp;quot;&amp;quot;
    pos_max = 0
    for i in range(1, len(temperature)):
        if temperature[i] &amp;gt; temperature[pos_max]:
            pos_max = i
    return pos_max, temperature[pos_max]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Invocazione e spacchettamento diretto del risultato:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;dati_meteo = [18.2, 22.5, 19.1, 25.4, 21.0]
indice_max, valore_max = massima(dati_meteo)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;comprehension-per-la-costruzione-di-collezioni&quot;&gt;Comprehension per la costruzione di collezioni&lt;/h3&gt;
&lt;p&gt;Accanto ai cicli espliciti, Python offre le comprehension come pattern idiomatico per costruire liste, dizionari e insiemi in modo più conciso e generalmente più performante rispetto a un ciclo &lt;code&gt;for&lt;/code&gt; equivalente.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;dati_meteo = [18.2, 22.5, 19.1, 25.4, 21.0]
sopra_soglia = [t for t in dati_meteo if t &amp;gt; 20]
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;indici_valori = {i: t for i, t in enumerate(dati_meteo)}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;operazioni-avanzate-con-le-liste-e-slicing&quot;&gt;Operazioni avanzate con le liste e slicing&lt;/h2&gt;
&lt;p&gt;La lista è la struttura dati mutabile più utilizzata in Python. Consente l&#39;annidamento arbitrariamente complesso, con liste multi-dimensionali, e mette a disposizione una serie di metodi nativi per la manipolazione degli elementi.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [1, 2, 3, 4]
matrice = [[1, 2], [3, 4]]
valore = matrice[0][0]
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;operazioni-fondamentali-sulle-liste&quot;&gt;Operazioni fondamentali sulle liste&lt;/h3&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt;
  &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt;
    &lt;thead class=&quot;table-dark&quot;&gt;
      &lt;tr&gt;
        &lt;th&gt;Operazione&lt;/th&gt;
        &lt;th&gt;Descrizione&lt;/th&gt;
      &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l[i] e l[-i]&lt;/td&gt;
        &lt;td&gt;Accesso diretto tramite indice positivo o negativo, partendo dal fondo&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l.append(x)&lt;/td&gt;
        &lt;td&gt;Inserisce l&#39;elemento in coda alla lista&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l.insert(i, x)&lt;/td&gt;
        &lt;td&gt;Posiziona l&#39;elemento all&#39;indice i&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l.pop() e l.pop(i)&lt;/td&gt;
        &lt;td&gt;Rimuove e restituisce l&#39;ultimo elemento o quello in posizione i&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l.clear()&lt;/td&gt;
        &lt;td&gt;Svuota completamente la lista&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;len(l)&lt;/td&gt;
        &lt;td&gt;Restituisce il numero di elementi della lista&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
        &lt;td class=&quot;fw-semibold&quot;&gt;l.sort(key=None, reverse=False)&lt;/td&gt;
        &lt;td&gt;Ordina la lista in-place&lt;/td&gt;
      &lt;/tr&gt;
    &lt;/tbody&gt;
  &lt;/table&gt;
&lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;prodotti = [&#39;server&#39;, &#39;switch&#39;, &#39;router&#39;]
prodotti.append(&#39;firewall&#39;)
elemento_rimosso = prodotti.pop(1)
prodotti.sort(reverse=True)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;la-tecnica-dello-slicing&quot;&gt;La tecnica dello slicing&lt;/h3&gt;
&lt;p&gt;Lo slicing è un meccanismo che permette di estrarre sotto-sequenze da liste o stringhe tramite la notazione &lt;code&gt;lista[start:stop]&lt;/code&gt;. L&#39;elemento corrispondente all&#39;indice &lt;code&gt;stop&lt;/code&gt; è sempre escluso.&lt;/p&gt;
&lt;p&gt;Regole operative dello slicing:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Il valore di default di &lt;code&gt;start&lt;/code&gt; è &lt;code&gt;0&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;Il valore di default di &lt;code&gt;stop&lt;/code&gt; è la lunghezza totale della sequenza, ottenibile con &lt;code&gt;len(l)&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;La notazione &lt;code&gt;lista[:i]&lt;/code&gt; estrae gli elementi dall&#39;inizio fino all&#39;indice &lt;code&gt;i-1&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;La notazione &lt;code&gt;lista[i:]&lt;/code&gt; estrae gli elementi dall&#39;indice &lt;code&gt;i&lt;/code&gt; fino alla fine.&lt;/li&gt;
&lt;li&gt;L&#39;espressione &lt;code&gt;lista[:]&lt;/code&gt; crea una copia superficiale, detta &lt;em&gt;shallow copy&lt;/em&gt;, dell&#39;intera lista.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;A differenza dell&#39;accesso diretto tramite indice singolo, che solleva l&#39;eccezione &lt;code&gt;IndexError&lt;/code&gt; in caso di sconfinamento, lo slicing è protetto e gestisce in modo sicuro gli indici fuori intervallo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;stringa = &#39;Prova&#39;
sub1 = stringa[0:3]
sub2 = stringa[2:100]
sub3 = stringa[7:99]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La comprensione dell&#39;indicizzazione a separatori costituisce la chiave visiva per padroneggiare lo slicing, gli indici non vanno intesi come posizioni sugli elementi, ma come delimitatori posizionati prima e dopo ciascun carattere o elemento della sequenza.&lt;/p&gt;


]]></content>
    
    
    
      
      
    <category term="python"/>
      
    
      
      
    <category term="sottoprogrammi"/>
      
    
      
      
    <category term="programmazione procedurale"/>
      
    
      
      
    <category term="moduli"/>
      
    
      
      
    <category term="package"/>
      
    
  </entry>
  
  <entry>
    <title>Controllo del flusso</title>
    <link href="https://dino-996.github.io/blog/controllo-del-flusso-in-python/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/controllo-del-flusso-in-python/</id>
    <published>2026-07-31T00:00:00.000Z</published>
    <updated>2026-07-31T00:00:00.000Z</updated>
    
    <summary>Padroneggia il controllo del flusso in Python: scopri le differenze tra cicli for e while, l&#39;uso idiomatico di enumerate() e zip(), e come ottimizzare la memoria evitando gli anti-pattern di iterazione.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;Nel contesto dello sviluppo software e dell&#39;architettura del codice, le strutture iterative rappresentano uno dei pilastri fondamentali per determinare l&#39;efficienza e la manutenibilità di un&#39;applicazione. In Python, il controllo del flusso iterativo è gestito principalmente da due costrutti, il ciclo &lt;code&gt;while&lt;/code&gt; e il ciclo &lt;code&gt;for&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Comprendere la differenza concettuale ed esecutiva tra iterazione definita e indefinita è essenziale per scrivere codice modulare, sicuro e performante.&lt;/p&gt;
&lt;h2 id=&quot;introduzione-alla-logica-iterativa&quot;&gt;Introduzione alla logica iterativa&lt;/h2&gt;
&lt;p&gt;Python distingue la gestione delle ripetizioni in base alla natura della fonte dei dati e alle condizioni di arresto:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Ciclo &lt;code&gt;while&lt;/code&gt; (iterazione indefinita):&lt;/strong&gt; esegue un blocco di codice fintanto che una condizione booleana rimane vera. Il numero preciso di iterazioni non è noto a priori.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Ciclo &lt;code&gt;for&lt;/code&gt; (iterazione definita):&lt;/strong&gt; attraversa gli elementi appartenenti a un oggetto iterabile (liste, tuple, dizionari, stringhe, generatori), terminando automaticamente all&#39;esaurimento della sequenza.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Entrambe le strutture consentono la personalizzazione del flusso di esecuzione tramite le istruzioni &lt;code&gt;break&lt;/code&gt;, &lt;code&gt;continue&lt;/code&gt; e la clausola &lt;code&gt;else&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;il-ciclo-while-gestione-delle-iterazioni-indefinite&quot;&gt;Il ciclo while, gestione delle iterazioni indefinite&lt;/h2&gt;
&lt;p&gt;Il ciclo &lt;code&gt;while&lt;/code&gt; valuta un&#39;espressione booleana prima di ogni ciclo di esecuzione. La struttura sintattica fondamentale è la seguente:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;condizione = True
while condizione:
    condizione = False

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Di seguito un esempio di conteggio progressivo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;contatore = 0
while contatore &amp;lt; 5:
    print(contatore)
    contatore += 1

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;prevenzione-dei-cicli-infiniti-ed-efficienza-delle-risorse&quot;&gt;Prevenzione dei cicli infiniti ed efficienza delle risorse&lt;/h3&gt;
&lt;p&gt;Nei sistemi di produzione, un ciclo &lt;code&gt;while&lt;/code&gt; privo di un&#39;adeguata condizione di uscita o di uno stato di alterazione delle variabili di controllo può generare un blocco del thread di esecuzione o un consumo anomalo di CPU.&lt;/p&gt;
&lt;p&gt;Esempio di anti-pattern non sicuro:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;while True:
    print(&amp;quot;Elaborazione in corso...&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per garantire la sicurezza e la resilienza del software, è raccomandato implementare meccanismi di salvaguardia basati su limite di tentativi o timeout:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;tentativi_massimi = 5
tentativo_attuale = 0
connessione_stabilita = False
while not connessione_stabilita and tentativo_attuale &amp;lt; tentativi_massimi:
    print(f&amp;quot;Tentativo di connessione {tentativo_attuale + 1}...&amp;quot;)
    tentativo_attuale += 1

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;ottimizzazione-dei-cicli-while-con-il-walrus-operator&quot;&gt;Ottimizzazione dei cicli while con il walrus operator&lt;/h3&gt;
&lt;p&gt;Un pattern moderno per rendere più compatte e leggibili le condizioni di uscita nei cicli &lt;code&gt;while&lt;/code&gt; consiste nell&#39;uso dell&#39;operatore di assegnazione nel contesto di un&#39;espressione (walrus operator), che permette di assegnare e valutare un valore nella stessa riga:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;dati_grezzi = iter([&amp;quot;a&amp;quot;, &amp;quot;b&amp;quot;, &amp;quot;c&amp;quot;, None])
while (elemento := next(dati_grezzi, None)) is not None:
    print(elemento)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questo approccio riduce la duplicazione di codice tra l&#39;inizializzazione della variabile e la sua valutazione all&#39;interno della condizione.&lt;/p&gt;
&lt;h2 id=&quot;il-ciclo-for-iterazione-su-sequenze-e-oggetti-iterabili&quot;&gt;Il ciclo for, iterazione su sequenze e oggetti iterabili&lt;/h2&gt;
&lt;p&gt;Il ciclo &lt;code&gt;for&lt;/code&gt; in Python si discosta dal modello basato su indice tradizionalmente presente in linguaggi come C o Java. Python utilizza un pattern basato su iteratori, accedendo direttamente ai valori forniti dall&#39;oggetto iterabile.&lt;/p&gt;
&lt;p&gt;La sintassi di base è:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for elemento in sequenza:
    pass

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Esempio di iterazione su una lista di stringhe:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;frutti = [&amp;quot;mela&amp;quot;, &amp;quot;banana&amp;quot;, &amp;quot;kiwi&amp;quot;]
for frutto in frutti:
    print(frutto)

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;generazione-di-sequenze-numeriche-con-range&quot;&gt;Generazione di sequenze numeriche con range()&lt;/h3&gt;
&lt;p&gt;Quando occorre eseguire un blocco di codice per un numero determinato di volte senza basarsi su una lista preesistente, si utilizza la funzione built-in &lt;code&gt;range()&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for numero in range(5):
    print(numero)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La funzione &lt;code&gt;range()&lt;/code&gt; non alloca l&#39;intera sequenza in memoria, ma genera i valori on-demand, garantendo un&#39;ottima efficienza di memoria anche con intervalli di grandi dimensioni.&lt;/p&gt;
&lt;h3 id=&quot;iterazione-idiomatica-con-enumerate-e-zip&quot;&gt;Iterazione idiomatica con enumerate() e zip()&lt;/h3&gt;
&lt;p&gt;In contesti in cui è necessario accedere sia all&#39;indice posizionale sia al valore dell&#39;elemento, l&#39;uso di &lt;code&gt;range(len(lista))&lt;/code&gt; è considerato un anti-pattern. La soluzione idiomatica è l&#39;impiego di &lt;code&gt;enumerate()&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;nomi = [&amp;quot;Anna&amp;quot;, &amp;quot;Luca&amp;quot;, &amp;quot;Marco&amp;quot;]
for indice, nome in enumerate(nomi):
    print(f&amp;quot;Posizione {indice}, {nome}&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Per iterare contemporaneamente su più sequenze in modo parallelo e lineare, è possibile combinare &lt;code&gt;enumerate()&lt;/code&gt; con &lt;code&gt;zip()&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;nomi = [&amp;quot;Anna&amp;quot;, &amp;quot;Luca&amp;quot;, &amp;quot;Marco&amp;quot;]
ruoli = [&amp;quot;Developer&amp;quot;, &amp;quot;DevOps&amp;quot;, &amp;quot;Architect&amp;quot;]
for indice, (nome, ruolo) in enumerate(zip(nomi, ruoli)):
    print(f&amp;quot;ID {indice}, {nome}, Ruolo, {ruolo}&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;controllo-del-flusso-break-continue-ed-else-nei-cicli&quot;&gt;Controllo del flusso, break, continue ed else nei cicli&lt;/h2&gt;
&lt;p&gt;Python mette a disposizione tre costrutti per alterare l&#39;esecuzione standard dei cicli.&lt;/p&gt;
&lt;h3 id=&quot;interruzione-immediata-con-break&quot;&gt;Interruzione immediata con break&lt;/h3&gt;
&lt;p&gt;L&#39;istruzione &lt;code&gt;break&lt;/code&gt; termina immediatamente il ciclo più interno in cui è contenuta, trasferendo l&#39;esecuzione alla prima istruzione esterna al ciclo.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for numero in range(10):
    if numero == 5:
        break
    print(numero)

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;salto-delliterazione-con-continue&quot;&gt;Salto dell&#39;iterazione con continue&lt;/h3&gt;
&lt;p&gt;L&#39;istruzione &lt;code&gt;continue&lt;/code&gt; interrompe l&#39;esecuzione del blocco corrente per l&#39;iterazione in corso e passa immediatamente all&#39;iterazione successiva.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for numero in range(5):
    if numero == 2:
        continue
    print(numero)

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;uso-avanzato-della-clausola-else&quot;&gt;Uso avanzato della clausola else&lt;/h3&gt;
&lt;p&gt;Una caratteristica distintiva di Python è la possibilità di associare un blocco &lt;code&gt;else&lt;/code&gt; a un ciclo. Il codice contenuto in &lt;code&gt;else&lt;/code&gt; viene eseguito esclusivamente se il ciclo giunge a completamento naturale, ovvero senza incontrare alcuna istruzione &lt;code&gt;break&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for numero in range(3):
    print(numero)
else:
    print(&amp;quot;Ciclo completato con successo.&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Se il ciclo viene interrotto prematuramente tramite &lt;code&gt;break&lt;/code&gt;, la clausola &lt;code&gt;else&lt;/code&gt; viene ignorata:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for numero in range(3):
    if numero == 1:
        break
else:
    print(&amp;quot;Questo messaggio non verrà stampato.&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Lo stesso meccanismo si applica identicamente al ciclo &lt;code&gt;while&lt;/code&gt;, la clausola &lt;code&gt;else&lt;/code&gt; viene eseguita solo se la condizione booleana diventa falsa senza che sia mai stata incontrata un&#39;istruzione &lt;code&gt;break&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;tentativi = 0
while tentativi &amp;lt; 3:
    tentativi += 1
else:
    print(&amp;quot;Condizione while esaurita senza interruzioni.&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id=&quot;anti-pattern-comuni-e-ottimizzazione-delle-prestazioni&quot;&gt;Anti-pattern comuni e ottimizzazione delle prestazioni&lt;/h2&gt;
&lt;h3 id=&quot;modifica-di-una-sequenza-durante-literazione&quot;&gt;Modifica di una sequenza durante l&#39;iterazione&lt;/h3&gt;
&lt;p&gt;Modificare la dimensione o gli elementi di una lista mentre la si sta percorrendo con un ciclo &lt;code&gt;for&lt;/code&gt; genera comportamenti indeterminati o salti negli indici.&lt;/p&gt;
&lt;p&gt;Anti-pattern da evitare:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [1, 2, 3, 4, 5]
for n in numeri:
    if n % 2 == 0:
        numeri.remove(n)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Approccio corretto tramite filtraggio su una nuova struttura dati o tramite list comprehension:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [1, 2, 3, 4, 5]
numeri_pari_rimossi = [n for n in numeri if n % 2 != 0]

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;comprehension-e-generatori-lalternativa-pythonica-ai-cicli-classici&quot;&gt;Comprehension e generatori, l&#39;alternativa pythonica ai cicli classici&lt;/h3&gt;
&lt;p&gt;Quando lo scopo del ciclo è trasformare o filtrare una sequenza, l&#39;uso delle list comprehension o delle generator expressions migliora la leggibilità e le prestazioni esecutive.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;import sys
quadrati_lista = [x ** 2 for x in range(1000)]
quadrati_generatore = (x ** 2 for x in range(1000))
print(f&amp;quot;Dimensione lista, {sys.getsizeof(quadrati_lista)} byte&amp;quot;)
print(f&amp;quot;Dimensione generatore, {sys.getsizeof(quadrati_generatore)} byte&amp;quot;)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;L&#39;uso dei generatori consente di elaborare flussi di dati di dimensioni arbitrarie riducendo l&#39;impatto sulla memoria RAM dell&#39;infrastruttura. Per generare sequenze infinite in modo lazy, senza dover gestire manualmente un contatore in un ciclo &lt;code&gt;while&lt;/code&gt;, la libreria standard mette a disposizione anche il modulo &lt;code&gt;itertools&lt;/code&gt;, con funzioni come &lt;code&gt;count()&lt;/code&gt; per produrre progressioni numeriche illimitate da consumare con moderazione, tipicamente in combinazione con &lt;code&gt;break&lt;/code&gt; o &lt;code&gt;islice()&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;tabella-comparativa-delle-strutture-iterative&quot;&gt;Tabella comparativa delle strutture iterative&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Caratteristica&lt;/th&gt; &lt;th&gt;Ciclo for&lt;/th&gt; &lt;th&gt;Ciclo while&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Tipo di iterazione&lt;/td&gt; &lt;td&gt;Definita, basata su iterabile&lt;/td&gt; &lt;td&gt;Indefinita, basata su condizione&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Rischio di loop infinito&lt;/td&gt; &lt;td&gt;Minimo, termina con l&#39;esaurimento della sequenza&lt;/td&gt; &lt;td&gt;Presente, richiede clausole di salvaguardia&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Caso d&#39;uso tipico&lt;/td&gt; &lt;td&gt;Attraversamento di liste, tuple, dizionari, generatori&lt;/td&gt; &lt;td&gt;Attesa di uno stato o di un evento esterno&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Supporto a break, continue, else&lt;/td&gt; &lt;td&gt;Sì&lt;/td&gt; &lt;td&gt;Sì&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;considerazioni-architetturali-e-linee-guida-per-la-scelta&quot;&gt;Considerazioni architetturali e linee guida per la scelta&lt;/h2&gt;
&lt;p&gt;Per garantire chiarezza architetturale e manutenibilità all&#39;interno di progetti software complessi, è consigliabile attenersi alle seguenti linee guida:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Prediligere for rispetto a while:&lt;/strong&gt; quando la fonte dati è una sequenza o un iterabile, il ciclo &lt;code&gt;for&lt;/code&gt; elimina il rischio di cicli infiniti accidentali.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Utilizzare while con clausole di salvaguardia:&lt;/strong&gt; assicurarsi che ogni ciclo &lt;code&gt;while&lt;/code&gt; preveda un numero massimo di iterazioni ammesse o un meccanismo di timeout esplicito.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sfruttare il protocollo iteratore:&lt;/strong&gt; evitare l&#39;uso di indici numerici manuali quando è possibile accedere direttamente agli oggetti o utilizzare funzioni helper come &lt;code&gt;enumerate()&lt;/code&gt; e &lt;code&gt;zip()&lt;/code&gt;.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Adottare la valutazione pigra per dataset di grandi dimensioni:&lt;/strong&gt; ricorrere ai generatori quando si processano flussi dati elaborati da I/O o database.&lt;/li&gt;
&lt;/ol&gt;


]]></content>
    
    
    
      
      
    <category term="python"/>
      
    
      
      
    <category term="ciclo for"/>
      
    
      
      
    <category term="ciclo while"/>
      
    
      
      
    <category term="iteratori"/>
      
    
      
      
    <category term="controllo del flusso"/>
      
    
  </entry>
  
  <entry>
    <title>Generatori</title>
    <link href="https://dino-996.github.io/blog/i-generatori-in-python/" rel="alternate" type="text/html"/>
    <id>https://dino-996.github.io/blog/i-generatori-in-python/</id>
    <published>2026-07-31T00:00:00.000Z</published>
    <updated>2026-07-31T00:00:00.000Z</updated>
    
    <summary>Scopri come funzionano i generatori in Python: sfrutta la lazy evaluation e la parola chiave yield per elaborare grandi flussi di dati in streaming, creare pipeline efficienti e ottimizzare l&#39;uso della memoria RAM.</summary>
    
    
    <content type="html"><![CDATA[

  &lt;h2 id=&quot;introduzione&quot;&gt;Introduzione&lt;/h2&gt;
&lt;p&gt;Un &lt;strong&gt;generatore&lt;/strong&gt; (generator) in Python è una funzione o un&#39;espressione che restituisce uno speciale tipo di iteratore chiamato &lt;em&gt;generator iterator&lt;/em&gt;. A differenza delle normali strutture dati, un generatore non calcola e non conserva tutti i suoi valori in memoria in anticipo: genera ogni valore solo quando viene richiesto. Questo comportamento viene definito &lt;strong&gt;lazy evaluation&lt;/strong&gt; (valutazione pigra).&lt;/p&gt;
&lt;h2 id=&quot;generatori-e-iteratori&quot;&gt;Generatori e iteratori&lt;/h2&gt;
&lt;p&gt;Un generatore è un particolare tipo di iteratore, cioè un oggetto che permette di attraversare una sequenza di valori uno alla volta mediante il protocollo di iterazione (i metodi &lt;code&gt;__iter__()&lt;/code&gt; e &lt;code&gt;__next__()&lt;/code&gt;). La differenza sostanziale è che un iteratore standard, come una lista, contiene già tutti i suoi elementi in memoria:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [1, 2, 3, 4, 5]

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Un generatore, invece, produce gli elementi solo quando servono:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def numeri():
    yield 1
    yield 2
    yield 3

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo caso i valori non vengono creati tutti contemporaneamente, ma vengono prodotti progressivamente durante l&#39;iterazione. Il generatore mantiene il proprio stato interno, rendendo possibile la ripresa dell&#39;esecuzione dopo ogni valore prodotto.&lt;/p&gt;
&lt;h2 id=&quot;la-parola-chiave-yield&quot;&gt;La parola chiave yield&lt;/h2&gt;
&lt;p&gt;Una funzione generatrice si comporta come una normale funzione, ma contiene una o più espressioni &lt;code&gt;yield&lt;/code&gt;. La parola chiave &lt;code&gt;yield&lt;/code&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;restituisce temporaneamente un valore;&lt;/li&gt;
&lt;li&gt;sospende l&#39;esecuzione della funzione;&lt;/li&gt;
&lt;li&gt;conserva lo stato corrente (variabili locali, punto di esecuzione, gestione delle eccezioni);&lt;/li&gt;
&lt;li&gt;permette di riprendere l&#39;esecuzione dal punto in cui era stata interrotta.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Esempio base:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def conta():
    yield 1
    yield 2
    yield 3

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Creiamo il generatore:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;generatore = conta()

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;A questo punto la funzione non è ancora stata eseguita: è stato creato un oggetto generatore. Possiamo ottenere i valori uno alla volta con la funzione &lt;code&gt;next()&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;print(next(generatore))
print(next(generatore))
print(next(generatore))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Quando non ci sono più valori disponibili, Python solleva l&#39;eccezione &lt;code&gt;StopIteration&lt;/code&gt;. Per questo motivo, è quasi sempre preferibile iterare con un ciclo &lt;code&gt;for&lt;/code&gt;, che gestisce automaticamente l&#39;eccezione.&lt;/p&gt;
&lt;h2 id=&quot;sospensione-e-ripresa-dello-stato&quot;&gt;Sospensione e ripresa dello stato&lt;/h2&gt;
&lt;p&gt;La caratteristica più importante dei generatori è che mantengono il proprio stato interno tra una chiamata e l&#39;altra. Quando Python incontra uno &lt;code&gt;yield&lt;/code&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;restituisce il valore corrente;&lt;/li&gt;
&lt;li&gt;mette in pausa l&#39;esecuzione;&lt;/li&gt;
&lt;li&gt;salva tutte le informazioni necessarie per riprendere successivamente.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Queste informazioni includono il punto di esecuzione raggiunto, le variabili locali e lo stato delle istruzioni &lt;code&gt;try&lt;/code&gt;. Quando il generatore viene richiamato nuovamente, riparte esattamente dal punto della sospensione.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Esempio con loop e variabile di stato:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def contatore():
    numero = 0
    while numero &amp;lt; 3:
        yield numero
        numero += 1

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for valore in contatore():
    print(valore)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Output:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;0
1
2

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La variabile &lt;code&gt;numero&lt;/code&gt; mantiene il proprio valore tra un &lt;code&gt;yield&lt;/code&gt; e il successivo, dimostrando come lo stato venga preservato.&lt;/p&gt;
&lt;h2 id=&quot;metodi-avanzati-dei-generatori&quot;&gt;Metodi avanzati dei generatori&lt;/h2&gt;
&lt;p&gt;Oltre a &lt;code&gt;next()&lt;/code&gt;, l&#39;oggetto generatore espone un piccolo insieme di metodi che permettono una comunicazione bidirezionale con il chiamante, non solo la produzione passiva di valori.&lt;/p&gt;
&lt;h3 id=&quot;send-inviare-un-valore-al-generatore&quot;&gt;Send: inviare un valore al generatore&lt;/h3&gt;
&lt;p&gt;Il metodo &lt;code&gt;send(valore)&lt;/code&gt; riprende l&#39;esecuzione del generatore e &amp;quot;inietta&amp;quot; un valore nel punto esatto in cui l&#39;espressione &lt;code&gt;yield&lt;/code&gt; era sospesa. Quel valore diventa il risultato dell&#39;espressione &lt;code&gt;yield&lt;/code&gt; stessa.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def contatore_incrementale():
    totale = 0
    while True:
        incremento = yield totale
        if incremento is not None:
            totale += incremento

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;gen = contatore_incrementale()
print(next(gen))
print(gen.send(5))
print(gen.send(10))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il primo &lt;code&gt;next()&lt;/code&gt; è sempre necessario per portare il generatore al primo punto di sospensione, prima di poter usare &lt;code&gt;send()&lt;/code&gt;.&lt;/p&gt;
&lt;h3 id=&quot;throw-sollevare-uneccezione-allinterno-del-generatore&quot;&gt;Throw: sollevare un&#39;eccezione all&#39;interno del generatore&lt;/h3&gt;
&lt;p&gt;Il metodo &lt;code&gt;throw(eccezione)&lt;/code&gt; permette di sollevare un&#39;eccezione esattamente nel punto in cui il generatore è sospeso, così che possa gestirla con un blocco &lt;code&gt;try&lt;/code&gt;/&lt;code&gt;except&lt;/code&gt; interno.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def generatore_robusto():
    try:
        while True:
            yield &amp;quot;valore normale&amp;quot;
    except ValueError:
        yield &amp;quot;eccezione gestita&amp;quot;

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;close-terminare-un-generatore&quot;&gt;Close: terminare un generatore&lt;/h3&gt;
&lt;p&gt;Il metodo &lt;code&gt;close()&lt;/code&gt; interrompe definitivamente il generatore sollevando internamente &lt;code&gt;GeneratorExit&lt;/code&gt;. È utile per liberare risorse quando un generatore non deve più essere consumato, e viene spesso invocato automaticamente dal garbage collector o dai blocchi &lt;code&gt;finally&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&quot;alert alert-info&quot; role=&quot;alert&quot;&gt; La combinazione di &lt;code&gt;yield&lt;/code&gt;, &lt;code&gt;send()&lt;/code&gt;, &lt;code&gt;throw()&lt;/code&gt; e &lt;code&gt;close()&lt;/code&gt; costituisce il protocollo completo su cui, storicamente, sono state costruite le prime forme di coroutine in Python, prima dell&#39;introduzione della sintassi &lt;code&gt;async&lt;/code&gt;/&lt;code&gt;await&lt;/code&gt;. &lt;/div&gt;
&lt;h2 id=&quot;generatori-e-memoria&quot;&gt;Generatori e memoria&lt;/h2&gt;
&lt;p&gt;Il vantaggio principale dei generatori è il basso utilizzo di memoria. Una lista crea e conserva tutti gli elementi contemporaneamente:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = [x for x in range(1000000)]

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Tutti i valori vengono memorizzati in un&#39;unica struttura. Un generatore, invece, produce gli elementi solo quando richiesti:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;numeri = (x for x in range(1000000))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In questo secondo caso Python mantiene solo le informazioni necessarie per generare il valore successivo. Questo rende i generatori particolarmente utili per:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;grandi quantità di dati;&lt;/li&gt;
&lt;li&gt;lettura di file molto grandi;&lt;/li&gt;
&lt;li&gt;elaborazioni in streaming;&lt;/li&gt;
&lt;li&gt;sequenze potenzialmente infinite.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;verificare-la-differenza-con-sysgetsizeof&quot;&gt;Verificare la differenza con sys.getsizeof&lt;/h3&gt;
&lt;p&gt;La differenza di ingombro in memoria non è solo teorica: può essere misurata direttamente con il modulo &lt;code&gt;sys&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;import sys
lista = [x for x in range(10000)]
generatore = (x for x in range(10000))
print(sys.getsizeof(lista))
print(sys.getsizeof(generatore))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;L&#39;oggetto generatore occupa uno spazio costante e trascurabile, indipendentemente dal numero di elementi che potenzialmente può produrre, mentre la lista cresce proporzionalmente al numero di elementi contenuti.&lt;/p&gt;
&lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt; I valori esatti restituiti da &lt;code&gt;sys.getsizeof()&lt;/code&gt; dipendono dalla versione di Python e dall&#39;architettura della macchina: eseguire il codice localmente per ottenere i numeri reali prima di citarli in un contesto pubblicato. &lt;/div&gt;
&lt;h2 id=&quot;generator-expression&quot;&gt;Generator expression&lt;/h2&gt;
&lt;p&gt;Oltre alle funzioni con &lt;code&gt;yield&lt;/code&gt;, Python permette di creare generatori tramite una sintassi simile alle list comprehension.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Lista (valutazione immediata):&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;quadrati = [x * x for x in range(5)]

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Generatore (valutazione pigra):&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;quadrati = (x * x for x in range(5))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;La differenza principale è che &lt;code&gt;[]&lt;/code&gt; crea immediatamente una lista, mentre &lt;code&gt;()&lt;/code&gt; crea un generatore che calcola i valori quando servono.&lt;/p&gt;
&lt;h2 id=&quot;generatori-infiniti&quot;&gt;Generatori infiniti&lt;/h2&gt;
&lt;p&gt;Poiché i generatori producono valori su richiesta, possono rappresentare anche sequenze senza una fine definita.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Esempio di sequenza infinita:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def numeri_infiniti():
    numero = 0
    while True:
        yield numero
        numero += 1

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Questo generatore può continuare indefinitamente senza occupare memoria proporzionale al numero di valori prodotti. Per interrompere l&#39;iterazione, si può utilizzare un &lt;code&gt;break&lt;/code&gt; o un&#39;istruzione condizionale all&#39;esterno.&lt;/p&gt;
&lt;h2 id=&quot;costruire-pipeline-di-elaborazione&quot;&gt;Costruire pipeline di elaborazione&lt;/h2&gt;
&lt;p&gt;I generatori si prestano naturalmente alla creazione di pipeline di elaborazione dati, dove ogni stadio trasforma o filtra il flusso in arrivo. Ad esempio:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def lettore(file_path):
    with open(file_path) as f:
        for riga in f:
            yield riga.strip()

def filtra_righe(iterable, parola_chiave):
    for riga in iterable:
        if parola_chiave in riga:
            yield riga

def trasforma_maiuscolo(iterable):
    for riga in iterable:
        yield riga.upper()

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;flusso = lettore(&amp;quot;dati.txt&amp;quot;)
filtrato = filtra_righe(flusso, &amp;quot;errore&amp;quot;)
maiuscolo = trasforma_maiuscolo(filtrato)
for riga in maiuscolo:
    print(riga)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Ogni passaggio produce un generatore, e i dati fluiscono senza mai caricare l&#39;intero file in memoria.&lt;/p&gt;
&lt;h2 id=&quot;yield-from-per-la-composizione&quot;&gt;Yield from per la composizione&lt;/h2&gt;
&lt;p&gt;Python permette di delegare la generazione di valori a un sub-generatore o a qualsiasi iterabile tramite l&#39;istruzione &lt;code&gt;yield from&lt;/code&gt;. Questo semplifica la composizione di generatori più complessi.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Esempio:&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;def sottogeneratore():
    yield 10
    yield 20

def generatore_principale():
    yield 1
    yield from sottogeneratore()
    yield 30

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;for v in generatore_principale():
    print(v)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;yield from&lt;/code&gt; si occupa automaticamente di iterare il sub-generatore e di propagare eventuali eccezioni, rendendo il codice più pulito e manutenibile.&lt;/p&gt;
&lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt; L&#39;istruzione &lt;code&gt;yield from&lt;/code&gt; è disponibile a partire da Python 3.3 (introdotta dalla PEP 380). Riferimento di versione da confermare prima della pubblicazione. &lt;/div&gt;
&lt;h2 id=&quot;generatori-come-context-manager&quot;&gt;Generatori come context manager&lt;/h2&gt;
&lt;p&gt;Uno degli usi più professionali dei generatori sfrutta esattamente la loro capacità di sospendersi e riprendersi: il decoratore &lt;code&gt;@contextmanager&lt;/code&gt; del modulo &lt;code&gt;contextlib&lt;/code&gt; permette di trasformare un generatore in un context manager utilizzabile con l&#39;istruzione &lt;code&gt;with&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from contextlib import contextmanager

@contextmanager
def apri_risorsa(nome):
    print(&amp;quot;apertura di&amp;quot;, nome)
    try:
        yield nome
    finally:
        print(&amp;quot;chiusura di&amp;quot;, nome)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;with apri_risorsa(&amp;quot;connessione_db&amp;quot;) as risorsa:
    print(&amp;quot;uso di&amp;quot;, risorsa)

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il codice prima dello &lt;code&gt;yield&lt;/code&gt; viene eseguito all&#39;ingresso del blocco &lt;code&gt;with&lt;/code&gt;, mentre il codice nel blocco &lt;code&gt;finally&lt;/code&gt; viene eseguito automaticamente all&#39;uscita, anche in presenza di eccezioni. Questo pattern è largamente usato per gestire in modo sicuro file, connessioni di rete, lock e transazioni.&lt;/p&gt;
&lt;h2 id=&quot;integrazione-con-itertools&quot;&gt;Integrazione con itertools&lt;/h2&gt;
&lt;p&gt;Il modulo &lt;code&gt;itertools&lt;/code&gt; fornisce una serie di strumenti progettati per lavorare con iteratori e generatori, permettendo di costruire cicli di elaborazione efficienti.&lt;/p&gt;
&lt;h3 id=&quot;islice-prelevare-una-porzione-di-un-generatore&quot;&gt;Islice: prelevare una porzione di un generatore&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from itertools import islice

def numeri_infiniti():
    numero = 0
    while True:
        yield numero
        numero += 1

primi_cinque = islice(numeri_infiniti(), 5)
print(list(primi_cinque))

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;cycle-ripetere-una-sequenza-allinfinito&quot;&gt;Cycle: ripetere una sequenza all&#39;infinito&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from itertools import cycle, islice

colori = cycle([&amp;quot;rosso&amp;quot;, &amp;quot;verde&amp;quot;, &amp;quot;blu&amp;quot;])
print(list(islice(colori, 7)))

&lt;/code&gt;&lt;/pre&gt;
&lt;h3 id=&quot;tee-duplicare-un-iteratore-in-piu-flussi-indipendenti&quot;&gt;Tee: duplicare un iteratore in più flussi indipendenti&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from itertools import tee

originale = (x for x in range(5))
copia_a, copia_b = tee(originale, 2)
print(list(copia_a))
print(list(copia_b))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Queste funzioni si combinano perfettamente con i generatori per creare trasformazioni dati complesse con un impatto minimo sulla memoria.&lt;/p&gt;
&lt;h2 id=&quot;generatori-e-programmazione-asincrona&quot;&gt;Generatori e programmazione asincrona&lt;/h2&gt;
&lt;p&gt;Sebbene non siano direttamente utilizzati per la concorrenza, i generatori hanno ispirato il modello delle coroutine in Python, evolutosi poi nelle parole chiave &lt;code&gt;async&lt;/code&gt;/&lt;code&gt;await&lt;/code&gt;. La capacità di sospendere e riprendere l&#39;esecuzione è alla base sia dei generatori che delle funzioni asincrone.&lt;/p&gt;
&lt;p&gt;Python offre anche i cosiddetti async generator, che combinano &lt;code&gt;async def&lt;/code&gt; con &lt;code&gt;yield&lt;/code&gt; per produrre valori in un contesto asincrono, consumabili con &lt;code&gt;async for&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;async def genera_dati_asincroni():
    for i in range(3):
        yield i

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Utilizzo tipico all&#39;interno di codice asincrono:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;async def consuma():
    async for valore in genera_dati_asincroni():
        print(valore)

&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;alert alert-warning&quot; role=&quot;alert&quot;&gt; Gli async generator sono disponibili a partire da Python 3.6 (PEP 525). Riferimento di versione da confermare prima della pubblicazione. &lt;/div&gt;
&lt;p&gt;Comprendere i generatori aiuta quindi ad affrontare con maggiore consapevolezza anche temi più avanzati come la programmazione asincrona e il coordinamento di flussi di dati in tempo reale.&lt;/p&gt;
&lt;h2 id=&quot;uninsidia-comune-lesaurimento-del-generatore&quot;&gt;Un&#39;insidia comune, l&#39;esaurimento del generatore&lt;/h2&gt;
&lt;p&gt;Un generatore può essere iterato una sola volta. Una volta consumati tutti i valori, o interrotta l&#39;iterazione, il generatore è esaurito e non può essere &amp;quot;riavvolto&amp;quot;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;quadrati = (x * x for x in range(5))
print(list(quadrati))
print(list(quadrati))

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Il secondo &lt;code&gt;list(quadrati)&lt;/code&gt; restituisce una sequenza vuota, perché il generatore ha già prodotto tutti i suoi valori. Per iterare più volte sugli stessi dati è necessario ricreare il generatore da capo, oppure, se serve conservare i valori per riutilizzarli, valutare l&#39;uso di una struttura dati tradizionale come una lista.&lt;/p&gt;
&lt;h2 id=&quot;confronto-tra-funzioni-normali-e-generatori&quot;&gt;Confronto tra funzioni normali e generatori&lt;/h2&gt;
&lt;div class=&quot;table-responsive shadow-sm rounded mb-3&quot;&gt; &lt;table class=&quot;table table-bordered table-striped table-hover align-middle mb-0&quot;&gt; &lt;thead class=&quot;table-dark&quot;&gt; &lt;tr&gt; &lt;th&gt;Funzione normale&lt;/th&gt; &lt;th&gt;Generatore&lt;/th&gt; &lt;/tr&gt; &lt;/thead&gt; &lt;tbody&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Restituisce un valore con &lt;code&gt;return&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Produce valori con &lt;code&gt;yield&lt;/code&gt;&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Termina completamente dopo il &lt;code&gt;return&lt;/code&gt;&lt;/td&gt; &lt;td&gt;Sospende e riprende l&#39;esecuzione&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Perde lo stato locale dopo la chiamata&lt;/td&gt; &lt;td&gt;Mantiene lo stato tra le chiamate&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Calcola tutti i risultati subito&lt;/td&gt; &lt;td&gt;Calcola i risultati quando servono&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Può richiedere molta memoria&lt;/td&gt; &lt;td&gt;Usa poca memoria&lt;/td&gt; &lt;/tr&gt; &lt;tr&gt; &lt;td class=&quot;fw-semibold&quot;&gt;Può essere richiamata ripetutamente&lt;/td&gt; &lt;td&gt;Si esaurisce dopo una sola iterazione completa&lt;/td&gt; &lt;/tr&gt; &lt;/tbody&gt; &lt;/table&gt; &lt;/div&gt;
&lt;h2 id=&quot;quando-usare-i-generatori&quot;&gt;Quando usare i generatori&lt;/h2&gt;
&lt;p&gt;I generatori sono particolarmente indicati quando:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;si devono elaborare grandi quantità di dati;&lt;/li&gt;
&lt;li&gt;non è necessario avere tutti i valori contemporaneamente;&lt;/li&gt;
&lt;li&gt;si vuole creare una pipeline di elaborazione efficiente;&lt;/li&gt;
&lt;li&gt;si lavora con dati prodotti progressivamente (streaming);&lt;/li&gt;
&lt;li&gt;si desidera scrivere codice più leggibile e modulare per sequenze complesse;&lt;/li&gt;
&lt;li&gt;si deve gestire in modo sicuro l&#39;apertura e la chiusura di una risorsa, tramite il pattern context manager.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Grazie alla loro capacità di produrre valori su richiesta mantenendo lo stato interno, i generatori rappresentano uno degli strumenti più potenti di Python per scrivere codice efficiente, orientato all&#39;elaborazione sequenziale e facilmente manutenibile.&lt;/p&gt;


]]></content>
    
    
    
      
      
    <category term="python"/>
      
    
      
      
    <category term="generatori"/>
      
    
      
      
    <category term="lazyevaluation"/>
      
    
      
      
    <category term="yieldpython"/>
      
    
      
      
    <category term="ottimizzazione memoria"/>
      
    
  </entry>
  

</feed>